# EVID-0001 — Runtime, schema, and authority-boundary proof

- Captured at: 2026-08-13T14:46:53Z
- Environment: dedicated linked Supabase Free demo project `puwzifjshgfmeregidev`, Singapore (`ap-southeast-1`)
- Migration: `supabase/migrations/20260813141941_slice001_core.sql`
- Contract: `supabase/tests/slice001_core_test.sql`
- Independent reviewer: `task1_schema_reviewer`
- Result: passed

## Verified outcomes

- The pre-migration linked query failed with SQLSTATE `3F000` because `app_private` did not exist.
- The migration dry run named only `20260813141941_slice001_core.sql`; the linked push completed successfully.
- The post-migration linked query passed all 37 pgTAP assertions inside a rolled-back transaction.
- The contract proves exactly eleven server-owned tables, bigint identity primary keys, forced RLS, no `USAGE` or `CREATE` privilege for `PUBLIC`, `anon`, or `authenticated`, indexed foreign keys, integer-cent money, integer-basis-point tax, and timezone-preserving instants.
- Behavioral database checks prove the valid `$5.00 + $0.53 = $5.53` quote, reject incorrect totals, reject duplicate provider events, and reject a second pending vault operation for the same correlation tuple.
- Nine focused Vitest checks passed for runtime environment validation and matching browser/server/proxy Supabase session behavior.
- TypeScript typecheck and ESLint passed.
- `npm audit --omit=dev --audit-level=high` returned `found 0 vulnerabilities`.
- Independent scoped re-review found all three Important security findings and the one Minor pgTAP gap addressed, with no new Critical or Important breakage.

## Bounded limitation

`supabase test db --linked` still requires a local Docker-based `pg_prove` runner. Because Docker is unavailable, the same pgTAP SQL was executed directly against the linked project through the Supabase CLI Management API. The transaction rolled back all test fixtures. This evidence does not claim hosted customer-flow, PSP, accessibility, or production readiness.

No database password, API key, OTP, customer email, provider credential, complete provider identifier, authorization header, or raw private payload is retained in this artifact.

## TASK-0006 candidate replacement-runtime addition — local-only

- Captured at: 2026-08-28T13:22:11Z
- Plan: TASK-0006, exact SHA-256 `5a7b74c75c8c9c4b5f033a8fc6ff4aa6e5cce837e34cd52345bc575f50427ba8`
- Approval: `user:TASK-0006:2026-08-28:plan-approved`
- Test authority: TC-0013
- Artifact: `tracking/evidence/artifacts/EVID-0001/TASK-0006-runtime-foundation.txt`
- Status: candidate local evidence awaiting independent specification and quality review

The replacement foundation pins the approved Vite 8, Express 5, React Router, TypeScript runner, and Supertest versions; builds `dist/server/src/server.js` plus `dist/web/index.html`; starts only the compiled Express service; and retains one Render service contract with Node 24.18.0 and `/api/v1/health` readiness.

The focused TC-0013 suite passed 42 tests. The full local suite passed 142 tests with two existing intentional skips. Typecheck, lint, combined production build, output-path checks, production dependency audit, protected-path diff, development proxy check, and compiled-service route smoke passed. The constant health response, sanitized API/webhook failures, static assets, browser-history fallback, optional capability configuration, public-variable allowlist, and passive reusable C3-G shell are covered by the attached artifact.

This addition does not alter or reinterpret the accepted schema/database evidence above. The verification host ran Node v26.0.0 while package and Render metadata remain pinned to Node 24.18.0, so no Node-24-hosted execution is claimed. This candidate also does not claim hosted Render, linked Supabase, provider, email, webhook-delivery, customer E2E, font-loading, accessibility, mobile, or production-readiness proof. Independent reviewer approval and user acceptance remain pending.
