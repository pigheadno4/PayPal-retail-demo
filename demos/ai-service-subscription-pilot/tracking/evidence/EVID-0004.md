# EVID-0004 — Allowance And Generate Answer Ledger Proof

- Evidence label: `TASK-0004 local E2E from accepted handoff`
- Requirements: REQ-0037, REQ-0038
- Slice: SLICE-001
- Captured: 2026-08-31
- Status: local passing; hosted and provider proof unproved
- Data handling: sanitized counts and client-safe DTO assertions only; no bearer, cookie, email, OTP, provider ID, reusable credential, or database credential retained

## Local Backend And Ledger Proof

The configured Supabase Postgres integration passed with the existing private schema and no migration:

| State | Granted | Reserved | Committed | Available | Terminal proof |
| --- | ---: | ---: | ---: | ---: | --- |
| Activated | 100 | 0 | 0 | 100 | Concurrent activation returned one window. |
| Confirmed/reserved | 100 | 10 | 0 | 90 | Concurrent duplicate client operation had one fixture owner. |
| Deterministic failure | 100 | 0 | 0 | 100 | Release won once; late completion stayed released and returned no answer. |
| Successful completion | 100 | 0 | 10 | 90 | Duplicate commit and late release stayed committed; return read 90. |

All reserve, commit, and release mutations use the documented allowance-first, usage-operation-second lock order. The fixture runner executes outside all database transactions. The final sanitized configured-database counts were unchanged from baseline: 0 allowance windows and 0 usage operations.

## Local API Proof

- `POST /api/v1/me/activation` accepts only the verified bearer identity and server-resolved verified-funded Go Monthly arrangement.
- `GET /api/v1/me/summary` reads persisted state and grants nothing.
- `POST /api/v1/usage/generate-answer` accepts a UUID, one approved prompt key, and literal confirmation. The caller cannot set units, account/window ownership, funding, or output.
- Client-safe responses use strict schemas and `Cache-Control: private, no-store`.
- Missing ownership/state maps to non-enumerating 404/409 responses.
- Production continues to use `createSupabaseTokenVerifier`. Only the dedicated test support server imports the strict test verifier and fixture identities.

Focused shared/domain/route/UI verification passed 5 files / 17 tests. The full regression passed 213 tests with 5 pre-existing/conditional skips. Typecheck, lint, production build, protected-path diff, whitespace, and high-severity production dependency audit passed.

## Local Interaction And Responsive Proof

The dedicated one-worker composition served the compiled Vite application plus real TASK-0004 Express endpoints and configured Postgres. It passed 4/4 Playwright cases:

- desktop success, confirmation, authoritative reserved state, 90-unit completion, dark theme, refresh, and return restoration;
- exact 390-pixel mobile coverage of the same success/return path;
- desktop deterministic failure with released reservation, 100 available, no answer, and light theme;
- exact 390-pixel mobile coverage of the same failure path.

The test records the real activation, summary, and Generate Answer requests and does not intercept TASK-0004 API endpoints. It checks no horizontal document overflow and no browser errors beyond the one expected initial summary 404 before first activation. Fixture-owned rows are deleted after each case and before/after allowance and usage counts must match.

TASK-0008 remained green after the narrow funded-handoff link change: 81 focused unit/integration assertions and 28/28 isolated local Playwright cases passed. No PayPal request, approval, capture, readiness, retry, or fail-closed implementation was changed.

No screenshots were promoted because evidence capture was not explicitly enabled. Playwright retained only ordinary temporary test output during execution.

## Unproved Hosted And Provider Evidence

This evidence does not establish any of the following:

- hosted six-digit OTP delivery or browser-session isolation;
- real PayPal sandbox approval, capture, vault creation, webhook delivery, or future recurring charge;
- Render deployment, production readiness, or complete customer E2E behavior.

The accepted local fixture begins after authentication and verified funding. TASK-0005 owns hosted/provider closure.

## Reproduction

```text
npm test -- shared/src/usage.test.ts server/src/domain/usage/service.test.ts server/src/routes/usage.test.ts web/src/routes/workspace.test.tsx web/src/components/checkout/payment-handoff.test.tsx
node --env-file=.env.local node_modules/vitest/vitest.mjs run server/src/domain/usage/repository.integration.test.ts
node --env-file=.env.local node_modules/@playwright/test/cli.js test --config=playwright.task0004.config.ts tests/e2e/generate-answer.spec.ts
npm test
npm run typecheck
npm run lint
npm run build
npm audit --omit=dev --audit-level=high
git diff --check
```
