# Supabase

Milestone 1 creates the folder structure only.

Milestone 2 will add:
- local Supabase CLI configuration
- private `app` schema migrations
- seed source files
- TypeScript seed runner implementation

Do not commit Supabase service role credentials, database passwords, or project secrets.
