# Debug

Use this file for implementation investigations, root causes, fixes, and verification notes.

Do not store secrets, credentials, private customer data, raw payment tokens, or sensitive PayPal/Supabase keys here.

## Current Stage

Active stage: Milestone 16 QA, UX Review, and Demo Polish. Current investigations should prioritize presentation-readiness blockers: generated media quality, filter metadata correctness, POP MART visual refresh, responsive QA, PayPal SDK/message layout stability, and sandbox/manual demo readiness. M15 Admin Portal work remains backlog unless explicitly selected.

## 2026-06-25

- Render HTTPS QA observability gap: local tests and browser errors were not enough to isolate hosted PayPal failures after the demo moved to `https://retail-demo.onrender.com`; the backend needed safe stage-by-stage evidence that can be copied from Render logs.
- Fix: route PayPal SDK config, client-token, create-order, express-review, and capture events through the shared redacting runtime debug logger, log sanitized server startup context, and include sanitized `error_message` on central `api_request_failed` events. Search Render logs for `server_starting`, `server_started`, `api_request_failed`, `paypal_sdk_config_returned`, `paypal_client_token_planned`, `paypal_client_token_generated`, `paypal_create_order_starting`, `paypal_create_order_prepared`, `paypal_create_order_gateway_created`, `paypal_create_order_recorded`, `paypal_express_review_lookup_starting`, `paypal_express_review_found`, `paypal_capture_starting`, `paypal_capture_prepared`, `paypal_capture_gateway_captured`, `paypal_capture_recorded`, and any matching `*_failed` events.
- Redaction rule: copied Render logs may include debug IDs, route stages, order/payment-session IDs, PayPal order/capture IDs, amount/currency metadata, and safe booleans. They must not include bearer tokens, cart secrets, client tokens, PayPal client secrets, OAuth access tokens, Supabase service keys, or raw buyer PII.
- Hosted smoke finding after pushing the logging slices: `https://retail-demo.onrender.com/api/health?probe=d92396b` returns `200` with debug ID `dbg_d225f9f99abb`, but `GET /api/config?market=US&profile=popmart&probe=d92396b` still returns a buyer-safe `500` with debug ID `dbg_35928edb0419`. Earlier browser evidence in the same hosted session also showed `/api/catalog/products?market=US&profile=popmart` returning `500` with `dbg_efe1384a184b` and `/api/cart?market=US` returning `500` with `dbg_dc5e78355b5a`. Admin runtime logs with the corrected hosted passcode isolate the repeated error message as `Invalid API key` while loading `profile popmart` or `market US`. A read-only local `.env` probe against Supabase project ref `ztuoytnvmsnmomiedbol` returned the expected `popmart` and `generic` profile rows, so the next fix is Render env correction: `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` must be copied from the same valid local credential pair, and the service must redeploy/restart after the update.
- Hosted smoke resolution: after the Render Supabase key/URL correction, read-only HTTPS probes returned 200 for `/api/health`, `/api/config?market=US&profile=popmart`, `/api/catalog/products?market=US&profile=popmart` with 25 products, and `/api/cart?market=US`. Browser QA confirmed Home/Category/PDP/Cart/Checkout entry render from live data without fallback status or relevant console errors.
- Hosted PayPal checkout QA closure: the in-app browser completed a full HTTPS Delivery checkout using the official PayPal sandbox modal. PayPal's secure iframe accepted password input only through keypress-level browser interaction after the field had visible focus; normal frame locators and bulk typing did not enter characters. Approval then returned to the hosted merchant app and captured order `DO-20260625-000001` with PayPal capture ID `70T37391TR665405C`; cart cleanup showed `Cart (0)`. Merchant-owned console health was clean, while PayPal sandbox emitted non-blocking Datadog storage and internal mutation/debug messages from `sandbox.paypal.com`.
- Guest confirmation UX note: the captured order page still shows the optional `Save order to an account` prompt. The use case is guest receipt recovery/order-history linking, but the copy can read account-centric in a post-payment moment; next polish should consider `Save receipt for later` / `Create account for order history` wording and keep it clearly optional below the capture receipt.

## 2026-05-26

- `npm install` produced no output for roughly two minutes in the tool session but completed successfully. Avoid treating a quiet install as failed unless it exits non-zero or leaves no lockfile/modules after enough time.
- Dependency evidence: `@paypal/react-paypal-js@9.2.0` depends on `@paypal/paypal-js@9.7.0`; `node_modules/@paypal/paypal-js/types/v6/index.d.ts` includes `testBuyerCountry?: string` in `BaseCreateInstanceOptions`.
- Adding the Supabase CLI with `npm install supabase --save-dev` required network approval after an initial sandbox DNS failure.
- Supabase CLI `2.101.0` needs access to `~/.supabase` for cache/telemetry files; CLI commands that touch that path require elevated execution in this sandbox.
- `npx supabase db reset --local --no-seed` failed because Docker is not available/running: `Cannot connect to the Docker daemon at unix:///var/run/docker.sock`. Migration apply verification is blocked until Docker Desktop/local Supabase is available.
- `npx supabase migration list --linked` failed without `SUPABASE_DB_PASSWORD`, but `npx supabase db push --linked`, `npx supabase db advisors --linked`, and `npx supabase db query --linked ...` worked through the linked project login role.
- Remote advisor pass initially found `app.set_updated_at` mutable search path and public execution on `public.rls_auto_enable()`. Migration `20260526094945_harden_function_search_paths.sql` fixed those; rerun returned `No issues found`.
- First storefront seed apply failed on `products_max_quantity_check` because unreleased products used `max_quantity_per_order = 0`. Fix: keep release blocking in `release_status` and seed a positive demo cap.
- Second storefront seed apply failed with `ON CONFLICT DO UPDATE command cannot affect row a second time`. Root cause: product image IDs were keyed by product slug only, and product slugs repeat across profiles. Fix: include profile slug in generated product image IDs and add a duplicate-ID regression test.
- `npm run seed:summary` can fail in the sandbox with `listen EPERM` on a `tsx` IPC pipe under `/var/folders/...`. Rerun outside the sandbox for seed-runner verification; the generated data is deterministic and does not need network access.
- First guarded auth seed apply failed because `auth.users.confirmed_at` is a generated column in the linked Supabase project. A follow-up generated-column check also showed `auth.identities.email` is generated from `identity_data`. Fix: do not insert generated auth columns and keep regression coverage in seed SQL tests.

## 2026-05-31

- Express app tests cannot bind a TCP listener in this sandbox: `listen EPERM: operation not permitted 0.0.0.0`. Fix: use an in-process `IncomingMessage`/`ServerResponse` harness for API smoke tests instead of opening a local port.
- Avoid `npx tsx ...` probes when `tsx` is already installed locally but `npx` still attempts registry resolution; network-restricted runs can fail with `ENOTFOUND registry.npmjs.org`.

## 2026-06-01

- During the cart repository TDD pass, the first green attempt preserved stale merged cart price snapshots. Fix: run shared cart refresh immediately after guest-to-authenticated merge and persist the refreshed line prices before returning the cart.
- Server typecheck caught that `CartApiResponse` is currently the generic catalog JSON type; nested typed DTOs such as the cart binding need JSON-compatible index signatures or explicit JSON mapping.
- Checkout repository typecheck/lint caught two useful boundaries: Supabase database rows should stay snake_case and map into shared camelCase helpers explicitly, and repository imports should avoid route input types unless they are referenced directly.
- A linked Supabase read query briefly failed with a TLS handshake timeout while initializing the CLI login role; a single retry succeeded and returned `checkout_drafts = 2`.
- Checkout promo implementation caught an `exactOptionalPropertyTypes` issue: build `PromoEvaluationInput` without `selectedCodes` when no explicit selection exists, rather than passing `selectedCodes: undefined`.
- Guest order lookup remote verification hit the same transient Supabase CLI login-role TLS handshake timeout once; a single retry succeeded and returned `orders = 5` and `guest_order_access = 1`.

## 2026-06-03

- M8 web-shell WIP was intentionally parked in Git stash `m8-web-shell-wip` before continuing Milestone 7 webhook/saved-payment work.
- PayPal webhook verification uses the PayPal verification API instead of local signature math because the local API spec source includes `POST /v1/notifications/verify-webhook-signature` with required notification header fields and `webhook_event`.
- Saved-payment capture handling intentionally runs only for authenticated buyers with `vault_requested = true`; guest and one-time captures do not create saved payment records.
- PayPal webhook processing reserves the `provider + event_id` audit row before mutation so PayPal retries do not repeat saved-payment/order state updates.
- M8 web-shell stash `m8-web-shell-wip` was applied after fast-forwarding `milestone8-web-shell` to merged M7; the stash entry is intentionally kept as a recovery point until the M8 slice is committed and reviewed.

## 2026-06-05

- Card Fields SDK v6 uses PayPal-hosted iframes that fill their parent containers. The checkout card box therefore gives number/expiry/CVV containers stable `min-height` and `width` before rendering `PayPalCardNumberField`, `PayPalCardExpiryField`, and `PayPalCardCvvField`.
- Apple Pay and Venmo are available as React SDK v6 components in `@paypal/react-paypal-js@9.2.0`, but their custom elements only render after SDK hydration and real wallet eligibility. Server-rendered tests therefore assert the wrapper/status/required-component metadata, not a hydrated wallet element.
- The installed React SDK v6 package does not export a Google Pay React button component. Google Pay uses the PayPal JS SDK `createGooglePayOneTimePaymentSession()` plus Google's PaymentsClient-controlled button/payment-data flow, so the current checkout surface is runtime-gated until both the PayPal Google Pay session and Google PaymentsClient are available.

## 2026-06-07

- Done-milestone gap root cause: some M9-M12 UI items were marked complete when the screen layout and component tests existed, but the actual buyer actions were still shell-level. Evidence includes checkout address inputs rendered as `readOnly`, checkout option rows rendered as non-changing fixture choices, section buttons without submit behavior, and PDP/cart/minicart action controls that render without completing the promised cart/payment session behavior.
- Prevention rule: do not close a UI milestone unless every visible action is wired, disabled with a buyer-facing reason, or explicitly recorded as deferred. Completion evidence must include interaction tests or manual verification notes, not only render/snapshot coverage.
- M11.5 interaction testing needed DOM helpers that were not present in the existing test stack. `npm install --save-dev @testing-library/react @testing-library/user-event jsdom` first failed in the sandbox with `ENOTFOUND registry.npmjs.org`; rerunning with network approval succeeded. `npm audit --omit=dev` reports 0 production vulnerabilities. Full dev audit reports a critical Vitest `<4.1.0` advisory; fixing requires a breaking Vitest 4 upgrade, so leave it for a dedicated tooling maintenance slice rather than bundling it into M11.5 behavior work.

## 2026-06-14

- Missing official PayPal surfaces root cause: checkout SDK config plumbing had three local-dev issues (`createApiClient()` ignored `VITE_API_BASE_URL`, Vite `root: "web"` did not load the demo-root `.env`, and Express did not emit CORS headers for the Vite origin). PDP/cart/minicart also remained local HTML express buttons from M11.5, so they route to Review and Confirm without mounting official SDK controls or invoking `/api/paypal/orders/express-delivery`.
- Prevention rule: payment-surface milestones must verify the hydrated SDK/runtime element in browser QA for every promised page placement, not only static text/buttons or route transitions. If an entry point is intentionally shell-only, the task must stay open and the UI must say it is deferred or disabled.
- Supabase distinction: `.env` stores the URL and service-role credentials, but checkout/cart/payment flows still depend on the API server reaching Supabase over HTTPS for cart, checkout draft, promo, shipping, tax, and order data. PayPal SDK config can render from PayPal/env config, but section submit and create-order flows can hang or fail if Supabase fetches fail.
- Live UX audit reopened checkout recovery items: initial checkout should not show the PayPal button before Payment is active; Delivery shipping/billing submits take too long and need visible loading/error behavior; saved sections must collapse with Edit controls; shipping options must not start as saved and must recalculate Order Summary; PayPal popup closes with create-order failure when backend data dependencies fail.
- Process root cause: prior tests asserted route transitions, selected labels, or static rendered text, but did not assert hydrated third-party SDK DOM, live backend success/error states, loading affordances, or exact accordion post-submit state. Future payment UI work needs browser QA plus tests that fail on shell-only controls.
- Checkout recovery root causes: `CheckoutPage` previously rendered the selected payment action from the active draft even when the Payment step was collapsed; `App` sent fixture draft IDs such as `draft_delivery_123` to Supabase-backed checkout endpoints instead of creating a live draft UUID first; and `App` caught checkout draft API failures and returned the previous data, causing `CheckoutPage` to mark failed saves as successful.
- Fix: gate all selected payment actions behind the expanded Payment step, create/reconcile a server checkout draft UUID before live PATCH calls, propagate App checkout update failures back into the accordion, and render inline `role="alert"` retry copy while preserving the edited section.
- Remaining open risk: PayPal create-order failure handling still needs the same visible merchant-side failure treatment, including buyer-safe error copy and debug evidence when Supabase or PayPal dependencies fail.
- Agent-system prevention update: the repo-level demo protocol and standard/complex templates now carry the reusable rule, so future generated demos should ask for multi-step state contracts, official SDK browser evidence, and API loading/success/failure verification before closing UI/payment milestones.
- PDP/cart/minicart express recovery root cause: the M11.5 local route buttons were never replaced by SDK-backed payment controls, and the frontend cart model dropped `cart_public_id`, leaving no active cart binding for express create-order. Fix: preserve `cart_public_id` in `CartData`, add a cart-bound express create-order request builder, mount `DeliveryExpressAction` through `PayPalSdkProviderScope` for PDP/cart/open-minicart, and route to Review and Confirm only from the PayPal approval callback. Also fixed `PayPalSdkProviderScope` to use the App-provided API client by default so SDK config requests are testable and consistent.
- Plan alignment finding: existing M13 items covered generic Supabase/create-order failure and popup feedback, but did not explicitly cover browser cart restore, checkout route cart continuity, minicart quantity editing, or Pay Later v6 eligibility/detail gating. Fix: add M13.1 before capture and align `IMPLEMENTATION_TASKS.md`, `PLAN.md`, `tracking/todos.md`, `tracking/test-cases.md`, `UX_STATE_CONTRACT.md`, and `IMPLEMENTATION_PLAN.md`.
- Live browser audit root cause for checkout draft/cart failures: the browser had fixture cart state with `cartPublicId`, but no persisted raw `cart_client_secret`; frontend API calls did not attach paired `x-cart-id` and `x-cart-secret` headers, while the backend guest cart middleware requires both. Result: checkout draft creation and cart quantity sync can fail even though the header visually shows a cart.
- Checkout submit failure path observed in the browser: Delivery shipping submit attempted `POST /api/checkout/drafts?market=US`, returned a failed API request, and correctly kept an inline retry error visible. The open fix is to bind a real server cart before draft creation so the happy path can advance.
- Cart durability audit: adding an item could update the in-memory header count, but navigation/refresh restored the fixture/default cart because the browser did not persist and reload a server cart binding as source of truth.
- Minicart audit: quantity controls are missing from the drawer, so the minicart cannot yet satisfy the same editable-cart contract as the full cart.
- PayPal surface audit: backend SDK config returned browser-safe PayPal config, including sandbox buyer-country data and components, but local SDK script loading could time out. PayPal official button appeared in cart/minicart after SDK hydration; Pay Later reported readiness in the slot but did not render a visible official Pay Later button, so eligibility/details gating and fallback copy remain open.
- Pickup audit: guest Pickup in the US market showed a GB postcode (`W1F 7JL`), default-address behavior, and a preselected POP MART Soho store before ZIP/postcode submit. Fix requires separate guest/logged-in initial states and active-market-only fixture defaults.
- Computer Use note: Computer Use was useful for Chrome visual confirmation and keyboard-level app observation, but element-click interactions were flaky for this audit. Playwright remained the deterministic tool for click/network/console evidence.
- Computer Use reload timing note: immediately after a browser reload, the cart page can briefly show initial static fixture state (`Storefront ready`) before the async server-cart restore finishes. Treat cart persistence as verified only after the live status reaches `Restored saved cart` or `Prepared guest cart`; otherwise a working restore path can look like a false cart reset.

## 2026-06-15

- Computer Use/Playwright audit evidence: PDP official PayPal and Pay Later controls can appear after server-cart restore and SDK hydration; Delivery checkout can advance shipping address -> billing address -> shipping option -> payment, with submitted sections collapsing and Order Summary shipping totals recalculating after submit.
- Supabase Auth blocker: seeded existing-user sign-in reached the Supabase Auth token endpoint, but the remote project returned HTTP 500 with `Database error querying schema`. The app then showed generic retry copy, so the next fix needs to distinguish remote Supabase configuration/schema failures from normal credential errors before signed-in cart merge can be trusted.
- Minicart viewport regression: in a Playwright desktop viewport, the complementary cart drawer mounted outside the visible viewport (`x` greater than viewport width), making Close/View cart/Checkout unreachable even though the drawer existed in the accessibility tree.
- Pickup market regression: the live US checkout Pickup flow starts correctly with blank ZIP and no preselected store, but ZIP `10012` opened a store modal containing London/GB stores such as POP MART Soho and POP MART Covent Garden. This reopens active-market store filtering despite earlier fixture-default cleanup.
- Checkout payment-state regression: Delivery and Pickup Order Summary can show `PayPal selected` and an empty payment action slot before the Payment method section is active. The summary should stay payment-neutral until the buyer reaches payment and selects a method.
- Smaller UX findings: saved address summaries are cramped after collapse; the auth password branch triggered a browser warning about missing username semantics for password managers; favicon still 404s; the broader POP MART blind-box visual energy remains intentionally queued for Milestone 16.
- Supabase Auth seeded-user root cause: the remote Auth service could sign in users created through the Admin API, but manually seeded `auth.users` rows failed at the token endpoint before credential validation. Diffing Auth-created rows against seeded rows showed Supabase Auth expects token string columns such as `confirmation_token`, `recovery_token`, `email_change`, and `email_change_token_new` to be empty strings, not null. Fix: seed those fields as `""`, patch existing demo-seed rows remotely, and keep regression coverage in `supabase/seed/seed-data.test.ts`.
- Minicart live viewport root cause: the closed drawer stayed in the accessibility tree offscreen; browser tooling could target hidden controls even when the buyer intended the drawer closed. Fix: render the closed minicart shell with `inert`, while open state remains visible at the right viewport edge with `aria-hidden="false"` and clickable controls.
- Pickup market leakage root cause: checkout draft API responses could not carry store cards through to the frontend, and the backend pickup DTO always returned `stores: []`, so `CheckoutPage` fell back to stale static London fixture cards. Fix: repository now returns active stores scoped to the draft market with inventory counts, frontend draft reconciliation maps `pickup.stores` into store cards, and Pickup location submit waits for the updated draft before opening the modal.
- Payment-neutral summary root cause: `CheckoutPage` derived `activeSummary` with the default selected PayPal method on every render, even when the Payment step was collapsed. Fix: Order Summary uses "Choose payment method" until the Payment section is expanded, while the selected PayPal/Pay Later/card/wallet summary still appears after payment selection.
- Live recovery verification on 2026-06-15: Playwright confirmed US ZIP `10012` Pickup modal includes US stores such as New York SoHo and excludes London, Pickup location is `saved` while Store selection is `editing`, initial checkout Order Summary says "Choose payment method", the minicart opens at `x=1127/right=1547` in a `1547px` viewport with quantity controls, and seeded Alice sign-in reaches "Signed in and merged cart."
- After-login PayPal surface regression root cause: M14 cart merge correctly returns an authenticated cart without the guest `cart_client_secret`, but the PDP/cart/minicart delivery-express renderer still required a guest cart secret and the checkout payment actions did not forward bearer/cart request options into create-order callbacks. Fix: delivery express now accepts either guest cart headers or authenticated bearer context, and checkout PayPal, Pay Later, card, Apple Pay/Google Pay/Venmo create-order calls preserve the App-built request options.
- Checkout submit latency root cause: normal Delivery/Pickup save steps waited through a second artificial timer before starting `onDraftUpdate`, so every submit paid avoidable delay before Supabase/API work began. Fix: start the API update immediately, keep the delayed "recalculating totals" label only for genuinely slow responses, then save/collapse or show retryable error from the real result.
- Shipping-option summary regression root cause: radio selection updated only local choice state, while Order Summary displayed only the last backend draft summary until submit. Fix: Order Summary overlays the selected shipping amount and total delta immediately when the buyer changes shipping option, while the backend draft update remains source of truth after submit.

## 2026-06-16

- Live Chrome verification note: after signed-in cart restore, PDP, full cart, minicart, and checkout all rendered official PayPal and Pay Later controls. Pay Later can show `eligibility pending` for a short SDK hydration window, then resolves to the official Pay Later button when eligibility/details return.
- Live checkout verification note: Delivery shipping and billing saves now begin immediately, show the recalculating/saving label only while backend work is pending, collapse on success, and expose Edit on saved sections. Shipping option selection updates the visible Order Summary before submit and remains aligned after backend reconciliation.

## 2026-06-18

- Cart PayPal delivery express flash-out root cause: live browser QA showed the official SDK button created a PayPal payment session but then navigated the current app tab to a sandbox error page saying pop-ups may be turned off. The demo was using SDK `presentationMode="auto"`, which can choose a popup-dependent path in local/in-app browser contexts. Fix: PayPal and Pay Later one-time buyer actions now share a modal presentation constant, with regression coverage for checkout standalone and PDP/cart/minicart delivery express surfaces.
- PayPal create-order debugging gap: buyer-safe inline errors exposed a debug reference, but frontend/backend evidence was too thin to quickly isolate whether failures happened before request construction, during API create-order, during PayPal gateway creation, or during persistence. Fix: delivery express create-order now logs safe structured start/prepared/failure context, and the backend create-order route logs safe route/gateway/recording stages with the same debug ID while redacting secrets and tokens.
- Category Pay Later fallback root cause: the official provider could be ready and the `paypal-message` node mounted, but the local fallback paragraph still existed in the rendered subtree and could appear in accessibility/browser QA evidence. Fix: the PayPal provider scope now owns an explicit fallback slot and renders storefront fallback copy only while SDK config is loading or errored.
- PayPal line-item URL root cause: app/catalog image paths were correct for browser rendering but still relative at the PayPal repository boundary. Fix: line-item PDP URLs and image URLs are resolved against the configured public origin before provider payload construction.

## 2026-06-19

- Cart PayPal delivery express create-order had three layered blockers. First, stale browser guest cart bindings could survive restore and then keep sending a mismatched `x-cart-id` / `x-cart-secret`; fix: treat cart public ID and client secret as a pair, clear invalid stored bindings, reacquire a fresh binding, and use the cart context returned by pre-create refresh. Second, `cartRepository` and `paypalOrderRepository` used different default guest-cart secret hash algorithms; fix: align PayPal order preparation to the shared SHA-256 hash used by cart/checkout repositories. Third, local HTTP origins produced PayPal-invalid shipping callback and line-item image URLs; fix: omit callback config and PayPal item `url` / `image_url` fields when the resolved public origin is not HTTPS.
- Live verification after the fixes: cart PayPal delivery express now runs `/api/cart/refresh`, prepares the PayPal order, reaches `gateway_create`, receives PayPal `PAYER_ACTION_REQUIRED`, records the order, and opens the PayPal sandbox modal. The remaining local constraint is expected: callback-enabled express shipping updates and provider-visible item links/images require `PUBLIC_HTTPS_ORIGIN`.

## 2026-06-20

- Repeat cart PayPal delivery express QA exposed two idempotency root causes after the first fix. First, cart-scoped pending-order lookup used `cart_id + fulfillment_mode + status` with `maybeSingle()`, so a cart that also had checkout-draft pending delivery orders or older express attempts could fail with "multiple rows returned." Fix: cart-scoped lookup now excludes checkout-draft orders, orders by `order_number_sequence` descending, and limits to the latest express pending order. Second, same-payload retry metadata was reused even after a payment session already had a `paypal_order_id`; that collided with the unique PayPal invoice index on a later retry. Fix: same-payload invoice/request reuse is now limited to pre-provider sessions where `paypal_order_id` is still null; consumed provider sessions create a new suffixed attempt such as `DO-...-A2`.
- Live verification after the 2026-06-20 fixes: `/cart` PayPal delivery express refreshed the cart, prepared express order `DO-20260619-000001` with suffixed invoice `DO-20260619-000001-A2`, reached PayPal gateway creation, recorded PayPal order `3TK87288PW762135A` as `PAYER_ACTION_REQUIRED`, and opened the PayPal sandbox login modal. Full approval/capture remains blocked until a sandbox buyer login/session is available; the demo `.env` intentionally does not store buyer login credentials.

## 2026-06-21

- Vite-only Category/PLP blank state root cause: the App initialized the category route with an empty pending model, and the failed `/api/catalog/products` catch path only logged `Catalog products could not be loaded` instead of promoting generated fallback merchandising. A second linked gap meant fallback category cards could navigate to generated slugs such as `/products/blind-boxes-2`, but direct PDP fallback still contained old fixture slugs, so the PDP landed on the not-found state when the backend was unavailable. Fix: promote generated category fallback data on catalog API failure, filter that fallback by the active category query, replace stale Labubu/Skullpanda/Hirono category fallback products with generated PNG products, and allow known generated fallback PDP records to render after product-detail fetch failure.
- Computer Use GUI verification after the fix: `/products` shows three generated fallback products in Vite-only/API-down mode, the first product card links to `/products/blind-boxes-2`, the fallback PDP renders `Molly Blind Boxes 2` instead of the not-found screen, Add to cart opens the minicart with the selected product, clicking outside the controlled shadcn minicart sheet closes it, the mobile menu opens/closes with the X icon, the sign-in dialog opens/closes with the X icon, and the category filter sheet can apply/reset `Blind Boxes`.
- UI/UX review findings from the same pass: controlled 1366px Playwright sizing shows no desktop horizontal overflow, so the narrow in-app Chrome view was a browser/window state artifact rather than app CSS. Real mobile polish remains open: the PLP sort/filter strip can feel cramped, filter sheet choices should read as clear 44px tap rows or selected chips, disabled wishlist affordance needs a buyer-readable reason or tooltip, and expected API-down fallback console noise should be visually/debug-labeled so QA can distinguish Vite-only fallback mode from runtime failure.
- Follow-up UI/UX fix: the mobile PLP sort/filter strip now avoids cramped horizontal scrolling by showing one compact active sort option in page chrome and moving the full filter/sort option set into the shadcn bottom Sheet. Playwright confirmed no horizontal overflow at 375px or 320px, a 44px `Filter & sort` trigger, a 44px active sort chip, and 17 Sheet rows with 44px minimum height. The disabled wishlist control now exposes an accessible reason instead of a silent disabled heart. The duplicate `Cart is refreshing before delivery express checkout.` copy came from the shared renderer returning the same missing-cart `StatusRegion` once for PayPal and once for Pay Later; fix: render that missing-cart status only from the PayPal branch and return `null` from the Pay Later branch until cart access exists.
- Cart/minicart density root cause: the cart summary still presented subtotal, Pay Later copy, checkout, delivery express, and pickup hints as a loose stack, so mobile buyers could reach the summary before the primary checkout CTA. A second a11y/debug gap reused generic PayPal SDK status IDs/copy across adjacent PayPal and Pay Later scopes. Fix: split the cart summary into a compact checkout-path breakdown, move the full cart Checkout CTA before Pay Later/express copy, mirror that primary/secondary action order in minicart, and make PayPal SDK status IDs/copy method-specific.
- Mobile minicart scroll root cause: after the shadcn Sheet migration, the drawer used one grid body where the item list and PayPal/Pay Later action area competed for the same mobile viewport height. The checkout/payment panel could grow to more than half the XS viewport, leaving only one visible item row even when the cart had multiple items. Fix: make the item region a shadcn `ScrollArea`, keep the drawer body overflow-hidden, and cap the XS/S checkout/payment panel with its own internal scroll so item confirmation and payment actions remain independently reachable.

## 2026-06-24

- Full sandbox approval/capture root cause: cart PayPal approval returned to `/checkout/express-review` with a valid payment session, matched merchant/provider amount, and pending order, but the review API returned `PAYPAL_EXPRESS_REVIEW_NOT_FOUND`. Supabase evidence showed the local express order had only same-session `review_confirm` snapshots and no `paypal_shipping_update` snapshot, because local/no-callback mode omits HTTPS PayPal shipping callback config. Fix: express review now prefers same-session `paypal_shipping_update` totals when PayPal callbacks arrive, falls back to the same-session `review_confirm` snapshot for local/no-callback express mode, and still uses the amount guard before capture. The frontend also replaces stale sample order data with a blocked unavailable state if review loading really fails.
- Live verification after the fix: approved PayPal order `5YR26262S4472494N` loaded real Review and Confirm data for merchant order `DO-20260624-000001`, both cart items, `$69.68` total, and amount verified; `Confirm and pay` captured successfully. Database verification showed order `paid`, payment session `captured`, capture ID present, matched totals, a `capture` total snapshot, and source cart items removed. Browser verification showed the confirmation page and then an empty cart.
- Delivery checkout PayPal root cause and previous blocker: checkout PayPal/Pay Later SDK surfaces created PayPal orders but previously had no App-level approval callback bridge, so an approval could not load the pending review snapshot, post capture, render the confirmation, or reload the cart. Fix: standalone PayPal/Pay Later actions now call `onApproved`, and App handles pending-review fetch, amount-guard/capture, confirmation rendering, and cart reload with focused tests. Live GUI then reached the official sandbox approval modal for PayPal order `14F26791XC702383N` / payment session `69630706-f257-4b55-8950-3bf2c0d875ad`, but PayPal sandbox returned 401 before SDK `onApprove`, so the local capture bridge could not run in that browser pass. Evidence screenshot: `/Users/tengtao/Development/demo-projects/checkout-paypal-sandbox-401-modal.png`.
- Separate cart-continuity finding from the same GUI pass: PDP Add to cart can populate the client minicart, but checkout navigation refreshes from the server cart and can restore an empty cart when no server cart item/binding was persisted. Fix: API-loaded PDP data now preserves the backend product id, PDP Add to cart posts `product_id` plus quantity to `/api/cart/items` with the active cart/auth request options, reconciles the returned server cart, and stores the returned cart binding before checkout refresh.
- 2026-06-25 checkout GUI follow-up root causes: Pay Later message could report SDK readiness while the custom element still painted no visible content, so `PayLaterAmountMessage` now falls back after loading/empty-render timeouts and logs placement/amount/country evidence. Checkout promo text showed `Promo calculating` from a pending promo flag even when no discount/code resolved, so frontend summary mapping now displays `No promo applied` unless real promo data exists. Pickup store modal could only render aggregate counts because the checkout store DTO lacked cart-line product names/status; the repository now joins product names into cart items and returns item-level `inventory_lines`. Mini-cart `View cart` looked like stray text because actions were squeezed into uneven columns; the drawer action stack is now one-column with explicit secondary-button styling.
- Delivery checkout PayPal capture closure: after the PDP cart-continuity fix and with a usable sandbox buyer session, Playwright GUI completed PDP Add to cart -> minicart Checkout -> Delivery checkout -> official PayPal approval -> app express-review/capture. Network evidence showed `GET /api/paypal/orders/express-review` 200 and `POST /api/paypal/orders/6GK84058A01890038/capture` 200; response evidence recorded order `DO-20260624-000004`, payment session `66f7e2bd-87bc-43b8-9398-d5a9e6664f0b`, capture `5VE76310HS283401C`, `COMPLETED` statuses, and matched amount guard. Console noise was sandbox-side autofocusing/preload warnings only.
- PDP detail-tab interaction root cause: the shadcn/Radix `TabsContent` panels were force-mounted, but only hidden-state CSS was trusted. A selected trigger could change while stale panel content stayed readable, making the tab UI look non-interactive. Fix: drive explicit `hidden` and `aria-hidden` from the controlled active tab, hide inactive `data-state="inactive"` panels in CSS, and let Radix keep generated `aria-controls`/panel IDs instead of overriding content IDs.

## 2026-06-25

- Hosted Pay Later QA finding: the official Pay Later flow is amount-sensitive. A low `$25.64` checkout correctly showed Pay in 4 ineligible with the provider's eligible purchase range, while an eligible `$154.39` delivery checkout exposed Pay in 4, completed the PayPal-hosted application/agreement, returned to merchant checkout, and captured as order `DO-20260625-000004` / capture `8AG36137NP271803X`.
- Automation/debug note: the in-app Browser could reach the PayPal-hosted Pay in 4 agreement but could not reliably toggle the nested cross-origin agreement checkbox; Playwright could interact with the nested PayPal frame and completed the same sandbox flow. Treat this as a QA-tooling limitation, not a merchant-app defect.
- UX follow-up from the same pass: direct official Pay Later button interaction completed the flow, but the checkout selected-payment primary action can still feel ambiguous when PayPal/Pay Later is selected. It should focus or trigger the mounted official provider button, or clearly tell the buyer to continue through the official provider surface.
- Checkout polish follow-up root cause: shipping/billing submits felt like they were hanging because the expanded form stayed visible until the checkout draft API save/recalculation completed. Local API-backed browser timing on 2026-06-25 showed the UI can collapse the completed section in `~30-38ms`, while the next section still waits on real backend reconciliation (`~2.3s` for billing, `~4.3s` for shipping in this run). Fix: collapse to concise summaries immediately, preserve semantic saving/recalculating state for tests, keep retryable inline errors on real failures, and leave backend/recalculation optimization as the remaining performance lever.
- Checkout payment UX root cause: inactive payment placeholders and local selected-heading wrappers made the Order Summary look like a fake payment surface before an official provider action was ready. Fix: hide inactive payment slots, render official PayPal/Pay Later actions directly when selected, keep Pay Later's official message directly below the selected action, and replace the mobile sticky `Continue` button with the selected official non-card payment action.
- Checkout selected Pay Later blank-message root cause: the amount message used passive `<paypal-message auto-bootstrap>` rendering, so PayPal sandbox presentment-message failures could leave the Order Summary/mobile sticky Pay Later action with a visible button but no buyer-facing message under it. Fix: use `usePayPalMessages().handleFetchContent()` with explicit `setContent()` application, structured fetch/apply error logs, and fallback copy only when official PayPal message content fails.
- Checkout Pay Later empty-ready root cause: PayPal sandbox can mount a `paypal-message` element and report readiness while both light DOM and shadow DOM remain empty after a presentment failure. Current guardrail: fetch each message once per amount/country/currency/placement key, apply returned content with `setContent()`, and keep buyer-safe fallback visible on SDK config, fetch, apply, or timeout failure instead of treating host-element presence as success.
- Pickup date stale-seed root cause: Supabase seed rows still used May 2026 store pickup dates, so the shadcn Calendar opened on past dates even after the UI change. Fix: checkout repository normalizes past-only pickup date windows into a rolling window from the current checkout date and validates selection against that same normalized window.
- Pickup date submit root cause: the Calendar visually defaulted to the first available date, but the underlying choice was not marked selected until the buyer clicked a date, causing an empty submit payload. Fix: pickup-date submit falls back to the first available choice and regression coverage asserts the default `YYYY-MM-DD` value is sent.
- Checkout/minicart regression root causes from the follow-up GUI pass: the Pickup store modal looked duplicated because inline `step.storeCards` were still rendered behind the dialog; the minicart `View cart` button lost its background because the shadcn Sheet portals outside `.buyer-shell` and could not resolve shell-scoped CSS variables; the Apple Pay row used the white `applepay-default.svg` mark on a white payment row; and the manual Pay Later message fetch/fallback path was temporarily split away from storefront placements. The Pay Later storefront auto-bootstrap conclusion from this pass was later superseded by the 2026-06-26 hosted blank-message finding; storefront placements now use the managed fetch/apply/fallback path as well.
- Render mini-cart follow-up: live Render/Safari QA showed `View cart` was fixed but the primary `Checkout` action still existed only in the accessibility tree and painted blank, for the same portaled Sheet variable-scope reason. Fix: add an explicit primary mini-cart action rule with fallback accent/background/border/text colors and stylesheet contract coverage so both buyer actions remain visible outside `.buyer-shell`.
- PayPal sandbox presentment-message evidence from the 2026-06-25 pass: storefront Pay Later placements mounted official `<paypal-message auto-bootstrap="true">` hosts without local fallback copy or manual fetch calls, but the later 2026-06-26 hosted audit showed that host mounting alone could still leave the buyer-visible message blank. Current rule: all buyer-facing Pay Later message placements use managed content fetch/application and fallback copy when PayPal presentment content fails.

## 2026-06-26

- Storefront Pay Later regression finding: hosted Render GUI probing showed Category, PDP, Cart, and open Minicart had visible official Pay Later message elements, but Home only rendered generic trust-strip copy. Root cause: `HomePage` owned `payLaterPromo` data but had no `renderPayLaterPromoMessage` hook, while `CategoryPage` already received the shared storefront PayPal SDK message renderer. Fix: add the Home promo render hook, wire App to call `renderStorefrontPayLaterMessage` with a typed `home-promo` placement, and keep the module low in the mobile order so the first viewport prioritizes toy imagery/release merchandising.
- PDP mobile tab regression finding: the visible vertical tab artifact came from the shadcn/Radix tab rail allowing vertical overflow, while the deeper clipping came from shadcn `Tabs`/`TabsList` utility classes (`w-fit`/content-sized root) making the tab root and tablist measure around `893px` inside a `343px` mobile card. Fix: constrain `.product-detail-tabs__root` and `.product-detail-tabs__nav` to `width: 100%`, hide vertical overflow/scrollbars, and let only horizontal tab scrolling occur inside the rail. Local GUI evidence after the fix: at 390px the shell was `343px`, nav `341px`, horizontal scroll true, and visible vertical scrollbar false; at 320px the shell was `273px`, nav `271px`, horizontal scroll true, and visible vertical scrollbar false.
- PDP review visibility finding: the Customer reviews tab did have seeded review/social-proof content, but QA selectors counted zero review cards because the shadcn `Card` instances had only the generic `product-review` class. Fix: tag review and social-proof cards with `data-review-card="true"` and add regression coverage.
- Storefront Pay Later blank-message root cause: hosted Render GUI checks on Home, Category, PDP, Cart, and Minicart showed a `paypal-message` host with a visible 28px box and provider status `ready`, but zero light DOM text, zero shadow text, zero visible iframes, no fallback, and no console error. The storefront renderer was still using passive `auto-bootstrap`, while checkout Order Summary used the managed `usePayPalMessages().handleFetchContent()` plus `paypal-message.setContent()` path. Fix: route all Pay Later message placements through the managed fetch/apply/fallback renderer and update regression coverage so host-element presence alone is not sufficient.
- Home release calendar stacking finding: live mobile screenshots showed calendar cells visually competing with the sticky header. Root cause: the sticky site header used a low local z-index (`10`) while the release calendar had no explicit lower stacking context. Fix: raise `.site-header` to `z-index: 40`, keep `.release-calendar` at `z-index: 0`, and stop stretching the adjacent hot-sales section to the full calendar/details column height.
- Checkout payment-logo accessibility finding: adding visual PSP/SCA logo marks inside payment radio labels made accessible names duplicate, for example `Apple PayApple Pay`, because the logo `alt` text was combined with the visible label. Fix: keep the SVG marks in place but make them decorative with `alt=""` and `aria-hidden="true"`, then update tests to assert the visible logo asset source while role queries keep clean payment method names.
