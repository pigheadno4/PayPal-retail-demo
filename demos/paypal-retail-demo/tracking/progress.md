# Progress

## 2026-07-13

- Started Post-Purchase Operations and Account Experience in isolated branch `codex/post-purchase-operations` after the approved Superpowers visual companion mockup and independent mockup review. Added the seven-task execution plan at `docs/superpowers/plans/2026-07-13-post-purchase-operations-account-experience.md`. Before feature edits, fixed fresh-worktree reproducibility by tracking the shadcn `components.json` manifest; the clean baseline then passed `72` test files and `624` tests.
- Closed Task 1 route isolation through TDD and independent read-only review: `/admin` and unknown Admin paths resolve to Orders; Lifecycle, Inventory, Webhooks, and Diagnostics resolve explicitly; each workbench loads only its owned list APIs; active navigation exposes `aria-current="page"`; Orders technical detail excludes lifecycle mutations; Lifecycle excludes technical debug sections; and workbench headings follow one level-2 route heading with nested level-3/4 structure. The reviewer reported no Critical findings, one Important inverse-contract test gap, and one Minor heading-hierarchy issue; both were fixed. Fresh verification passed `72` test files and `627` tests plus typecheck, lint, Prettier, production build, `git diff --check`, and `scripts/check-agent-system.sh`.
- Closed Task 2 typed Admin filters and cursor pagination through TDD and independent review. Pure parsers validate allowed values, strict ISO boundaries, reversible time ranges, IANA timezone echo, `1..100` limits, canonical endpoint-bound cursors, and safe cursor IDs; Webhooks and Runtime Logs default to the last 24 hours. Supabase repositories apply server filters, exact counts, and bounded `limit + 1` keysets. Inventory merges Central/Store streams by `(updated_at, type:id)` without offset or row-cap stalls; Runtime repositories require immutable internal IDs; Stock/Pickup and Payment/Runtime keep independent cursors; and `availability` reaches both Stock and Pickup. The first review found no Critical and four Important cursor/merge/query-ownership gaps; the replacement keyset and required-ID repair received a final PASS with no Critical, Important, or Minor findings. Fresh verification passed `650` tests across `75` files plus typecheck, lint, Prettier, production build, `git diff --check`, and `scripts/check-agent-system.sh`.
- Implemented Task 3 Admin workbenches through failing-first component and App-boundary tests. The shared shell preserves passcode/session ownership and adds active route navigation, refresh/logout, and last-updated context. Desktop filters and a mobile shadcn Sheet restore URL state, expose deterministic chips/clear actions, apply 1h/24h/7d/30d/custom ranges with explicit timezone, close after mobile apply, and drive canonical route-owned requests through `pushState`, `popstate`, retry, and independent cursor keys. Orders/Lifecycle preserve master-detail and merchant actions, Inventory owns Stock/Pickup tabs, Webhooks remain genuine and read-only, and Diagnostics separates Payment/Runtime datasets. Loading, failure/alert/retry, filtered-empty, true-empty, counts, and responsive page-overflow guards are covered. Pre-review verification passed `662` tests across `76` files plus typecheck, lint, Prettier, production build, `git diff --check`, and `scripts/check-agent-system.sh`; independent review is the current checkpoint.
- Addressed the findings from the first Task 3 review, which reported no Critical issues, seven Important gaps, and one Minor active-count issue. Inventory and Diagnostics tabs remain operable through loading/empty/error states; Stock/Pickup load and retry independently; Diagnostics filter definitions, counts, cursors, and requests follow the URL-backed active dataset; Webhooks and Runtime materialize their last-24-hour defaults into the visible URL and filter controls; custom date-times convert through the selected IANA timezone; orphan timezone values do not create false filtered-empty state; and Orders, Lifecycle, and Webhooks render distinct dense semantic tables with contained horizontal scrolling and separate drill-down detail.
- Closed the two Important and two Minor follow-ups found by the first Task 3 re-review. The remaining native Inventory quantity and Pickup availability controls now meet the 44px mobile target; timezone conversion rejects nonexistent DST-gap wall times with an inline blocking error and chooses the earlier instant for repeated fall-back times; Stock and Pickup compute filtered-empty copy from their own request parameters; and Webhook detail uses the full one-column card width after its event list moved into the dense table. Failing-first component/App/style coverage raises the full suite to `674` tests across `76` files. A second independent re-review remains the Task 3 gate before Task 4.
- Fixed the new Important interaction regression found by the second Task 3 re-review after it confirmed the prior four findings closed. Changing the timezone now preserves the entered wall-clock values, reinterprets every populated date boundary under the new IANA zone, updates their canonical ISO instants, and clears or replaces DST-gap errors atomically; a Los Angeles gap changed to UTC now applies successfully without retyping, while switching it back restores the correct blocking error. The final independent re-review remains the Task 3 gate before Task 4.

## 2026-07-12

- Closed the Round 4 hosted Render correction gate. Commit `87f7ec96` deployed as `/assets/index-C1Q5eUuT.js` and `/assets/index-Cw49jF97.css`; `npm run evidence:round4:auth-minicart-checkout` passed against `https://paypal-retail-demo.onrender.com` with `31/31` required rows, `40/40` JPEGs, `missingRows: []`, `failedRows: []`, `4.60:1` minimum contrast, 44px minimum measured target, and `0.0027` maximum near-black ratio. The checksum-locked workspace copy is `.playwright-cli/round4-hosted-auth-minicart-checkout-evidence/` with metrics SHA1 `436b1c6b9b53ad47e3b444966deb740959ed7d6f`. A fresh independent read-only `ui-ux-pro-max` reviewer inspected all `40` hosted captures and returned PASS with no P0/P1/P2 findings. Round 4 is deploy-quality closed; live Card Fields submit/capture and wallet-eligible device checks remain separate action-time QA.

## 2026-07-09

- Continued Round 4 with the minicart drawer polish slice. The drawer keeps shadcn Sheet semantics and outside-click dismissal, adds a product-first row contract, line-clamped product names, compact category/amount metadata, product-specific quantity accessible names, full-width primary Checkout plus framed secondary View cart actions, and keeps Pay Later/express content secondary to the product/action hierarchy. Empty minicarts now keep payment controls hidden behind a clear browse action. Read-only sub-agent review found one P1 focus-return gap and one P2 tracking-wording gap; the app now restores focus to the header cart trigger after close button, Escape, and outside-click dismissal, and the tracking wording distinguishes local implementation from the still-open browser matrix. Focused proof passed `npm test -- web/src/features/cart/MinicartShell.test.tsx web/src/app/App.interactions.test.tsx web/src/styles/global.test.ts`; rendered 320/390/desktop screenshot and sticky-overlap evidence remains in the Round 4 evidence/review gate.

## 2026-07-07

- Continued Round 3 with the deploy-quality evidence helper slice. Added `tools/round3-checkout-pickup-drawer-evidence.playwright.js` and `npm run evidence:round3:checkout-pickup-drawer`. The helper records explicit rows for pickup picker open at 320/390/414/1440, pickup cancel, pickup confirm, preselected Pickup store summary, Delivery billing latency, Delivery billing failure, payment-ready no-method, selected PayPal/Pay Later/Apple Pay/Google Pay/Venmo, expanded drawer rows for those providers, collapsed drawer focus-return state, and selected Card inline. Metrics include `selectedProviderRects`, `pickerHeaderOverlap`, `storeSummaryHasContinue`, `drawerTrigger`, `billingTransitionMs`, `createOrderRequests`, `providerCounts`, horizontal overflow, sticky occlusion targets, request/callback deltas, and route-scoped console/response triage. Updated the Round 3 plan, UX contract, design notes, implementation tasks, todos, and test cases to reflect helper/script completion while keeping full API-backed evidence open. Verification passed `npm test -- server/tests/evidenceScripts.test.ts`, `npm run lint`, and `npm run typecheck`.

## 2026-07-04

- Continued Cart + Checkout A+ Delivery selected-payment QA in the in-app browser with an API-backed cart restored on `/checkout`.
- Continued the final Cart + Checkout A+ proof with a clean browser pass after rebuilding a fresh guest cart through the PDP/cart flow. Direct `/checkout` entry showed cart count `2`, no provider scopes before payment, no reserved selected-payment action, no framework overlay, and `overflowX: 0`; evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260704-final/checkout-entry-clean-390.png`.
- Advanced Delivery checkout to payment at 390px. Default PayPal selection rendered one `paypal:ready` provider on the mobile sticky bar, progress `Delivery - Payment method - 4 of 4`, total `$75.63`, and no horizontal overflow; evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260704-final/checkout-delivery-payment-paypal-default-390.png`.
- Selected Pay Later in the same clean pass. Browser metrics showed selected method `paylater`, one `paylater:ready` provider, one Pay Later message slot for `$75.63`, no fallback duplicate, sticky rect fully in viewport, `overflowX: 0`, and zero warning/error logs after the pass timestamp; evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260704-final/checkout-delivery-paylater-selected-clean-390.png`.
- Reopened Delivery shipping from the selected Pay Later state to verify upstream edit suspension. The checkout returned to `Delivery - Shipping address - 1 of 4`, payment rows unmounted, provider scopes were `0`, `.checkout-sticky-action` was absent, no selected-payment slot was reserved, `overflowX` stayed `0`, and warning/error logs after the pass timestamp remained empty; evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260704-final/checkout-delivery-paylater-upstream-edit-shipping-390.png`.
- Closed the remaining warning-triage and eligibility/error-state proof gap by recording that clean buyer-browser states are warning-free while synthetic missing/stale/recalculating/failed readiness and ineligible wallet states are covered by focused component/App tests rather than a live buyer trigger. The runtime has no safe buyer-facing way to force those failure states without adding a QA hook.
- Captured the selected Pay Later provider matrix at 320, 375, 414, 768, 1024, 1280, and 1440. Every captured state selected `paylater`, rendered one `paylater:ready` provider action, and reported `overflowX: 0`; mobile sticky rects stayed fully in viewport, while 768+ desktop/tablet states rendered without the mobile sticky action. Evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260704/checkout-delivery-paylater-selected-*.png`.
- Captured PayPal/Card comparison states at 320 and 1024. PayPal rendered one `paypal:ready` selected provider action, while Card rendered the inline card box with no mobile sticky action; all comparison states reported `overflowX: 0`. Evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260704/checkout-delivery-paypal-selected-320.png`, `checkout-delivery-card-selected-320.png`, `checkout-delivery-paypal-selected-1024.png`, and `checkout-delivery-card-selected-1024.png`.
- Found and fixed a third open-overlay sticky overlap: with Delivery Pay Later selected, opening the shadcn Sign in dialog left `.checkout-sticky-action` visible under the full-screen dialog overlay. Added a CSS guard that hides `.checkout-sticky-action` while `[data-slot="dialog-content"]` is mounted, with red/green `global.test.ts` coverage.
- Browser proof after the dialog fix shows Sign in open at 390px, selected Pay Later preserved, checkout sticky action present but `display: none`, zero sticky rect, `stickyOverlapsDialog: false`, `stickyOverlapsOverlay: false`, and no horizontal overflow. Evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260704/checkout-delivery-paylater-signin-dialog-fixed-390.png`.
- Fixed a date-dependent test assumption exposed after midnight local time: pickup-date tests now compute the local `YYYY-MM-DD` value instead of UTC ISO slicing, matching the checkout calendar default. Verification passed the focused Cart/Checkout/App/PayLater/style suite including checkout interactions (`155` tests), `npm run typecheck`, `npm run lint`, `npm run format:check`, and `git diff --check`. The final in-app browser console warning/error sweep for `/checkout` returned no entries, and ports 5173/3000 were clear after shutdown.

## 2026-07-03

- Continued Cart + Checkout A+ selected-payment QA in the in-app browser at the 390px mobile breakpoint. Selected PayPal and Pay Later each reached the Delivery payment step with one selected mobile sticky provider action, no horizontal overflow, and Pay Later amount messaging at `$25.64` without duplicate fallback.
- Found and fixed a real mobile overlap: opening the header mobile menu while the checkout sticky payment action was present allowed the sticky bar to cover the bottom of the menu. Added a CSS guard that hides `.checkout-sticky-action` while `.site-header__mobile-menu:not([hidden])` is present, with `global.test.ts` coverage.
- Browser proof after the fix shows `#mobile-menu` open, checkout sticky action present but `display: none`, zero sticky rect, `stickyOverlapsMenu: false`, and no horizontal overflow. Evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260703/checkout-mobile-paylater-mobile-menu-open-390-fixed.jpg`.
- Verification after the fix passed the focused Cart/Checkout/App/PayLater/style suite (`73` tests), `npm run typecheck`, `npm run lint`, `npm run format:check`, and `git diff --check`.
- Continued the 390px Delivery selected-provider matrix with a two-item cart. Browser proof now covers selected PayPal sticky action, selected Pay Later sticky action/message at `$75.63`, and selected Card inline fields with no sticky action; screenshots are `checkout-delivery-paypal-selected-390.jpg`, `checkout-delivery-paylater-selected-390.jpg`, and `checkout-delivery-card-selected-390.jpg` under `/private/tmp/paypal-retail-cart-checkout-a-plus-20260703/`.
- Found and fixed a second open-overlay overlap: with Pay Later selected, the minicart shadcn Sheet rendered above the checkout sticky action but the sticky action still existed behind the dialog and overlapped its rect. Added a CSS guard that hides `.checkout-sticky-action` while `[data-slot="sheet-content"]` is mounted, with red/green `global.test.ts` coverage. Browser proof after the fix shows the minicart Sheet open, selected Pay Later preserved, checkout sticky action present but `display: none`, zero sticky rect, `stickyOverlapsDialog: false`, and no horizontal overflow. Evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260703/checkout-delivery-paylater-minicart-open-active-390-fixed.jpg`.
- Verification after the minicart Sheet fix passed the focused Cart/Checkout/App/PayLater/style suite (`73` tests), `npm run typecheck`, `npm run lint`, `npm run format:check`, and `git diff --check`. Browser console triage for the pass showed two local Pay Later message timeout warnings from `PayLaterStandaloneAction.tsx`; the selected Pay Later state still rendered the buyer-safe `$75.63` message, so this remains a sandbox presentment-warning note rather than an A+ hard blocker.
- Continued Cart + Checkout A+ Pickup/payment QA at 390px through the real minicart-bound checkout entry. Browser proof reached `Pickup - Payment method - 5 of 5` after ZIP `10012`, full-inventory `Brooklyn Williamsburg`, billing, and selected pickup date `July 3`; selecting Pay Later produced one selected sticky provider host, `$89.37` total, official Pay Later button/message text, and no horizontal overflow. Evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260703/checkout-pickup-paylater-selected-390.png`.
- Verified focused-input sticky safety from the selected Pickup Pay Later state by reopening billing and focusing `Billing street address`. The checkout sticky action was absent during upstream edit, the active input rect did not overlap any sticky rect, and horizontal overflow stayed `0`. Evidence: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260703/checkout-pickup-focused-billing-after-paylater-390.png`. Console triage for this pass showed early direct-checkout cart-restore errors before minicart-bound checkout entry and PDP Pay Later timeout warnings; the actual minicart-bound Pickup payment flow completed with Pay Later ready messaging.

## 2026-07-02

- Created the Category + PDP Refinement V6 implementation handoff after the approved visual companion direction. The plan lives at `docs/superpowers/plans/2026-07-02-category-pdp-refinement-v6.md` and translates Category `A+B` plus PDP `A with simpler B wording` into file ownership, tasks, acceptance criteria, and inspection standards.
- Spawned read-only sub-agent `019f22a4-6297-7222-8166-46ff59ffdda9` using `ui-ux-pro-max` to review the selected mockup and plan. The review confirmed the V6 direction but flagged implementation risks: current code still reflects V5, the mobile filter FAB needs real shadcn/Lucide/accessibility mechanics, the mockup must not undo existing PDP tab accessibility fixes, and tests must be rewritten where they still assert old V5 Pay Later/filter/support placement.
- Updated the active router and tracking contract for V6: `PLAN.md` now points the next page-polish slice at the V6 plan; `DESIGN.md` includes the V6 inspection details; `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` now track Category first-viewport proof, Pay Later strip, floating mobile filter entry, PDP support-tile relocation, real-data-only review/rating copy, footer payment marks, and the final six-width GUI matrix.

## 2026-06-30

- Completed the PDP V5 released detail-density slice. The released PDP now replaces the old 2x2 support-card grid with a compact `product-support-band`, preserves the purchase order of price -> official Pay Later -> purchase options -> Add to cart -> secured PayPal frame -> support band, and adds real-data-only rating/review summaries near the title plus inside the Customer reviews detail panel. Reviews stay absent when released review data is empty and remain hidden on unreleased PDPs.
- Fixed the PDP review-gate finding from the independent review: social proof alone can no longer create or reopen the `Customer reviews` tab, and supplemental collector proof is separately instrumented with `data-social-proof-card` only when real `reviews[]` records exist.
- Fixed a responsive tab-rail regression found during GUI verification: the shadcn/Radix tab list still inherited centered justification, causing the first tab to start offscreen after tablet/mobile resizing. The PDP detail nav now uses a constrained nav shell with edge affordance, hidden vertical overflow, 44px triggers, and `justify-content: flex-start`; browser metrics at 768/390/320 show first tab visible, `scrollLeft: 0`, no page-level horizontal overflow, and the 320px Customer reviews click activates the real review summary.
- Completed the PDP V5 mobile sticky Add to cart slice. Released mobile PDPs now observe the main purchase action and render a bottom safe-area sticky shadcn `Card` only when the main CTA is out of view; it mirrors selected option, quantity, and price, reuses the same add-to-cart handler, hides when the main CTA returns to view, and never includes PayPal, Pay Later, card, wallet, or express-payment surfaces. Unreleased PDPs never initialize the sticky purchase action.
- Verification passed `npm test -- web/src/features/catalog/ProductDetailPage.test.tsx web/src/styles/global.test.ts`, `npm run typecheck`, `npm test -- web/src/features/catalog/ProductDetailPage.test.tsx web/src/styles/global.test.ts web/src/app/App.test.tsx`, `npm run lint`, `npm run format:check`, and `npm run build:web`. Local browser evidence for `/products/blind-boxes-2?qa=pdp-v5-task6-tab-start` is in `/private/tmp/paypal-retail-pdp-v5-task6-20260630/` with screenshots/metrics at 1440/1280/768/390/320. Playwright MCP reported local Vite console errors during PDP visits, but direct Node Playwright console capture was blocked by missing local browser binaries/system Chrome headless abort; exact hosted console capture remains open for the V5 final matrix.
- Sticky verification so far passed `npm test -- web/src/features/catalog/ProductDetailPage.test.tsx web/src/styles/global.test.ts` and local browser proof at `/private/tmp/paypal-retail-pdp-v5-sticky-20260630/` for released 390/320, released Whole Box sticky click, and unreleased 390/320. Full typecheck/lint/build and the PDP independent review gate still need to run before committing/final close.

## 2026-06-26

- Fixed the hosted storefront Pay Later blank-message regression found on Home, Category, PDP, Cart, and Minicart. Browser evidence showed the PayPal provider scopes were `ready` and `paypal-message` hosts had visible boxes, but the hosts contained no text, no shadow content, no visible iframes, and no fallback because storefront placements still used passive `auto-bootstrap`. All Pay Later amount/message placements now use the managed SDK v6 content fetch/application path with buyer-safe fallback copy, and the regression test now fails if storefront placements do not call the official content fetch/apply path.
- Continued hosted Render checkout wallet/card QA after the latest deploy. Live HTTPS delivery checkout reached Payment, but shipping-address submit still waited about `6.1s`, billing submit about `4.0s`, and shipping-option submit about `4.0s`, confirming repeated backend draft/totals reconciliation remains on the perceived-latency path. Card Fields rendered successfully on HTTPS with three mounted PayPal hosted fields, stable `50px` containers, ready provider status, enabled `Pay by card`, and no merchant-app console warnings/errors. Code review found the real Card Fields completion gap: successful hosted-card submit only updated local status and never called the checkout approval/capture bridge. `CardFieldsCheckoutAction` now stores the create-order response, hands successful card approvals to App with PayPal order/session IDs, guards duplicate approvals by order ID, and shows visible failure notices for create/submit/approval failures. Focused verification passed `npm run test -- CardFieldsCheckoutAction.test.tsx`, `npm run test -- paymentActionFailure.test.tsx App.checkout-paypal-capture.test.tsx`, and `npm run typecheck`. Live sandbox card submit/capture remains open pending explicit action-time approval to submit public PayPal sandbox card data.

## 2026-06-25

- Fixed the follow-up checkout/minicart regressions found in GUI review. Pickup store picker no longer leaves the inline store list visible behind the modal; mini-cart `View cart` renders as a visible secondary button even from the portaled shadcn Sheet; the checkout Apple Pay row uses the official black PayPal SDK logo asset instead of the invisible white mark. The storefront Pay Later auto-bootstrap conclusion from this pass was superseded by the 2026-06-26 hosted blank-message finding above, which moved storefront messages to the managed fetch/apply path. Focused TDD coverage was added for the original regressions, and browser verification confirmed the modal background, mini-cart button contrast, Apple Pay image loading, storefront Pay Later host mounting at the time, and checkout fallback behavior.
- Completed the hosted HTTPS Pay Later Pay in 4 checkout sandbox QA pass on `https://retail-demo.onrender.com`. Playwright QA built an eligible `$154.39` delivery checkout, selected Pay Later, completed the official PayPal Pay in 4 application/approval flow, clicked the final Pay in 4 confirmation, returned to `/checkout/express-review`, captured successfully, rendered order `DO-20260625-000004` with payment method `Pay Later`, capture ID `8AG36137NP271803X`, `Payment captured`, and cart count `0`. The low-amount `$25.64` pass correctly showed Pay in 4 eligibility limits, and console/network noise during the final pass was PayPal sandbox-side telemetry/presentment noise rather than merchant-app errors. Follow-up: the selected-payment primary action should route/focus buyers to the official PayPal/Pay Later button instead of leaving an ambiguous separate `Continue` action.
- Completed the hosted HTTPS PayPal checkout sandbox QA pass on `https://retail-demo.onrender.com`. Browser QA exercised Delivery checkout through shipping address, billing address, shipping option, active Payment, official PayPal sandbox login/approval, return to `/checkout/express-review`, capture, order confirmation, and cart cleanup. Final evidence showed order `DO-20260625-000001`, PayPal capture ID `70T37391TR665405C`, `Payment captured`, cart count `0`, no framework overlay, and no merchant-app console warnings/errors; observed console noise was limited to PayPal sandbox-side telemetry/debug messages.
- Verified the Render Supabase credential fix on `https://retail-demo.onrender.com`. Read-only HTTPS probes now return 200 for `/api/health`, `/api/config?market=US&profile=popmart`, `/api/catalog/products?market=US&profile=popmart` with 25 products, and `/api/cart?market=US`. Browser QA also confirmed the hosted Home, Category, PDP, PDP Add to cart, Minicart, Cart, and Checkout entry are data-backed, do not show fallback status, and have no relevant console errors. Hosted PayPal checkout sandbox QA is unblocked as the next active pass.
- Added Render-hosted PayPal/backend observability for the HTTPS blocking QA loop. Server startup now logs sanitized deployment context, PayPal SDK config/client-token/create-order/express-review/capture routes emit structured stage events through the redacting runtime debug logger, central API failures include sanitized error messages for hosted Supabase diagnosis, PayPal capture gateway errors include safe provider debug metadata, and regression coverage asserts cart secrets, bearer tokens, client tokens, and PayPal credentials are not emitted. Verification passed `npm run verify` (66 files, 497 tests, typecheck, lint, format check) and `npm run build`.
- Added Render-ready production build/start support after the first Render Web Service deploy failed on missing `npm run build`. The demo now exposes `npm run build` for Vite + Express compilation, `npm start` for the compiled server, explicit `0.0.0.0:$PORT` binding for Render, and Express static serving for built SPA routes while preserving `/api/*` JSON responses. Updated README/ENVIRONMENT deployment guidance and Deployment QA tracking. Verification passed focused `npm test -- server/tests/app.test.ts`, full `npm run verify` (66 files, 496 tests, typecheck, lint, and format check), and `npm run build`.
- Closed stale SDK-provider tracking rows after fresh verification. Existing `PayPalSdkProviderScope` tests prove `buildPayPalProviderOptions()` maps backend `sandbox_test_buyer_country` to SDK v6 `testBuyerCountry` for sandbox config and omits it for production config. Focused verification passed `npm test -- web/src/features/payments/PayPalSdkProviderScope.test.tsx` (8 tests).
- Closed the M16 generic MochiToy public-safe asset gate with TDD. Generic product rows now seed one conservative existing `/assets/generic/products/placeholder.svg` image per product instead of three missing `/generic/products/*.webp` gallery placeholders, generic homepage/category image references also use existing `/assets/generic/` media, and generic order item snapshots no longer point at missing webp paths. The SVG placeholder was retitled/recolored for MochiToy's generic palette. Red tests first failed on 75 generic image rows, missing generic product snapshots, and non-`/assets/generic/` homepage/category paths; focused verification then passed `npm test -- supabase/seed/seed-data.test.ts -t "generic"`.
- Closed the remaining M15 Admin Portal runtime debug logs slice. Added a bounded in-memory sanitized runtime log store, wired Express completion/error logging through shared debug IDs, exposed signed `GET /api/admin/debug-logs`, and added an Admin runtime debug card that loads with `x-admin-session`, shows level/debug ID/source/path, and flattens redacted context without exposing raw access tokens or client secrets. Focused verification passed `npm test -- server/tests/adminRoutes.test.ts -t "admin runtime debug log routes"`, `npm test -- web/src/app/App.interactions.test.tsx -t "loads admin runtime debug logs"`, and `npm run typecheck -- --pretty false`; final verification passed `npm run verify` (66 files, 492 tests, typecheck, lint, and format check).
- Continued M15 Admin Portal with the payment/order debug viewer. Added signed `GET /api/admin/payment-debug`, a Supabase admin payment-debug repository that joins recent payment sessions with order summaries, total snapshots, sanitized PayPal snapshots, and linked webhook events, and an Admin shell Card that loads the viewer with `x-admin-session` in a compact responsive layout. Initial TDD checks failed on the missing route/UI as expected. Final verification passed `npm test -- server/tests/adminRoutes.test.ts web/src/app/App.interactions.test.tsx web/src/styles/global.test.ts` (97 tests), `npm run typecheck -- --pretty false`, `npm run lint`, touched-file Prettier check, and targeted `git diff --check`.
- Continued M15 Admin Portal with the standalone webhook viewer. Added signed `GET /api/admin/webhooks`, a Supabase admin webhook repository that lists recent sanitized webhook events without mutation, and an Admin shell Card that renders event ID/type, verification status, processing status, linked order/payment-session IDs, and received/processed timestamps with `x-admin-session`. Initial TDD red checks failed on the missing route/UI as expected. Final verification passed `npm test -- server/tests/adminRoutes.test.ts web/src/app/App.interactions.test.tsx web/src/styles/global.test.ts` (94 tests), `npm run typecheck -- --pretty false`, `npm run lint`, touched-file Prettier check, and targeted `git diff --check`.

## 2026-06-24

- Continued M15 Admin Portal with signed inventory and pickup-date controls. Added `GET/PATCH /api/admin/inventory` for central/store stock rows using opaque API IDs, `GET/PATCH /api/admin/pickup-dates` for capacity/availability rows, and Admin shell controls that load and save with `x-admin-session`. Verification passed `npm test -- server/tests/adminRoutes.test.ts web/src/app/App.interactions.test.tsx web/src/styles/global.test.ts` (91 tests), `npm run typecheck -- --pretty false`, `npm run lint`, touched-file Prettier check, and targeted `git diff --check`. The downstream checkout/pending-resume inventory revalidation row remains open for a separate end-to-end pass.
- Refined checkout tab accessibility/readability on `/checkout`: improved non-selected tab contrast against the dark tab-strip, nudged disabled tab contrast to reduce legibility issues on blue backgrounds, and preserved the fixed 54px row height model to avoid vertical content clipping. Re-ran focused CSS/interaction tests and Playwright checks at `375px` and `1440px`; no tab-row scrolling/overflow, active/inactive contrast, and touch targets remained stable.
- Re-verified `/checkout` fulfillment tabs on desktop, 320px, and 375px mobile after the latest UI feedback. Runtime checks show active/inactive tab styles remain correct (`height: 44px`, `min-height: 44px`, brighter inactive tab text), no tab-row vertical overflow occurs at load (`checkout-tabs` uses `box-sizing: border-box` with `54px` explicit bounds), and active panel visibility semantics remain correct.
- Executed `npm run test -- web/src/styles/global.test.ts web/src/features/checkout/CheckoutPage.test.tsx web/src/features/checkout/CheckoutPage.interactions.test.tsx`; all tests passed (`48` tests). Ran Playwright live checks from `/checkout` at `320`, `375`, and `1440` widths and confirmed no tab-row jitter/overflow.
- Kept the current tab styling in place and prepared to move to the next open UX/asset slice unless you want a further visual re-theme (for example active/inactive contrast tuning).
- Re-ran `/checkout` with Playwright at `320`, `375`, and `1440` after your latest tab request: active/inactive contrast remains stable (`#082747` active, white inactive), strip metrics stay `52px` client / `54px` layout height and `44px` buttons, and no tab-row scrollbars appeared. Updated lint/typecheck also run clean.

## 2026-05-26

- Brainstormed and approved the flagship PayPal retail demo direction.
- Selected demo folder name: `paypal-retail-demo`.
- Confirmed default POP MART profile and reusable MochiToy Studio profile.
- Confirmed Vite React frontend, Node.js Express backend, Supabase Auth/database, local assets, PayPal JS SDK v6/npm v9.x.
- Created initial protocol documentation and tracking structure.
- Verified the initial files exist, contain no placeholder markers, and pass `scripts/check-agent-system.sh`.
- Corrected BOPIS planning docs: v1 uses capture-at-checkout with specific Create Order pickup parameters, not the authorize-at-pickup guide.
- Added `DATA_MODEL.md` with Supabase schema, profile scoping, snapshots, inventory, promo/tax/shipping, and seed plan.
- Added `API_CONTRACT.md` with Express route draft, PayPal payload contracts, cart/checkout/payment rules, and Admin API scope.
- Updated planning todos and test cases for the data/API contract review gate.
- Added `IMPLEMENTATION_TASKS.md` with planned file structure, milestone sequencing, TDD gates, and QA milestones.
- Refined PayPal API contract for SDK config/client token split, DO/PO order numbers, duplicate-safe PayPal invoice IDs, idempotency keys, and detailed line-item payloads.
- Refined market-switch planning: markets now carry currency, locale, buyer country, Pay Later buyer country, sandbox test buyer country, component set, and provider key behavior; carts/prices/pending orders are market-scoped.
- Confirmed from local `wiki-v2` PayPal JS SDK v6 type snapshot that sandbox buyer simulation maps to `createInstance({ testBuyerCountry })`; updated docs and tests to use backend `sandbox_test_buyer_country` as the stored/API field.
- Filled schema gaps for guest order lookup, promo region/product scoping, and total snapshots; moved SDK `testBuyerCountry` frontend mapping from PayPal payload-builder milestone to payment UI milestone.
- Added `ENVIRONMENT.md` with local tool probe, Supabase/PayPal environment strategy, secret boundaries, wallet testing approach, and initial env variable shape.
- Added `PAYPAL_EVIDENCE.md` to map each planned PayPal capability to local `wiki-v2` source pages and distinguish PayPal-confirmed behavior from demo-specific BOPIS receiver-name convention.
- Refined data model after database review: shared market/store/tax/shipping reference data, profile-scoped inventory/promos, checkout-capable total snapshots, promo explanation rows, and explicit constraint/index expectations.
- Locked the implementation language to TypeScript across web, server, shared modules, tests, and seed tooling.
- Folded UI/UX review findings into the design and execution docs: POP MART visual contract, checkout states, mobile sticky payment rules, accessibility requirements, Admin information architecture, and visual QA gates.
- Added `PLAN.md` as the active Superpowers-style execution router, with `IMPLEMENTATION_TASKS.md` as the canonical detailed implementation plan.
- Closed Milestone 0 decision gates for Supabase, PayPal env handling, strict TypeScript scaffold, Apple Pay/Google Pay local testing, and POP MART asset naming.
- Completed Milestone 1 scaffold: demo-level npm workspace, strict TypeScript project references, Vite React app shell, Express server shell, shared module/test smoke suite, Supabase seed folder, lint/test/typecheck scripts, `.env.example`, and README runbook stub.
- Installed dependencies and generated `package-lock.json`. Local install resolved `@paypal/react-paypal-js@9.2.0` with transitive `@paypal/paypal-js@9.7.0`.
- Verified installed PayPal SDK v6 types include `CreateInstanceOptions.testBuyerCountry?: string`, matching the sandbox buyer-country planning requirement.
- Verified Milestone 1 commands: `npm run typecheck`, `npm test`, `npm run lint`, and `npm run format:check`.
- Added local Supabase CLI tooling as a demo dev dependency and initialized `supabase/config.toml`.
- Created the first Supabase migration through `npx supabase migration new init_app_schema`.
- Drafted the private `app` schema migration with 38 app tables, RLS enabled on every app table, service-role-only grants, profile/market scoping constraints, promo explanation rows, total snapshots, PayPal invoice/webhook uniqueness, and comments for tax/shipping/inventory demo assumptions.
- Local migration apply is not yet verified because Docker Desktop/daemon is unavailable in this environment.
- Linked a remote Supabase project and applied the initial schema migration with `npx supabase db push --linked`.
- Added and applied a follow-up hardening migration for function search paths and public execution grants after Supabase advisors flagged security warnings.
- Verified the linked remote database reports 38 `app` tables and `npx supabase db advisors --linked` returns no issues.
- Added deterministic TypeScript seed tooling for storefront/reference data with dry-run summary, SQL generation, linked apply, and local apply modes.
- Added first seed dataset for 2 profiles, 2 markets, 5 categories/profile, 25 products/profile, active market prices, 9 stores/market, store pickup dates, central/store inventory, tax rates, shipping options, homepage sections, release events, and promo rules/compatibility data.
- Verified seed tooling with `npm run verify` and applied the storefront/reference seed slice to the linked remote Supabase project.
- Verified linked remote seed counts match the generated summary for profiles, markets, categories, products, product prices, product images, stores, pickup dates, inventory, tax, shipping, and promo tables.
- Added guarded buyer/account/order seed data: 5 demo auth users, auth identities, user profiles, default addresses, saved payment placeholders, active carts, pending delivery/pickup orders, completed delivery/pickup orders, guest lookup data, payment sessions, total snapshots, PayPal snapshots, lifecycle events, webhooks, and reviews.
- Verified the guarded seed slice with tests, applied it to the linked remote Supabase project, and confirmed linked remote counts match the generated summary for auth, account, cart, order, payment, webhook, and review tables.
- Started Milestone 3 deterministic domain logic with TDD for shared money helpers, US/GB market config/provider keys, DO/PO order-number generation, and PayPal invoice attempt suffixes.
- Added shared catalog helpers with TDD for market-scoped active product price lookup, sale/current price normalization, product purchase state, and outlined release calendar entries.
- Added shared promo, tax, and shipping helpers with TDD for best compatible promo-set recommendation, region/product promo scopes, tax after promo excluding shipping, and cheapest eligible shipping defaults.
- Added shared BOPIS inventory helpers with TDD for Haversine store distance ranking, full-inventory fallback store selection, and partial pickup ready/unavailable item splits.
- Added shared cart lifecycle helpers with TDD for guest-to-user cart merge, stale-cart refresh triggers, canonical quantity/price refresh, checkout blockers, and browser cart binding shape.
- Added shared market switch planner with TDD for Admin profile/market switches, cart binding reset, fetch-or-create cart actions, config/catalog/cart/PayPal SDK refresh targets, preserved business resources, and no cart currency conversion.
- Added shared pending order resume planner with TDD for locked order market/config snapshots, order item price snapshots, resume revalidation actions, expired/invalid payment-session replacement, stable buyer-facing order numbers, fresh PayPal invoice IDs, and pickup-date rebooking.
- Added shared order status transition helpers with TDD for payment capture, delivery manual lifecycle, pickup manual lifecycle, timeline event plans, cross-fulfillment blocking, and terminal-state blocking.
- Synchronized tracking after Milestone 3 completion and queued Milestone 4 PayPal payload/config helpers as the next active implementation stage.
- Added the first Milestone 4 PayPal helper with TDD: full-checkout Delivery Create Order payload builder using `CAPTURE`, merchant-provided shipping address semantics, detailed physical-goods line items, item/shipping/tax/discount breakdown, and minor-unit amount reconciliation.
- Added the express Delivery PayPal Create Order payload builder with TDD: `GET_FROM_FILE`, server-side shipping callback config, default `SHIPPING_ADDRESS` subscription, optional `SHIPPING_OPTIONS`, detailed physical-goods line items, and no locked merchant shipping address.
- Added the BOPIS PayPal Create Order payload builder with TDD: `CAPTURE`, `SET_PROVIDED_ADDRESS`, selected pickup store address, `shipping.type: PICKUP_IN_STORE`, receiver name `s2s ${storeName}`, detailed physical-goods line items, and BOPIS amount breakdown without shipping fee.
- Added the PayPal SDK config response builder with TDD: browser-safe client ID, sandbox/production SDK URL, market currency/locale/buyer-country fields, sandbox-only test buyer country, normalized components, deterministic provider key, and vaulting `needs_client_token`.
- Added the PayPal client-token request planner with TDD: standard flows skip token generation, guest vaulting rejects with `GUEST_VAULTING_NOT_ALLOWED`, logged-in card/PayPal vaulting can generate a normalized OAuth client-token request, and unsupported methods/domains are rejected before server-side PayPal calls.
- Added the PayPal request metadata planner with TDD: first attempts generate fresh request IDs and base invoice IDs, same-payload retries reuse the prior `PayPal-Request-Id`, changed payloads generate fresh request IDs, and fresh attempts use suffixed invoice IDs.
- Added PayPal line-item tax allocation and amount consistency guard prep with TDD: aggregate order-line tax can become per-unit PayPal `items[].tax`, uneven cents split a same-product PayPal line into grouped quantities, mismatched line tax is blocked before PayPal calls, and payload amount consistency returns structured mismatch reasons.
- Added the PayPal payment method mapper with TDD: maps PayPal, Pay Later, card, Apple Pay, Google Pay, and Venmo to SDK components/session methods/UI surfaces, hides runtime-ineligible rows, keeps card inside the card box, enforces Venmo US/USD scope, and returns hidden-method reasons for debug/Admin use.
- Added the PayPal vault attribute planner with TDD: emits PayPal/card save-for-future attributes only for authenticated opt-in buyers, includes card customer/3DS attributes when a PayPal customer ID exists, omits attributes when not requested, and rejects guest or unsupported-method vault requests before PayPal calls.
- Added the PayPal capture amount guard with TDD: extracts provider amount snapshots from PayPal purchase units, compares them with locked merchant snapshots, blocks capture on amount/currency mismatches, and allows only configured rounding tolerance.
- Added the sanitized PayPal snapshot storage helper with TDD: builds the `paypal_order_snapshots` row shape, preserves explainable item-level request/response details, stores merchant amount snapshots, and redacts tokens, client secrets, auth headers, payer email, and phone data.
- Completed Milestone 5 Express API foundation with TDD: `/api/health` success shape, standard API 404 shape, env validation, server Supabase client factory, buyer auth middleware, guest cart middleware, signed admin session guard, reusable API response helpers, and sanitized debug logger.
- Started Milestone 6 storefront APIs with TDD route contracts: `/api/config`, homepage, categories, products, PDP, and release-events routes resolve profile/market context, preserve filter inputs, return standard response envelopes, and handle missing PDP products with buyer-safe 404s.

## 2026-05-31

- Added the Supabase-backed catalog repository with TDD: profile/market config mapping, homepage sections, categories, product filters, unreleased PDP checkout/review blocking, and PDP-linked release calendar events.
- Wired the Express server startup to create a service-role Supabase client, attach the app-schema catalog data source, and serve storefront catalog APIs from real Supabase rows.
- Adjusted the server TypeScript project boundary so server code can consume shared catalog helpers while preserving strict typecheck.
- Added the Admin profile/market switch API with TDD: signed admin-session protection, profile/market ID lookup, same-shape config response, active no-query storefront config refresh, and buyer-safe missing-context errors.
- Added the cart API route surface with TDD: active cart read/create contract, add/update/delete item inputs, merge after login/register, refresh triggers, buyer auth context, guest cart binding headers, and buyer-safe validation errors. Supabase-backed cart persistence remains queued as the next slice.

## 2026-06-01

- Added the Supabase-backed cart repository with TDD: guest cart creation with hashed client-secret storage, existing guest cart verification, current-price add-to-cart with quantity caps, authenticated cart merge, stale-price refresh, and unreleased checkout blockers.
- Wired live server startup to attach buyer auth, guest cart middleware, and the Supabase cart repository so `/api/cart` uses real app-schema persistence.
- Kept merge behavior aligned with login/register cart sync by refreshing merged lines against canonical product rules before returning the authenticated cart.
- Added the checkout draft API route surface with TDD: draft create/refresh, fulfillment switching, delivery shipping/billing/shipping-option updates, pickup location/store/date updates, buyer/auth/cart context propagation, and buyer-safe validation errors. Supabase-backed checkout persistence remains queued as the next slice.
- Added the Supabase-backed checkout draft repository with TDD: verified guest-cart draft creation, draft ownership checks, delivery address persistence, cheapest eligible shipping default, tax calculation excluding shipping, pickup store/date persistence, and BOPIS ready/unavailable item split.
- Wired live server startup to attach buyer auth, guest cart middleware, and the Supabase checkout repository so `/api/checkout/drafts` uses real app-schema persistence.
- Added checkout promo evaluate/apply/remove APIs with TDD: route-level code normalization, Supabase promo rule/scope/compatibility loading, persisted promo evaluation snapshots and explanation lines, selected promo draft linking, discount-driven tax recalculation, and remove-code recalculation.
- Added guest order lookup API with TDD: `GET /api/guest-orders/:orderNumber?email=...` normalizes order number/email, returns buyer-safe not-found errors, hashes guest email for lookup, updates lookup attempt metadata, and returns read-only order details without internal IDs.
- Started Milestone 7 PayPal APIs with TDD: added the PayPal OAuth/client-token gateway, browser-safe SDK config API, authenticated-only client-token API, server wiring, and default client-token domain selection from `PUBLIC_HTTPS_ORIGIN` or `APP_BASE_URL`.
- Added PayPal create-order route/gateway boundaries with TDD: `/api/paypal/orders/delivery`, `/express-delivery`, and `/bopis` validate source input, resolve buyer/storefront context, build shared PayPal payloads, call `/v2/checkout/orders` with `PayPal-Request-Id`, record merchant/provider snapshots through a repository boundary, and preserve Delivery/Express/BOPIS payload semantics. Supabase-backed order preparation remains queued.
- Added the Supabase-backed PayPal order preparation repository with TDD: Delivery drafts create pending `DO-*` orders from finalized shipping address/shipping option/promo/tax snapshots, BOPIS drafts create pending `PO-*` orders with pickup-store address and partial-inventory payable quantities only, and express delivery creates pending Delivery orders from verified guest/auth carts with a server-side shipping callback URL.
- Wired live server startup to attach the PayPal order repository and guest cart middleware to PayPal routes, so create-order APIs can prepare payment sessions from real app-schema cart/checkout data before calling PayPal.
- Added PayPal create-order persistence: payment sessions store invoice/request IDs, method, attempt number, merchant totals, and source fingerprints; total snapshots are written for Admin debug; PayPal create-order responses update the payment session and write sanitized `paypal_order_snapshots`.
- Added PayPal express shipping callback recalculation with TDD: raw PayPal callback responses, raw PayPal decline JSON for invalid callback input, selected/default shipping option handling, tax recalculation excluding shipping, pending order/payment-session total updates, order item tax refresh, and `paypal_shipping_update` total snapshots.
- Added auto promo re-evaluation to PayPal express shipping callbacks with TDD: callback totals now use shared promo rules/scopes/compatibility, write order-scoped `promo_evaluations` and `promo_evaluation_lines`, include PayPal discount breakdown when an auto promo applies, and store the promo evaluation ID on the shipping-update total snapshot.

## 2026-06-02

- Added the PayPal capture route/gateway/repository slice with TDD: `/api/paypal/orders/:paypalOrderId/capture` prepares capture through the repository amount guard, blocks mismatches before PayPal, calls Orders capture with `PayPal-Request-Id`, and returns a standard app envelope with capture IDs/status plus the guard decision.
- Added capture finalization persistence: successful captures mark the order paid and payment session captured, write sanitized PayPal capture snapshots, write `capture` total snapshots, add lifecycle events, decrement central or pickup-store inventory, and clear only paid order items from the active cart.
- Documented the v1 capture snapshot behavior in `API_CONTRACT.md`: PayPal capture responses are stored in full after sanitization, while the guard relies on locked merchant/provider amount snapshots when the minimal capture response does not echo the original full amount breakdown.

## 2026-06-03

- Completed the pending-order resume promo wrap-up with TDD: Delivery and BOPIS create-order preparation now re-evaluates eligible auto promos when reusing a pending checkout order, writes order-scoped promo evaluation rows/lines, recalculates tax and totals from the refreshed discount, links the `pending_resume` total snapshot, and keeps the buyer-facing order number stable while fresh PayPal attempts receive the expected suffixed invoice ID.
- Completed the Milestone 7 webhook and saved-payment slice with TDD: PayPal webhook verification now calls the PayPal verification API with `PAYPAL_WEBHOOK_ID` and notification headers, invalid webhooks are stored as ignored without mutation, valid capture webhooks reconcile payment/order status, vault-created/deleted webhooks reconcile saved-payment state, authenticated capture vaulting creates active or pending saved-payment records, and account saved-payment delete calls PayPal Payment Method Tokens delete before marking local records deleted.
- Tightened the Milestone 7 webhook repository after review: PayPal webhook events are now reserved/audited before mutation and duplicate `event_id` deliveries return the stored processing result without repeating saved-payment/order updates.
- Restored the Milestone 8 web-shell WIP after merging M7: added the typed web API client, route resolver, profile asset resolver, provider-key shell state helper, buyer/admin app shell split, auth modal shell, minicart shell, and POP MART/generic responsive theme tokens.
- Finished the Milestone 8 web app shell/config foundation with TDD: added reusable accessibility primitives, skip-to-content and live-status shell affordances, reduced-motion/focus-visible CSS guards, app API/runtime providers, and a PayPal provider scope keyed only around the future payment subtree so provider-key changes do not reset the whole buyer shell.
- Started Milestone 9 storefront/catalog UI with TDD: added the homepage component, route wiring, required merchandising sections, local POP MART placeholder SVG assets, descriptive image alt text, amount-free homepage Pay Later promo, outlined release calendar dates, color-independent release labels/legend, and PDP links from selected release products.
- Continued Milestone 9 category/catalog UI with TDD: added the category page component, `/products` route wiring, typed filter/product data, "All options" category switcher, applied filter count/reset controls, disabled pickup-location hint, amount-free category Pay Later promo, responsive filter layout, and PDP links from product cards.
- Completed the Milestone 9 PDP UI slice with TDD: added the product detail component, `/products/:slug` route wiring, 3-image gallery controls, product status/details/current-versus-regular price display, amount-aware PDP Pay Later message for purchasable products, delivery express action placeholders, released-product reviews, local gallery placeholder assets, and unreleased-product checkout/review blocking.
- Completed Milestone 10 cart/minicart UI with TDD: added the shared cart model, `/cart` route, minicart item summary, full cart item rows with quantity controls, merchandise-only cart total calculation, amount-aware Pay Later cart messaging, checkout/view-cart actions, delivery-only PayPal/Pay Later express metadata, and pickup hints as text only with no pickup button.
- Started Milestone 11 checkout UI with TDD: added the checkout page component, `/checkout` route wiring, Delivery/Pickup tab shell with preserved per-mode drafts, explicit checkout step state labels, fulfillment-mode lock notice/disabled inactive tabs, mode-aware order summary, pickup ready/unavailable item split, selected-payment slot placeholder, and mobile sticky selected-payment action shell.
- Continued Milestone 11 detailed checkout accordions with TDD: added data-driven step details for delivery shipping/billing/shipping-option/payment content, pickup ZIP/default-location/store-selection/billing/pickup-date/payment content, and pickup store cards with distance, phone, available/unavailable item counts, selected state, and partial-inventory callout before store submit.

## 2026-06-05

- Completed the Milestone 11 checkout validation UI with TDD: added a typed validation state contract, assertive validation summary, field-level `aria-invalid`/described-by errors, focus-target step markup, browser focus movement to the first invalid step, and visible error/focus styling.
- Started Milestone 12 payment UI integration with TDD: added a scoped PayPal SDK v6 provider that fetches browser-safe backend SDK config, renders loading/error and SDK status regions, preserves currency/buyer-country/Pay Later buyer-country metadata for child payment surfaces, verifies the installed v9.2.0 `testBuyerCountry` type, and maps sandbox `sandbox_test_buyer_country` into `PayPalProvider` `testBuyerCountry`.
- Continued Milestone 12 payment UI integration with TDD: added typed app API POST support, passed Checkout Order Summary payment-action context from the active Delivery/Pickup draft, moved the PayPal provider boundary into the selected checkout action, and rendered the official PayPal standalone button with Delivery/BOPIS create-order request selection.
- Continued Milestone 12 Pay Later UI integration with TDD: added active-draft Pay Later row message mounting, a scoped Pay Later provider request using `method=paylater`, amount-aware `<paypal-message>` rendering, the official SDK v6 `PayLaterOneTimePaymentButton`, and Delivery/BOPIS create-order request selection with `method: "paylater"`.
- Continued Milestone 12 Card Fields UI integration with TDD: selected card now expands inside the active payment row instead of Order Summary/mobile sticky action, mounts a scoped `card-fields` provider request, renders PayPal-hosted number/expiry/CVV fields with fixed-height containers, keeps the save checkbox and card pay button inside the card box, and builds Delivery/BOPIS create-order requests with `method: "card"` and `vault_requested`.
- Continued Milestone 12 wallet UI integration with TDD: checkout choices now hide rows marked ineligible and suppress selected wallet actions, Delivery/Pickup payment walls include Apple Pay, Google Pay, and Venmo rows when eligible, selected wallets request method-scoped PayPal SDK provider config, Apple Pay waits for PayPal eligibility config before mounting the official React SDK v6 component, Venmo mounts the official React SDK v6 component surface, Google Pay exposes a runtime-gated PayPal Google Pay session surface, and wallet create-order request builders select Delivery/BOPIS endpoints with the selected wallet method.

## 2026-06-06

- Completed the Milestone 12 layout and save-for-future gating slice with TDD: Checkout Order Summary now marks and reserves a stable selected-payment action slot, Pay Later radio-row messages reserve space while async PayPal message rendering completes, checkout context exposes save eligibility only for eligible logged-in PayPal/card selections, PayPal renders an unchecked save checkbox under its button only when eligible, card fields hide the save checkbox unless eligible, and PayPal create-order request builders include `vault_requested` only for explicit save opt-in.

## 2026-06-07

- Started Milestone 13 Express Review and Confirm with TDD: added `/checkout/express-review` routing, a typed `ExpressReviewPage` for delivery express returns, merchant/PayPal order identifiers, synchronized status and amount guard display, delivery address and shipping option sections, item rows, final total snapshot, a confirm CTA, and App routing that keeps full checkout on `/checkout` without PayPal SDK provider surfaces on the review page.
- Audited the checked UI milestones before continuing M13 and found a done-milestone gap: several M9-M12 items were closed at the visual-shell level while buyer interactions remained unwired. Queued Milestone 11.5 Buyer Flow Interaction Recovery to restore PDP/cart/minicart actions, checkout accordion progression, editable checkout forms, and payment-method-driven surfaces before deeper express review/capture work resumes.
- Fed the milestone-gap lesson back into the agent system: demo-level rules, the PayPal demo's local rules, new-demo protocol, standard/complex/simple templates, and the reusable `learnings/demo-ops` pool now distinguish rendered UI shell progress from verified user behavior.
- Started Milestone 11.5 with TDD: added jsdom/@testing-library interaction tests that first failed because PDP add-to-cart and cart quantity changes did not open/update the minicart, then wired App-owned cart/minicart state so PDP add-to-cart increments shared cart quantity, opens minicart, updates Pay Later amount messaging, and full-cart quantity changes synchronize into minicart.
- Continued Milestone 11.5 checkout interaction recovery with TDD: added failing Delivery shipping-address interaction tests for editable fields, submit, saved/collapsed shrink state, edit/re-expand, and billing editing handoff; then added local checkout field/state/collapsed-step overrides for that shipping-address path only.
- Continued Milestone 11.5 checkout interaction recovery with TDD: added failing Delivery billing/shipping-option and payment-radio tests, then wired billing same-as-shipping expansion, saved summary collapse, shipping-option selection collapse, payment editing handoff, and PayPal/Pay Later/card surface switching from the selected radio method.
- Re-aligned the M11.5 checkout UX plan after review: the remaining work now explicitly requires a single-expanded-section accordion, guest Pickup ZIP/postcode to ranked store-list modal, logged-in preselected nearest/default-address store with Change store modal, accessible modal focus behavior, and partial-inventory Order Summary updates before Pickup can be marked interactive.
- Re-aligned the visual direction after a UI/UX review: the POP MART profile was too restrained for a blind-box collectible storefront, so Milestone 16 now includes a playful premium collectible visual refresh with multi-accent tokens, rounder retail typography, tactile product/category cards, sticker-like badges, drop-calendar styling, collectible-event promo cards, compact store-ticket pickup cards, and guarded PayPal surface readability.

## 2026-06-09

- Continued Milestone 11.5 checkout recovery with TDD: added the single-expanded-section accordion contract, then wired Delivery/Pickup panels so only the active step renders editable details, submitted Delivery steps collapse to summaries, edit reopens only the edited step, and payment rows/card boxes render only when the payment step is active.
- Continued Milestone 11.5 Pickup guest recovery with TDD: added a failing ZIP/postcode-to-store-modal interaction test, then wired the Pickup location submit action, accessible ranked store-list dialog, selected store radio/confirm behavior, selected-store collapsed summary, Change store affordance, and billing-section handoff.
- Continued Milestone 11.5 logged-in Pickup recovery with TDD: added a preselected-store fixture for logged-in/default-address Pickup, hid the guest ZIP input in that mode, rendered the selected store summary with Change store, focused the selected store radio when the modal opens, and returned focus to the Change store trigger on Escape/close.
- Continued Milestone 11.5 partial Pickup inventory recovery with TDD: added a store-change Order Summary test, then derived the active Pickup summary context, ready item line, unavailable item line, and partial-inventory callout from the selected pickup store so full and partial store choices update the buyer-facing payable split.
- Continued Milestone 11.5 Pickup progression recovery with TDD: added a failing store-to-payment interaction test, then wired Pickup billing-address submit to pickup-date editing and pickup-date submit to payment-method editing while preserving collapsed summaries and PayPal/Pay Later radio-driven payment surfaces.
- Continued Milestone 11.5 logged-in Pickup happy path recovery with TDD: added a failing direct-continue test for the preselected nearest/default-address store, then marked preselected pickup-location/store-selection steps saved and added a "Continue with this store" summary action that advances into billing, pickup date, and payment selection without opening the Change store modal.
- Closed the stale Milestone 11.5 wallet-switching gap with buyer-flow evidence: added an App interaction test proving Delivery checkout can advance to payment and selecting Apple Pay, Google Pay, or Venmo updates the Order Summary payment action to the matching method while existing static coverage continues to hide ineligible wallet rows.
- Continued Milestone 11.5 checkout submit-state recovery with TDD: added a failing timer-driven interaction test for section submit transitions, then wired save-capable Delivery/Pickup steps through saving, recalculating totals, saved/collapsed, and next-section editing states while updating existing buyer-flow helpers to wait for settled transitions.

## 2026-06-12

- Continued Milestone 11.5 delivery express recovery with TDD: added a failing App interaction test for PDP/cart/minicart PayPal or Pay Later express entry, then wired the visible delivery-only express actions into App-owned route/status state so those entry points move the buyer to Review and Confirm with the selected source and payment method. The actual PayPal-approved return with synchronized shipping/tax/promo totals and capture stays open for Milestone 13.
- Continued Milestone 11.5 cart/minicart action recovery with TDD: added failing App interaction tests for Close minicart, View cart, minicart Checkout, and full-cart Go to checkout behavior, then wired App-owned route/status state so those actions close the minicart and render Cart or Delivery/Pickup checkout without document-navigation fallbacks. Server-backed cart reconcile before checkout/express remains open.

## 2026-06-13

- Continued Milestone 11.5 cart API recovery with TDD: added failing App and API-client tests for server-backed cart quantity sync, then added web API PATCH support, stable cart item IDs, optimistic quantity updates that call `PATCH /api/cart/items/:id`, and `POST /api/cart/refresh` triggers before checkout and delivery express entry. Mapping backend cart update/refresh response DTOs back into buyer `CartData` remains open as the next reconciliation slice.
- Closed the M11.5 cart response reconciliation gap with TDD: added a pure backend-cart-response mapper test and an App interaction test proving server-returned quantity/price changes replace optimistic cart state in the header, cart, minicart, subtotal, and Pay Later amount copy. The mapper preserves UI metadata, applies server item IDs/product IDs/prices/quantities, and renders checkout-blocker notices; checkout promo/tax/shipping recalculation hooks were queued as the final M11.5 closeout.
- Closed Milestone 11.5 Buyer Flow Interaction Recovery with TDD: added failing CheckoutPage/App interaction tests for checkout draft recalculation hooks, then wired Delivery shipping/billing/shipping-option submits and Pickup location/store/billing/date submits to checkout draft API updates, mapped returned draft summary totals/promos/shipping options/pickup inventory back into buyer checkout data, and added journey tests proving PDP-to-minicart-to-Delivery-payment and cart-to-Pickup-payment paths reach payment selection. PayPal synchronized shipping callback totals, final express review snapshot, capture, and amount consistency guard remain deferred to Milestone 13.

## 2026-06-14

- Continued Milestone 13 Express Review and Confirm with TDD: added failing route/repository/App interaction tests for loading a synchronized delivery express review snapshot by PayPal order ID, then added `GET /api/paypal/orders/express-review`, repository loading from the latest `paypal_shipping_update` total snapshot, optional shipping-address display fields captured from PayPal callbacks, frontend API-to-review-page mapping, and App route loading for `/checkout/express-review?paypal_order_id=...`. Review now shows server-backed item, shipping, promo, tax, total, and amount-guard data; confirm-triggered capture and buyer-facing capture blocking remain open.
- Investigated missing official PayPal surfaces in the running app. Checkout SDK config was blocked because the web API client ignored `VITE_API_BASE_URL`, Vite was not loading the demo-root `.env`, and the API server lacked local CORS for the Vite origin. Added focused tests and fixed those plumbing issues; checkout now loads PayPal SDK v6 assets and renders the official "Pay with PayPal" button. PDP/cart/minicart still render local express shell buttons and are now tracked as a remaining Milestone 13 recovery item because they must mount official SDK controls and call `/api/paypal/orders/express-delivery` with the active cart binding.
- Reopened checkout interaction recovery after live UX review. The active truth is now: fix official payment button placement, Delivery shipping/billing collapse-and-edit behavior, shipping-option saved state and Order Summary recalculation, Supabase-backed loading/error handling, and PayPal popup/create-order error visibility before continuing confirm capture work.
- Continued the Milestone 13 checkout recovery with TDD: added `UX_STATE_CONTRACT.md` plus a Superpowers mockup, gated checkout PayPal/Pay Later/wallet/card surfaces so they render only when the Payment step is active, normalized initial Delivery/Pickup accordion states, created a server checkout-draft UUID before live PATCH calls, mapped shipping totals into Order Summary, added retryable section-level API failure alerts, and added a standard API error envelope for route dependency failures. Official PDP/cart/minicart express SDK controls and PayPal popup/create-order failure handling remain open.
- Browser sanity on a clean `/checkout?sanity=1` reload showed no console errors, Shipping address expanded with Submit, Payment collapsed, and no official PayPal action mounted on initial checkout load.
- Fed the recovery lesson back into future demo generation: `NEW_DEMO_PROTOCOL.md`, standard/complex `AGENTS.md` templates, implementation-plan templates, and test-case templates now require state-contract/mockup alignment for multi-step UI, browser evidence for official PSP/wallet SDK surfaces, and backend loading/success/failure coverage for API-backed UI.
- Recovered PDP/cart/minicart official express surfaces with TDD: backend cart responses now preserve `cart_public_id` into buyer `CartData`; `DeliveryExpressAction` mounts official PayPal/Pay Later SDK v6 one-time buttons, refreshes cart before create-order, calls `/api/paypal/orders/express-delivery` with `{ cart_id, method }`, and routes to Review and Confirm after PayPal approval. App route tests and browser checks now show ready SDK provider scopes for PayPal and Pay Later on PDP, cart, and open minicart.
- Reviewed the implementation plan after merging the baseline to main and queued Milestone 13.1 Cart, Checkout Draft, and Pay Later Recovery before confirm/capture work. The recovery plan maps newly found live UX gaps to existing M13 work where possible, adds explicit cart persistence, minicart, Pay Later eligibility, and create-order failure tasks, and keeps capture/amount guard work after readiness is stable.
- Reconciled the live browser UX audit back into the truth files: M13.1 now explicitly covers persisted cart public ID plus client secret, paired cart headers on cart/checkout/PayPal API calls, checkout-route cart continuity, minicart quantity editing, Pay Later visible eligibility/details gating, and Pickup guest/logged-in initial-state plus active-market fixture cleanup. No runtime code changed in this alignment step; local audit logs/screenshots remain untracked evidence artifacts.
- Started M13.1 implementation with TDD for the cart-binding blocker: web cart reconciliation now preserves `cart.binding.cart_client_secret`, the API client accepts per-request headers, App restores a persisted `cart_public_id`/`cart_client_secret` binding from market/profile-scoped browser storage through `GET /api/cart`, cart/checkout draft calls attach paired `x-cart-id`/`x-cart-secret`, and PDP/cart/minicart delivery express create-order requests carry the same headers when a guest cart secret is present. Focused tests cover API headers, cart model binding preservation, App restore/header propagation, and express create-order request options.

## 2026-06-15

- Continued M13.1 with TDD for the mini-cart quantity gap: added failing `MinicartShell` and App interaction tests for mini-cart plus/minus controls, then wired mini-cart quantity edits through the same App-owned server-backed cart update/reconcile path as the full cart. The mini-cart now PATCHes `/api/cart/items/:id` with paired guest cart headers when available, reconciles server-returned quantity/price, and refreshes item count plus amount-aware Pay Later copy.
- Added checkout-route cart continuity regression coverage for M13.1: cart-to-checkout navigation now proves `POST /api/cart/refresh` carries the paired guest cart headers, the header cart count stays bound after `/checkout` renders, and the mini-cart still shows the active cart items and amount-aware Pay Later copy. This behavior was already satisfied by the cart-binding/header recovery slice and is now explicitly tracked.
- Continued M13.1 with server-ready binding guards: checkout draft updates now stop before `/api/checkout/drafts` when the guest cart lacks a paired `cart_public_id` and `cart_client_secret`, preserving buyer-safe "cart is still syncing" status; PDP/cart/minicart delivery express create-order now requires the paired binding before official PayPal/Pay Later buttons can call `/api/paypal/orders/express-delivery`.
- Continued M13.1 Pay Later SDK v6 recovery with local wiki and installed SDK type evidence: checkout Pay Later and PDP/cart/minicart delivery express Pay Later now call `useEligibleMethods` with amount, currency, and `ONE_TIME_PAYMENT` flow, require `isEligible("paylater")` plus `getDetails("paylater")` country/product details, and hide official Pay Later buttons behind buyer-safe pending/unavailable status until eligible details exist.
- Continued M13.1 Pickup initial-state recovery with TDD: guest Pickup now starts with a blank ZIP/postcode field, no default-address checkbox, no preselected store, and an Order Summary prompt to choose a pickup store; logged-in/default-address Pickup still starts with the nearest/default-address store and Change store action. Regression coverage also blocks active-market fixture leakage such as the GB `W1F 7JL` postcode appearing in the US Pickup flow.
- Continued M13.1 create-order failure recovery with TDD: checkout PayPal, checkout Pay Later, and delivery express SDK buttons now translate backend app error envelopes into visible buyer-safe alerts with merchant debug references and a retry affordance, while preserving console debug output for local troubleshooting.
- Continued M13.1 fresh-cart bootstrap recovery with TDD after live browser smoke exposed fixture cart IDs reaching the server-backed cart path. A fresh browser now reads/creates a guest cart without headers, persists the returned `cart_public_id`/`cart_client_secret`, seeds starter cart items through `/api/cart/items` using released Supabase product IDs, and preserves starter cart metadata so later quantity edits use returned server item IDs plus paired cart headers. This remains a tactical bridge until the buyer UI loads its product/cart state directly from the server catalog.
- Verified the M13.1 cart recovery in Chrome through Computer Use: full-cart quantity edits persisted after reload once the async restore completed, minicart quantity controls updated through the same cart path, checkout navigation kept `Cart (4)` instead of resetting to zero, Delivery shipping submit saved/collapsed and advanced Billing, and checkout Order Summary recalculated from the server draft. Also fixed a live presentation regression where cart API reconciliation could replace curated POP MART product labels with raw seed catalog names after quantity sync.
- Completed Milestone 13 Capture Completion with TDD: added failing App interaction tests proving Review and Confirm sends no capture before buyer confirmation, posts `/api/paypal/orders/:paypalOrderId/capture` only after Confirm and pay, shows captured status plus PayPal capture ID after success, and blocks the buyer capture action when the amount consistency guard fails. Fresh verification passed `npm run typecheck`, `npm test` (60 files, 325 tests), `npm run lint`, `npm run format:check`, and `git diff --check`; Graphify was refreshed after the code change.
- Started Milestone 14 Account, Guest, Reviews with TDD: added server route tests for `POST /api/account/auth/lookup` and App interaction tests for the email-first Sign in modal, then wired normalized account email lookup through the account repository and branched existing accounts to password entry or unknown emails to password-only registration. This slice intentionally stops before real Supabase Auth session creation/cart merge, which remains tracked separately. Fresh verification passed `npm run verify` (60 files, 329 tests); Graphify was refreshed after the code change.
- Continued Milestone 14 with Supabase Auth submit and authenticated cart merge using TDD: added App interaction tests for existing-account sign-in, new-account registration, and restored-session cart loading; added the browser Supabase Auth client with session persistence; sign-in/register now merges the active guest cart with bearer plus guest-cart headers, drops guest cart secrets once the backend returns an authenticated cart, clears the local opaque guest binding, and refreshes the UI to the signed-in cart. Fresh verification passed `npm run verify` (60 files, 333 tests) and `git diff --check`; Graphify was refreshed after the code change. Logged-in checkout/delivery-express PayPal create-order bearer-context wiring is tracked as the next M14 follow-up.
- Ran a Computer Use plus Playwright UX audit after the M14 auth/cart-merge slice. PDP official PayPal/Pay Later surfaces resolve after cart restore, Delivery checkout accordion progression, shipping recalculation, Pay Later switching, and card switching mostly match the agreed UX. Reopened follow-ups for real Supabase Auth sign-in failing at the remote token endpoint with a database schema error, Playwright/desktop minicart placement outside the viewport, US Pickup ZIP search showing GB/London stores, and Order Summary displaying "PayPal selected" before Payment is active. Also noted smaller polish gaps for cramped saved-address summaries, auth password-manager semantics, missing favicon, and the Milestone 16 playful POP MART visual refresh.
- Recovered the June 15 live audit blockers with TDD and browser evidence: seeded Supabase Auth rows now include Auth-readable empty token fields and the remote seeded users can sign in; closed minicart state is inert and the open drawer is inside the desktop viewport with quantity controls; Pickup location submit now saves/collapses location, expands store selection, and opens a US-market store modal for US ZIP `10012`; checkout Order Summary now says "Choose payment method" until the Payment section is active. Fresh verification passed `npm run verify` (60 files, 336 tests), `git diff --check`, live Playwright checks for auth/Pickup/minicart/payment-neutral summary, and Graphify was refreshed after the code change.
- Continued Milestone 14 with TDD for logged-in payment context and checkout option responsiveness: delivery express no longer disappears after auth cart merge when the guest cart secret is removed, checkout PayPal/Pay Later/card/wallet create-order actions preserve authenticated bearer request options, Delivery/Billing submit starts the draft API update immediately instead of waiting behind artificial timers, and Delivery shipping-option changes immediately update Order Summary shipping and total before final backend reconciliation.

## 2026-06-16

- Verified the logged-in payment-context recovery in Chrome through Computer Use: seeded Alice can sign in and merge the cart, Delivery checkout advances shipping -> billing -> shipping option -> payment with immediate submit feedback, changing Standard to Express updates Order Summary shipping from `$5.95` to `$12.95` and total from `$313.88` to `$320.88` before submit, and checkout/PDP/cart/minicart all render official PayPal plus Pay Later surfaces after SDK eligibility settles.
- Aligned `PLAN.md` and `IMPLEMENTATION_TASKS.md` after the M14 recovery: `.env.example` setup is now marked complete, stale Milestone 1 setup wording was removed from the active router, and final PayPal sandbox capability review is tracked under the Milestone 16 manual sandbox checklist.
- Continued Milestone 14 account settings with TDD: added web API `DELETE` support, a signed-in Account route, profile email display, saved-payment list loading through bearer auth, and saved-payment deletion through the account API. Fresh verification passed `npm run verify` (60 files, 344 tests), `git diff --check`, and root/demo Graphify refresh.
- Continued Milestone 14 address book with TDD: added authenticated `GET/POST/PATCH/DELETE /api/account/addresses`, Supabase account repository support, make-default behavior that clears prior defaults, default shipping/billing delete constraints, ownership checks before address updates, Account settings address list/add/edit/delete UI, and focused server/repository/App interaction coverage. Fresh verification passed `npm run verify` (61 files, 351 tests) and `git diff --check`.
- Started the UI/UX Design Recovery gate after the account/address UX review: expanded `DESIGN.md` from broad direction into the frontend source of truth with POP MART tokens, component contracts, detailed page implementation specs, UX-flow contracts, and slice close gates; updated `PLAN.md`, `IMPLEMENTATION_TASKS.md`, demo/root guardrails, and tracking so Account, Order history/detail, Guest lookup, inline registration, reviews, and Milestone 16 visual refresh must follow the recovered design instead of extending the generic red/white shell.
- Completed the Account settings UI polish against the recovered `DESIGN.md` account spec: added a buyer account hub header/nav, overview cards, POP MART-style profile/address/payment cards, visible default-address delete reasons, buyer-friendly loading/error/empty states, and inline destructive confirmation for saved-payment/address deletion. Fresh verification passed `npm run verify` (62 files, 354 tests). Browser visual QA captured desktop and mobile account screenshots through Vite; live account data could not load in that pass because the Express API server was not started with required `.env` values, so the pass verified the polished shell plus error/empty states.
- Continued Milestone 14 order history/detail with TDD: added authenticated `GET /api/account/orders` and `GET /api/account/orders/:orderNumber`, Supabase account repository mapping for buyer-owned orders, item review eligibility, lifecycle timeline, buyer-safe address/totals DTOs, and frontend bearer-auth loading for list/detail routes. Account order UI now follows the recovered `DESIGN.md` retail-card/detail contracts, avoids built-in fake fallback data in the App path, and keeps PayPal/payment-session/internal IDs out of buyer markup. Fresh verification passed `npm run verify` (62 files, 362 tests). Playwright visual QA against Vite with mocked local account APIs captured `account-orders-visual.png` and `account-order-detail-visual.png`, confirming the order card, timeline, items, totals, and date display.

## 2026-06-17

- Continued Milestone 14 guest lookup with TDD: added `/guest-orders` route resolution, a POP MART receipt lookup page, API-backed lookup through existing `GET /api/guest-orders/:orderNumber?email=...`, and read-only guest order detail mapping for fulfillment, payment, recipient, items, and totals while keeping technical PayPal/payment-session/internal IDs out of buyer markup. Fresh verification passed `npm run verify` (62 files, 363 tests). Browser visual QA remains required before closing the customer-facing slice.
- Continued Milestone 14 guest confirmation account linking with TDD: added authenticated `POST /api/account/guest-orders/link`, Supabase repository claiming of unowned guest orders by hashed checkout email, an express Review and Confirm post-capture account creation prompt for guests, and App interaction coverage proving registration links matching guest orders while signed-in captures do not show the guest prompt.
- Ran a Computer Use plus `ui-ux-pro-max` UX review of the guest confirmation account-link flow against mocked local review/capture APIs. Confirmed the modal has contextual copy, a labeled email input, and a mobile-readable layout. Reopened polish follow-ups because the save-order CTA sits below the first desktop post-capture viewport, the disabled `Confirm and pay` button still dominates the captured summary, the capture status and PayPal capture ID are cramped together, and the CTA copy wraps awkwardly in the desktop sidebar and 390px mobile view. Evidence screenshots: `guest-account-link-ux-review-lower.png`, `guest-account-link-modal-ux-review.png`, and `guest-account-link-mobile-prompt-ux-review.png`.
- Ran a Computer Use plus `ui-ux-pro-max` UX review of homepage, category, PDP, and checkout desktop/mobile screenshots. The pages remain functional and readable, but the reviewed baseline still feels closer to a sparse red/white ecommerce shell than the recovered POP MART playful-premium direction. Reopened Milestone 16 follow-ups for richer collectible/drop homepage modules, mobile category filter compression, mobile PDP purchase-action visibility, and checkout empty-state/mobile-summary polish. Evidence screenshots: `ux-home-desktop.png`, `ux-home-mobile.png`, `ux-category-desktop.png`, `ux-category-mobile.png`, `ux-pdp-desktop.png`, `ux-pdp-mobile.png`, `ux-checkout-desktop.png`, and `ux-checkout-mobile.png`.
- Ran a Computer Use plus `ui-ux-pro-max` UX review of cart and minicart desktop/mobile states. The prior desktop minicart placement fix is holding, and quantity controls remain visible, but the cart summary still reads like a thin subtotal-only sidebar, mobile cart barely reaches the checkout action in the first summary pass, PayPal/Pay Later pending surfaces repeat identical "cart is refreshing" copy, and the minicart action area feels crowded by Pay Later, View cart, Checkout, express pending copy, and pickup hint. Reopened Milestone 16 follow-ups for cart summary hierarchy, express pending-state labeling/consolidation, and minicart drawer density/backdrop polish. Evidence screenshots: `ux-cart-desktop.png`, `ux-cart-mobile.png`, `ux-minicart-desktop.png`, and `ux-minicart-mobile.png`.
- Generated a first-pass primary toy image for each of the 25 seeded POP MART-profile products using POP MART GB as high-level merchandising reference only, without copying official logos, packaging, or characters. The generated original demo images were saved as 900x900 PNGs under `web/public/assets/popmart/products/`; remaining work is to generate second/third gallery views, reconcile seeded runtime image paths with the local generated asset convention, and review IP distance plus image quality before customer-facing use.
- Wired the generated POP MART product images into deterministic seed/runtime data with TDD: POP MART product image rows now seed one existing `/assets/popmart/products/{slug}-1.png` primary image per toy, seeded order item snapshots use the same generated PNG path, and generic profile gallery rows keep their existing 3-image `.webp` convention. Verified `npm run verify` passes, `npm run seed:summary` reports 100 product image rows, and a local seed asset audit found 25 generated `/assets/...` product paths with zero missing files.
- Applied a targeted linked Supabase image refresh after full linked seeding was blocked by an existing live-cart duplicate row. Verified the remote POP MART slice has 25 generated product image rows, zero stale extra gallery rows, zero non-generated image paths, and generated order-item image snapshots.
- Continued generated-image runtime integration with TDD and Computer Use verification: category and direct PDP routes now fetch live catalog API data and render generated PNGs, starter guest carts no longer overlay stale Labubu/Hirono display metadata, and cart reconciliation now prefers API slug/name/image/price fields for cart and minicart rows. Computer Use verified generated images and corrected links/alt text on category, PDP, minicart, and full cart.
- Reopened two data-continuity follow-ups from the Computer Use pass: home still renders static Labubu/Skullpanda/Hirono merchandising because the current home API returns section config rather than product-card payloads, and initial checkout still shows the default `$25.98` order summary while the active restored cart total is `$69.68`.
- Closed the generated-image data-continuity follow-up with TDD: home now maps live catalog products into hero, hot-sales, category, calendar, and series merchandising while preserving fallback content for malformed catalog responses; direct checkout now reconciles the initial Order Summary subtotal/total from the restored active cart; and direct API-backed PDP routes render a loading state instead of a transient not-found page while product detail data is pending.
- Ran live Computer Use plus Playwright verification for API-backed Home, PDP, Category, Cart, Minicart, and Checkout. Closed two UX regressions found in the pass: category route queries now forward filter params to the catalog API and mark the active filter/count state, and the mini-cart express PayPal/Pay Later actions now stack without overlap on desktop and 375px mobile. Clean Playwright reload after clearing stale storage showed no app console errors.
- Reopened live-route hydration and Pay Later message follow-ups after Browser QA: Home, Category, released PDP, Cart, Checkout, and hidden Minicart still mount old fixture SVG/image data during first paint before generated API-backed data resolves; Category, PDP, Cart, and Minicart render static Pay Later copy but no official PayPal `<paypal-message>` component. Evidence screenshots were saved outside the repo under `/private/tmp/paypal-retail-category-initial-fallback.png` and `/private/tmp/paypal-retail-category-final-generated-no-message.png`.
- Closed the live-route hydration and Pay Later message follow-ups with TDD plus live browser verification: App interaction coverage now holds cart/catalog/PayPal SDK requests pending and proves Home, Category, PDP, Cart, Checkout, and hidden Minicart do not render old Labubu/Hirono/Skullpanda fixture text or SVGs before API-backed generated data resolves. Category, PDP, Cart, and open Minicart now render official PayPal `<paypal-message>` components with placement markers (`catalog-promo`, `product-detail`, `cart-summary`, `minicart-summary`) and static fallback copy for SDK loading/error states. Fresh `npm run verify` passed (62 files, 380 tests). Playwright verified hydrated Category/PDP/Cart/Minicart have generated PNGs, no old fixture hits, official PayPal message nodes, and no browser console errors/warnings; evidence screenshot: `ux-after-fix-minicart.png`.
- Aligned stage wording across `PLAN.md` and tracking files after the generated-image/Pay Later recovery: Milestone 16 QA, UX Review, and Demo Polish became the active execution stage. At that time, M14 review submission/edit/delete was still deferred alongside M15 Admin Portal, local Supabase migration verification, and full PayPal sandbox wallet/capture checks. No runtime code changed in this alignment step.
- Completed the remaining Milestone 14 review submission/edit/delete slice with TDD: added authenticated account review routes, Supabase repository mutations against active `reviews` rows, account order DTO review mapping, inline account order review forms, App shell mutation wiring with bearer auth, and product-detail cache invalidation for reviewed product slugs so the next PDP load refetches active reviews. Focused verification passed account route/repository/component/App suites and `npm run typecheck`. Full `npm run verify` passed afterward. Browser visual verification against Vite with mocked auth/account APIs captured `account-review-form-visual.png` and `account-review-submitted-visual.png`; Playwright console scan reported zero warnings/errors.

## 2026-06-18

- Closed the Milestone 16 guest confirmation account-link polish follow-up with TDD and browser evidence: the captured Express Review state now removes the disabled `Confirm and pay` action, renders a receipt-style capture status block with a separate PayPal capture ID row, focuses/scrolls the `Save guest order` prompt after guest capture, and uses tighter `Save order` CTA copy that fits the desktop summary column and 390px mobile viewport. Focused component/App tests passed, and Playwright verified desktop plus 390px mobile capture and modal states with no console warnings/errors or Vite overlay. Evidence screenshots: `.playwright-mcp/paypal-retail-guest-link-desktop-viewport.png`, `.playwright-mcp/paypal-retail-guest-link-mobile-viewport.png`, `.playwright-mcp/paypal-retail-guest-link-desktop-modal-viewport.png`, and `.playwright-mcp/paypal-retail-guest-link-mobile-modal-viewport.png`.
- Completed the Milestone 16 generated toy imagery review with `ui-ux-pro-max` guidance and a Playwright contact sheet. All 25 primary PNG assets load at 900x900 and the set is visually coherent enough for development/visual refresh work, but it is not final customer-demo media yet: `blind-boxes-1`, `plush-11`, and `accessories-21` lean too close to monster-eared POP MART character cues, while `blind-boxes-4`, `plush-14`, and `accessories-24` lean too close to black panda/moon Skullpanda cues. `vinyl-figures-6` and `mega-collection-16` need a stricter second pass for tear-heavy Crybaby similarity. Seeded POP MART product-image alt text now uses category-aware descriptive copy instead of generic `view 1` labels, with focused seed test coverage. Evidence screenshot: `.playwright-mcp/popmart-product-image-contact-sheet.png`.
- Rejected the flat/vector replacement attempt for the six high-IP-risk product images because it degraded the premium collectible catalog look. Restored the original 900x900 PNGs for `blind-boxes-1`, `plush-11`, `accessories-21`, `blind-boxes-4`, `plush-14`, and `accessories-24` from backup, byte-verified the restores, and kept the high-IP-risk replacement task open until a path-controllable high-quality generation/source workflow can produce assets that match the current rendered catalog quality. Evidence screenshot: `.playwright-mcp/popmart-product-image-contact-sheet-restored.png`.
- Closed the category sidebar metadata alignment follow-up with TDD: category filters now suppress unsupported `series` query links until the API exposes real series filtering/count metadata, the default category subtitle no longer advertises series filtering, and `DESIGN.md` now lists category rather than `category/series` for V1. Focused CategoryPage coverage proves unsupported series filter metadata is not rendered.
- Closed the POP MART visual foundation slice with a CSS contract test and browser evidence: `.theme-popmart` now owns the approved coral, candy pink, lemon, mint, sky, lavender, warm-white, card, ink, muted, border, and error tokens, aliases them into shared shell variables, and applies a rounder Rubik/Nunito Sans font stack with system fallbacks. Generic remains on its separate profile palette. Storefront cards, category modules, Pay Later panels, checkout panels, and related retail surfaces now use shared radius/shadow/payment tokens instead of extending the generic red/white shell. Evidence screenshots: `.playwright-mcp/paypal-retail-visual-foundation-home-desktop.png`, `.playwright-mcp/paypal-retail-visual-foundation-home-mobile.png`, and `.playwright-mcp/paypal-retail-visual-foundation-category-mocked-products.png`.
- Reopened and corrected the home visual-foundation evidence after the Vite-only homepage screenshots still showed old Labubu fallback merchandising. Root cause: `defaultHomePageData` still pointed at the old Labubu/Hirono/Skullpanda SVG fixture set even though the API-backed homepage path maps live generated PNG catalog products, and the hard network-error path stayed on the pending shell instead of promoting fallback data. Added focused fallback regression coverage, replaced the default home fallback with generated PNG product assets and category-backed links, and promoted the cleaned fallback after catalog request failures. Browser verification against Vite-only home showed generated PNGs, no old fixture text/image paths, and no unsupported `series=` links; the expected console errors were the absent Express API calls. Replacement evidence screenshot: `/private/tmp/paypal-retail-home-fallback-generated.png`.
- Deferred the high-quality replacement workflow for high-risk toy images behind the non-image Milestone 16 gates, then closed the current continuity/payment-message follow-up with TDD and live browser evidence. Category Pay Later now removes local fallback copy once the official PayPal provider is ready, while keeping fallback copy for SDK config loading/error. PayPal Create Order line items now resolve product URLs and image URLs to public absolute URLs before provider payload construction. Live browser checks after clearing stale Playwright routes verified API-backed Home and Checkout generated PNGs at 900x900 with no broken images, and Category rendered a ready PayPal provider with an official `paypal-message` and zero local fallback paragraphs. Fresh verification passed `npm run verify` (63 files, 399 tests) and `git diff --check`.
- Updated the M16 visual target from "playful red/white POP MART refresh" toward the user-provided Sakura Figure Shop reference level, adapted to the POP MART profile: dense retail header/search/nav, cinematic image-led home hero, trust strip, release/product/category/promo modules, deep footer, and PDP gallery/economics/trust/details hierarchy. Also accepted the current generated POP MART-leaning primary images for this Popmart-specific demo version, while preserving the need for separate safer media in generic/non-Popmart audience demos.
- Added implementation-grade guidance for the approved reference-level polish so the next M16 implementation slice has concrete file ownership, module order, data-truth constraints, test expectations, and visual QA gates. New guide: `docs/superpowers/plans/2026-06-18-popmart-reference-polish.md`. Updated `DESIGN.md`, `PLAN.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` to require the plan before coding, prevent fake search/preorder economics/ratings/trust claims, and split shell/Home/Category/PDP/Cart/Minicart/Checkout polish into independently verifiable acceptance rows.
- Extended the approved reference-level polish plan with the user's checkout, order confirmation, cart, category, and account reference collage. The M16 guide and related trackers now include checkout left/right step-summary composition, cart item-table plus summary/progress/trust hierarchy, captured Express Review/order-confirmation success hierarchy, and Account/sign-up split art/benefits/form composition. Added explicit safeguards against fake social auth and fake payment-method visuals while preserving official PayPal surfaces.
- Extracted durable guidance from `/Users/tengtao/Downloads/sakura_figure_shop_design.md` and the detailed PDP reference screenshot into the M16 guide and `DESIGN.md`: premium calm collector tone, navy/gold/cream system, optional serif page-title direction, 1280-1440px dense commerce width where safe, card/image/badge rules, responsive behavior, and a richer PDP target with vertical thumbnail rail, stable gallery, factual chips, right-side purchase/preorder rail, dense product facts, tabbed details, and real-data-only review/recommendation rails.
- Translated the generated page-level UX concept mockups and `ui-ux-pro-max` mobile guidance into the M16 design and implementation docs. `DESIGN.md` and the M16 polish guide now require mobile-first defaults, no horizontal page scroll, touch-sized controls, drawer/sheet/accordion treatment for filters, vertical cart cards, independent minicart scroll/action regions, checkout/account form input type/inputmode/autocomplete/validation/submit-feedback rules, and visual QA at 320/375/414/768/1024/1440px. Updated `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` with generated-mockup hierarchy and mobile acceptance gates.
- Closed the M16 buyer shell reference-polish slice with TDD and browser evidence: the buyer shell now has a deep navy utility/header band, supported navigation/discovery links, buyer actions with sign-in, intentionally disabled Wishlist, live cart count, no inert keyword search, and a route-wide production footer with shop/help links. Focused App shell test was written red-first and now passes. Fresh verification passed `npm run typecheck`, full `npm test` (63 files, 399 tests), `npm run lint`, `npm run format:check`, and `git diff --check`. In-app browser QA verified the home route at 1280px, 375px, and 320px with shell content present, minicart open interaction working, no Vite overlay, and no horizontal page overflow; expected console errors were limited to homepage/cart API fetch failures because the visual pass ran Vite without the Express API server. Evidence screenshots: `/private/tmp/paypal-retail-shell-desktop.png` and `/private/tmp/paypal-retail-shell-mobile-320.png`.
- Refined the M16 buyer header against the user's sample header screenshots: replaced the coarse inline Home/Shop/New arrivals/Checkout nav with a three-part header, including utility links, a brand/discovery/action row, and a dedicated product-category nav for NEW, COLLECTIONS, PRE-ORDERS, BLIND BOXES, FIGURES, ACCESSORIES, BRANDS, SALE, and ABOUT. Checkout is intentionally removed from the product nav because Cart/minicart own checkout entry. The central discovery pill links to `/products` rather than pretending to be keyword search. Fresh verification passed focused App shell test, `npm run typecheck`, full `npm test` (63 files, 399 tests), `npm run lint`, `npm run format:check`, and browser checks at 1280px, 375px, and 320px with no horizontal overflow and no product-nav Checkout. Evidence screenshots: `/private/tmp/paypal-retail-header-refined-desktop-v2.png` and `/private/tmp/paypal-retail-header-refined-mobile-320-v2.png`.
- Closed the M16 Home reference-level merchandising slice with red-first Home module-order coverage and in-app browser evidence. Home now renders the image-led blind-box hero, local trust strip, release calendar plus hot-sales board, category shelf, promo cards, and popular-series rail in the approved order while leaving newsletter/deep footer ownership to the buyer shell. Removed the old standalone Home Pay Later/footer block, tightened the mobile hero image so the trust strip starts in the 320px first viewport, fixed tablet shell overflow at 768px, and prevented the closed minicart from contributing page width. Fresh verification passed `npm run verify` (63 files, 400 tests) and `git diff --check`. Browser QA verified Home at 320px, 375px, 414px, 768px, 1024px, and 1440px with no horizontal overflow, no Vite overlay, generated POP MART PNG assets, no old Labubu/Skullpanda/Hirono fallback text, and hero Browse all navigation to `/products`; expected console errors were limited to homepage/cart API fetch failures because the visual pass ran Vite without the Express API server. Evidence screenshots: `/private/tmp/paypal-retail-home-refined-desktop.png` and `/private/tmp/paypal-retail-home-refined-mobile-320.png`.
- Closed the M16 compact mobile buyer header slice with `ui-ux-pro-max` guidance, red-first App shell/interactions coverage, and in-app browser evidence. Mobile now hides the desktop product-category nav grid, visually simplifies the brand to a POP mark, keeps Browse/Sign in/Wishlist/Cart/Menu in compact touch-sized controls, and moves product categories plus Help/Track order/Order recovery into an `aria-expanded` hamburger drawer. Desktop keeps the full product nav and hides the hamburger. Fresh verification passed `npm run verify` (63 files, 401 tests). Browser QA verified 320px, 375px, 414px, 768px, 1024px, and 1440px with no horizontal overflow, 44px mobile action targets, no Vite overlay, drawer open/close behavior, and expected Vite-only API/cart fetch errors because the Express API server was not running. Evidence screenshots: `/private/tmp/paypal-retail-mobile-header-compact-closed-320.png`, `/private/tmp/paypal-retail-mobile-header-menu-open-320.png`, and `/private/tmp/paypal-retail-mobile-header-desktop-1440.png`.
- Closed the M16 mobile category filter compression slice with red-first CategoryPage/static CSS coverage and in-app browser evidence. Mobile category pages now hide the desktop sidebar below the mobile breakpoint, render a compact filter/details rail before the product grid, preserve unique desktop/mobile filter IDs, keep the opened filter panel full width with Reset as a top-row action, and use a two-column mobile product grid without horizontal overflow. Browser QA verified `/products?category=plush` at 320px, 375px, 414px, 768px, 1024px, and 1440px with no Vite overlay or page overflow; product-card visual loading stayed blocked in the Vite-only pass because the Express API server requires local Supabase/PayPal environment variables, while component tests prove product-grid ordering when product data is present. Evidence screenshots: `/private/tmp/paypal-retail-category-mobile-filters-closed-320.png`, `/private/tmp/paypal-retail-category-mobile-filters-open-320.png`, and `/private/tmp/paypal-retail-category-desktop-filters-1440.png`.

## 2026-06-19

- Added targeted PayPal create-order diagnostics after live cart checkout debugging showed buyer-safe errors needed stronger frontend/backend evidence. Delivery express now logs safe structured start/prepared/failure stages for PDP/cart/minicart PayPal and Pay Later starts, and the backend create-order route logs safe route/gateway/recording stages with debug ID, method/source/kind, amount/order/session context, and secret-presence booleans instead of raw secrets. Focused frontend/backend regression coverage verifies the logs exist and do not include cart secrets, bearer tokens, or PayPal credentials.
- Refined the M16 mobile Home/category/Pay Later density slice from the latest user screenshot guidance. Mobile Home now uses a clickable image-led hero with contrast-safe overlaid title/subtitle and compact CTAs, and it visually orders the release/product board before the utility trust strip on mobile. Category runtime removes the explanatory `All products` subtitle, renders discounted product cards with a top-right `Sale` text badge, lazy-loads product-card imagery, and renders the official Pay Later message directly without wrapper-card chrome. Cart/minicart Pay Later message chrome was also removed through shared CSS, and initial Home loading gained a skeleton-style hero shimmer while non-hero product imagery uses lazy loading. Focused verification passed `npm test -- --run web/src/features/catalog/HomePage.test.tsx web/src/features/catalog/CategoryPage.test.tsx web/src/styles/global.test.ts`; full verification passed `npm run verify` (63 files, 418 tests). Browser QA at 375px verified mobile Home, Category, Cart, and open Minicart have no horizontal overflow; Category has no filter-description subtitle, shows one sale badge, and Pay Later wrappers/messages are transparent/borderless/padding-free. Evidence screenshots: `/Users/tengtao/Development/demo-projects/paypal-retail-home-mobile-hero-refined-375.png`, `/Users/tengtao/Development/demo-projects/paypal-retail-category-mobile-direct-paylater-sale-375.png`, and `/Users/tengtao/Development/demo-projects/paypal-retail-cart-minicart-direct-paylater-375.png`.
- Closed the M16 mobile PDP purchase-hierarchy slice with red-first ProductDetailPage and CSS coverage. `ProductDetailPage` now renders a purchase panel before long intro/details/review content, single-image PDPs omit the redundant thumbnail rail, and mobile gallery imagery is capped/contained so title, price, and Add to cart are reachable in the first mobile pass. Live browser QA used API-backed released product `blind-boxes-2` and verified 320px, 375px, 414px, 768px, 1024px, and 1440px with no horizontal overflow; Add to cart opened the minicart at 375px. Console scan showed only the known Supabase multiple-client warning from repeated browser tabs, with no relevant app errors. Evidence screenshots: `/private/tmp/paypal-retail-pdp-mobile-purchase-320.png`, `/private/tmp/paypal-retail-pdp-mobile-purchase-375.png`, `/private/tmp/paypal-retail-pdp-mobile-after-add-375.png`, and `/private/tmp/paypal-retail-pdp-desktop-purchase-1440.png`.
- Continued the M16 PDP detailed-reference polish with red-first structure/CSS tests and in-app browser evidence. PDP gained breadcrumb, status/review row, desktop-capable gallery stage, purchase panel, trust grid, and lower detail coverage for collector details, product facts, gallery, released-product customer reviews, shipping/returns caveat copy, and empty Q&A while avoiding deposit/remaining fake economics. Browser QA used API-backed `blind-boxes-2` and verified 320px, 375px, 414px, 768px, 1024px, and 1440px with no horizontal overflow, no framework overlay, and Add to cart reachable in the first 900px viewport after fixing mobile panel order; Add to cart opened the minicart at 375px. Console scan reported no warnings/errors. Evidence screenshots: `/private/tmp/paypal-retail-pdp-reference-mobile-375.png`, `/private/tmp/paypal-retail-pdp-reference-mobile-after-add-375.png`, and `/private/tmp/paypal-retail-pdp-reference-desktop-1440-settled.png`. Remaining PDP polish stays open for real-data recommendation/recently-viewed rails and any future richer gallery coverage beyond the current one-image API products.
- Refined the M16 PDP payment hierarchy after the user payment-frame review and `ui-ux-pro-max` funnel/progressive-disclosure guidance: removed the duplicate `Purchase status` / release-facts card from the current V1 purchase rail, placed the official amount-aware Pay Later message directly under the price, kept Add to cart as the primary action, wrapped official PayPal/Pay Later delivery express actions in an unclosed `Secured by PayPal` fieldset frame, and converted lower detail panels to semantic tabs. The frame collapses to PayPal-only while Pay Later eligibility is pending/unavailable and shows PayPal plus Pay Later when eligibility becomes `eligible`; mobile and tablet stack the official buttons to avoid PayPal SDK intrinsic-width overflow, while desktop keeps the two-column layout. Browser QA used API-backed `blind-boxes-2` and verified 320px, 375px, 414px, 768px, 1024px, and 1440px with no horizontal overflow, no `Purchase status` text or `.product-release-panel`, correct payment hierarchy, tab semantics, eligible and pending Pay Later frame states, and Add to cart opening the minicart at 375px. Evidence screenshots: `/private/tmp/paypal-retail-pdp-payment-polish-mobile-375.png` and `/private/tmp/paypal-retail-pdp-payment-polish-desktop-1440.png`.
- Continued M16 PDP polish with blind-box-specific commerce and merchandising behavior. API-backed blind-box PDPs now expose wired Random 1PC and Whole Box options; selecting Whole Box updates the displayed amount, official Pay Later message amount, add-to-cart selection, and cart quantity. The cart model now creates a local cart row for API-loaded PDP products that were not already in the cart, and browser QA caught and fixed the existing-cart `maxQuantity: 5` clamp so whole-box additions can exceed a stale server cart max. The purchase panel adds demo-safe scarcity/viewing copy and the PayPal frame now uses container-responsive sizing: mobile stacks PayPal/Pay Later and the 1440px desktop right rail renders them side by side. Lower PDP content adds media slots, collector story, icon-style specs, series lineup with secret odds, demo social proof, and a You may also like rail from existing generated catalog asset paths while keeping real multi-angle/package/silhouette/video asset generation open. Verification passed focused PDP/cart/CSS/App interaction coverage and full `npm run verify` (63 files, 415 tests). Browser evidence screenshots: `/Users/tengtao/Development/demo-projects/paypal-retail-pdp-blindbox-commerce-mobile-375.png` and `/Users/tengtao/Development/demo-projects/paypal-retail-pdp-blindbox-commerce-desktop-1440.png`.
- Refined the blind-box PDP mobile density after the user flagged the five media placeholder cards as unnecessary. `ProductDetailPage` no longer renders the text-only media strip below the gallery or the duplicate media-card grid inside the Gallery tab; media asset goals remain in design/tracking until real multi-angle/package/silhouette/video assets exist. Focused PDP coverage now asserts `Front render`, `Size comparison`, `Package view`, `Secret silhouette`, `Material detail`, `.product-media-strip`, and `.product-media-card` stay out of the rendered PDP. Full `npm run verify` passed (63 files, 415 tests). In-app browser QA at 375px confirmed API-backed `blind-boxes-2` renders the PDP with no media placeholder text/classes, no horizontal overflow, working detail tab interaction, and only the known Supabase multiple-client warning. Evidence screenshot: `/private/tmp/paypal-retail-pdp-no-media-placeholders-mobile-375.png`.
- Closed the live cart PayPal delivery express create-order blocker with systematic debugging and TDD. The fix pairs guest cart public ID/client-secret restore, clears/reacquires invalid stored bindings, forwards refreshed cart context into PayPal create-order, aligns PayPal/cart guest-secret hashing, and omits local HTTP PayPal callback/item URL fields unless a public HTTPS origin is configured. Fresh focused verification passed `npm test -- --run web/src/features/cart/cartModel.test.ts web/src/app/App.interactions.test.tsx web/src/features/payments/paymentActionFailure.test.tsx web/src/features/payments/DeliveryExpressAction.test.tsx web/src/features/payments/PayPalSdkProviderScope.test.tsx server/tests/paypalOrderRepository.test.ts shared/src/paypal.test.ts` (120 tests) and `npm run typecheck`. In-app browser QA on `/cart` verified the PayPal sandbox modal opens; backend logs showed cart refresh succeeded, PayPal create-order reached gateway, PayPal returned `PAYER_ACTION_REQUIRED`, and the order was recorded.

## 2026-06-20

- Continued the cart PayPal sandbox pass with systematic debugging after repeat-click QA found two additional create-order retry blockers. Cart-scoped pending-order lookup now chooses the latest express pending order only, excluding checkout-draft orders, and consumed PayPal sessions with an existing `paypal_order_id` now generate a new suffixed invoice/request attempt instead of reusing the old unique invoice. Focused red-first regressions cover both cases. In-app browser QA on `/cart` now reaches the PayPal sandbox login modal with backend evidence for order `DO-20260619-000001`, invoice `DO-20260619-000001-A2`, PayPal order `3TK87288PW762135A`, and `PAYER_ACTION_REQUIRED`. Full approval/capture remains open until a sandbox buyer login/session is available.
- Continued M16 cart/minicart payment polish with red-first cart/minicart/CSS coverage and browser QA. Added a shared `PayPalPaymentFrame`, moved cart and minicart PayPal/Pay Later delivery express controls into the `Secured by PayPal` frame, suppressed checkout/PayPal/Pay Later controls for empty cart/minicart states, and fixed the official SDK custom elements so the PayPal and Pay Later buttons get stable 44px+ render boxes instead of collapsing inside the frame. Desktop and tablet cart layouts now give the frame enough width for eligible PayPal/Pay Later buttons side by side, while mobile and minicart stack the official buttons; cart and minicart quantity steppers now meet 44px touch targets. Focused verification passed `npm test -- web/src/features/cart/CartPage.test.tsx web/src/features/cart/MinicartShell.test.tsx web/src/features/catalog/ProductDetailPage.test.tsx web/src/styles/global.test.ts`; full `npm run verify` passed typecheck, all 433 tests, and lint, then stopped at the existing repo-wide `format:check` drift in `server/src/paypal/client.ts`, `server/src/routes/cart.ts`, `server/src/routes/paypal.ts`, `web/src/app/App.interactions.test.tsx`, `web/src/app/App.tsx`, `web/src/features/cart/cartModel.ts`, `web/src/features/payments/DeliveryExpressAction.tsx`, and `web/src/features/payments/PayPalSdkProviderScope.test.tsx`. Touched-file Prettier check and targeted `git diff --check` both passed. In-app browser QA verified no horizontal overflow, `PayPal payment options ready`, visible PayPal/Pay Later SDK elements, side-by-side desktop/tablet controls, stacked 320/375px controls, and a clipped open-minicart drawer with visible framed buttons. Evidence screenshots: `/private/tmp/paypal-retail-cart-frame-qa-final/desktop-1440.png`, `/private/tmp/paypal-retail-cart-frame-qa-final/tablet-768.png`, `/private/tmp/paypal-retail-cart-frame-qa-final/mobile-375.png`, `/private/tmp/paypal-retail-cart-frame-qa-final/small-320.png`, and `/private/tmp/paypal-retail-cart-frame-qa-final/minicart-mobile-375-open-clipped.png`.

## 2026-06-21

- Re-sequenced the remaining M16 UI work around a shadcn-first component foundation before further page-level polish. `DESIGN.md`, `PLAN.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, `tracking/test-cases.md`, and the root M16 execution guide now require adopting shadcn source-owned primitives for repeated UI such as buttons, cards, badges, separators, skeletons, sheets, dialogs, tabs, accordions/collapsibles, scroll areas, and form fields. The plan explicitly keeps POP MART-specific ecommerce page composition custom and preserves official PayPal SDK-rendered buttons/messages/custom elements instead of replacing them with generic shadcn page blocks or local button facsimiles. Also removed the interrupted cart-summary hierarchy test expectations from the paused pre-shadcn slice while retaining the completed cart/minicart payment-frame coverage.
- Completed the M16 shadcn foundation gate with red-first coverage. The Vite app now has Tailwind v4 integration, `@/*` aliases into `web/src`, `components.json`, `web/src/lib/utils.ts`, and official shadcn Radix/Nova primitives for `Button`, `Card`, `Badge`, `Separator`, `Skeleton`, `Sheet`, `Dialog`, `Tabs`, `Accordion`/`Collapsible`, `ScrollArea`, and form fields. Added `web/src/components/ui/shadcnFoundation.test.ts` to lock the foundation files and aliases. Verification passed full `npm test` (64 files, 435 tests), `npm run typecheck`, `npm run lint`, production-only `npm audit --omit=dev`, targeted Prettier checks for touched files/docs, and targeted `git diff --check`. Full `npm run format:check` remains blocked by the existing eight-file formatting drift in `server/src/paypal/client.ts`, `server/src/routes/cart.ts`, `server/src/routes/paypal.ts`, `web/src/app/App.interactions.test.tsx`, `web/src/app/App.tsx`, `web/src/features/cart/cartModel.ts`, `web/src/features/payments/DeliveryExpressAction.tsx`, and `web/src/features/payments/PayPalSdkProviderScope.test.tsx`. The remaining shadcn work is migration of repeated shared shell/action/card/dialog/sheet/tab/form surfaces through these primitives before page-specific polish continues.
- Completed the first M16 shared shadcn migration cluster with red-first cart/minicart coverage. Cart and minicart local navigation, empty-state links, and fallback delivery express buttons now compose through shadcn `Button`; the shared `PayPalPaymentFrame` now composes through shadcn `FieldSet`/`FieldLegend`; Vitest resolves the same `@/*` alias used by Vite; and official SDK-rendered PayPal/Pay Later actions remain externally rendered instead of being replaced by local buttons. Verification passed focused cart/minicart/PDP/payment interaction tests, full `npm test` (64 files, 435 tests), `npm run typecheck`, `npm run lint`, touched-file Prettier checks, and targeted `git diff --check`.
- Completed the second M16 shared shadcn migration cluster with red-first PDP coverage. The lower PDP detail section now uses shadcn `Tabs`, `TabsList`, `TabsTrigger`, and `TabsContent` with the line variant while preserving the existing PDP class hooks, tab semantics, inactive panel rendering, and collector/product facts/gallery/review/shipping/Q&A hierarchy. Verification passed focused PDP/CSS tests, full `npm test` (64 files, 435 tests), `npm run typecheck`, and `npm run lint`.
- Completed the third M16 shared shadcn migration cluster with red-first checkout coverage. Checkout Delivery/Pickup fulfillment switching now uses shadcn `Tabs`, `TabsList`, `TabsTrigger`, and `TabsContent`, with checkout panels rendered directly as `TabsContent` to avoid nested tab panels. Locked-mode disabling, separate Delivery/Pickup panel rendering, step progression, and active payment context behavior are preserved. Verification passed focused checkout component/interactions plus App interactions coverage, full `npm test` (64 files, 435 tests), `npm run typecheck`, and `npm run lint`.
- Completed the fourth M16 shared shadcn migration cluster with red-first Category/PLP mobile-filter coverage. Category mobile filters now use shadcn `Sheet`, `SheetTrigger`, `SheetContent`, `SheetHeader`, `SheetTitle`, and `SheetDescription`; the compact trigger keeps an explicit accessible label, the opened sheet preserves supported mobile filter IDs, the desktop sidebar keeps its own IDs, and the native details/summary fallback is removed. Verification passed focused Category/CSS tests, full `npm test` (64 files, 436 tests), `npm run typecheck`, `npm run lint`, touched-file Prettier checks, targeted `git diff --check`, and a 375px Vite browser sanity check confirming the compact trigger opens a bottom `Filters` sheet. The browser pass showed only expected catalog/cart API fetch errors because the Express API server was not running.
- Completed the fifth M16 shared shadcn migration cluster with red-first auth dialog/form coverage. `AuthModalShell` now composes shadcn `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `FieldGroup`, `Field`, `FieldLabel`, `FieldError`, `Input`, and `Button` for the sign-in/register modal and the guest save-order account path. The Express Review save-order prompt remains an inline CTA card, but its account creation path opens the migrated dialog through existing App wiring. The auth form now opts out of native browser validation so the shadcn `FieldError` and `aria-invalid` state render consistently in browser and tests. Verification passed focused `AuthModalShell` and App interaction tests, full `npm test` (65 files, 438 tests), `npm run typecheck`, `npm run lint`, touched-file Prettier checks, targeted `git diff --check`, and a 375px Vite browser sanity check confirming the dialog opens and invalid email displays the accessible field error.
- Completed the sixth M16 shared shadcn migration cluster with red-first cart/minicart coverage. `CartPage` now composes the order summary through shadcn `Card` header/content/footer structure, uses shadcn `Separator` for summary division, and renders cart item categories through shadcn `Badge`. `MinicartShell` now renders the open drawer through a controlled shadcn `SheetContent` with `SheetHeader`, `SheetTitle`, `SheetDescription`, and `SheetClose`; closed minicarts are unmounted from the accessible drawer surface instead of remaining as an offscreen inert custom aside. App interaction tests were updated to follow the modal drawer contract by interacting inside the open drawer or closing it before page-level controls. Verification passed red/green focused cart/minicart tests, App interaction tests, full `npm test` (65 files, 439 tests), `npm run typecheck`, `npm run lint`, touched-file Prettier checks, targeted `git diff --check`, and a 375px Vite browser sanity check confirming the cart summary card slots, right-side minicart Sheet, scroll lock, outside-content hiding, close control, and no horizontal overflow.
- Completed the seventh M16 shared shadcn migration cluster with red-first Express Review/order confirmation coverage. `ExpressReviewPage` now composes the payment-session status, order/delivery/items cards, final snapshot rail, capture receipt/status, and guest save-order prompt through shadcn `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardAction`, `CardContent`, `CardFooter`, `Badge`, `Separator`, and `Button` while preserving amount-guard blocking, capture ID display, captured-state confirm removal, and save-order focus/scroll behavior. Verification passed the red/green focused Express Review suite, focused App interaction coverage (`54` tests), full `npm test` (`65` files, `439` tests), `npm run typecheck`, `npm run lint`, touched-file Prettier checks, and targeted `git diff --check`.
- Completed the eighth M16 shared shadcn migration cluster for the buyer shell and release calendar. The header now uses shadcn `Avatar` for account/sign-in, `lucide-react` icons for wishlist/cart/search/menu/close actions, higher-contrast secondary header/footer text, and X/icon-only close controls with accessible labels. The Home release calendar now renders through shadcn `Calendar` while preserving release markers and product links. Minicart outside-click dismissal is covered through the controlled shadcn `Sheet` overlay path. Verification passed focused `npm test -- web/src/app/App.test.tsx web/src/features/catalog/HomePage.test.tsx web/src/features/cart/MinicartShell.test.tsx` (25 tests), `npm run typecheck`, and `npm run lint`. Vite-only browser sanity at `/` confirmed the header/calendar shell renders, the minicart opens, clicking the sheet overlay closes it, and the status region announces `Minicart closed`; console errors were expected API/fav icon misses because the Express/Supabase backend was not running. `npm run format:check` remains blocked by existing formatting drift in `server/src/paypal/client.ts`, `server/src/routes/cart.ts`, `server/src/routes/paypal.ts`, `web/src/features/cart/cartModel.ts`, `web/src/features/payments/DeliveryExpressAction.tsx`, and `web/src/features/payments/PayPalSdkProviderScope.test.tsx`.
- Completed the ninth M16 shared shadcn card-migration cluster for Home and Category/PLP. `HomePage` now renders trust cards, hot-sale product cards, category pills, promo cards, selected-release details, and popular-series cards through shadcn `Card` slot composition while preserving full-tile links and existing POP MART retail styling hooks. `CategoryPage` product tiles now render through shadcn `Card`, `CardContent`, `CardHeader`, `CardTitle`, `CardDescription`, and `CardFooter`, while keeping the PDP link, sale badge, status/pickup metadata, and lazy product image behavior intact. Verification passed red/green focused `npm test -- web/src/features/catalog/HomePage.test.tsx web/src/features/catalog/CategoryPage.test.tsx` (12 tests), `npm run typecheck`, and `npm run lint`. Vite-only browser sanity confirmed Home renders 17 shadcn cards with no horizontal overflow; `/products` also had no overflow, but product-card browser verification was blocked by expected API fetch failures because the Express/Supabase backend was not running. `npm run format:check` remains blocked by existing formatting drift in `server/src/paypal/client.ts`, `server/src/routes/cart.ts`, `server/src/routes/paypal.ts`, `web/src/features/cart/cartModel.ts`, `web/src/features/payments/DeliveryExpressAction.tsx`, and `web/src/features/payments/PayPalSdkProviderScope.test.tsx`.
- Completed the tenth M16 shared shadcn card-migration cluster for PDP non-payment surfaces. `ProductDetailPage` now renders the collector story, series lineup container, lineup items, review/social-proof cards, trust badges, and recommendation tiles through shadcn `Card` slot composition while preserving detail tab semantics, purchase option behavior, Pay Later placement, the isolated `Secured by PayPal` payment frame, and full recommendation links. Verification passed red/green focused `npm test -- web/src/features/catalog/ProductDetailPage.test.tsx` (9 tests), combined catalog coverage `npm test -- web/src/features/catalog/HomePage.test.tsx web/src/features/catalog/CategoryPage.test.tsx web/src/features/catalog/ProductDetailPage.test.tsx` (21 tests), `npm run typecheck`, and `npm run lint`. Vite-only browser sanity confirmed the product route shell has no horizontal overflow, but the PDP body could not render because the Express API was not running and `localhost:3000/api/catalog/products/labubu-have-a-seat` returned connection refused; the Card-slot acceptance remains covered by component tests until an API-backed browser pass is run. `npm run format:check` remains blocked by existing formatting drift in `server/src/paypal/client.ts`, `server/src/routes/cart.ts`, `server/src/routes/paypal.ts`, `web/src/features/cart/cartModel.ts`, `web/src/features/payments/DeliveryExpressAction.tsx`, and `web/src/features/payments/PayPalSdkProviderScope.test.tsx`.
- Completed the eleventh M16 shared shadcn card-migration cluster for Checkout and Account. `CheckoutPage` now composes checkout step shells, pickup store cards, and the order summary through shadcn `Card` slots while preserving the `article` step semantics used by interaction tests, the complementary order-summary landmark, native form/radio controls, Pay Later row/message placement, and selected payment action slot. `AccountPage` now composes overview stats, profile/address/payment panels, address/payment cards, guest lookup/result surfaces, status cards, order history cards, order-detail panels, item/review panels, and totals through shadcn `Card` slots while preserving named region semantics, route links, inline delete confirmations, and review submit/edit/delete behavior. Verification passed red/green focused `npm test -- web/src/features/checkout/CheckoutPage.test.tsx web/src/features/account/AccountPage.test.tsx` (24 tests), broader `npm test -- web/src/features/checkout/CheckoutPage.test.tsx web/src/features/account/AccountPage.test.tsx web/src/app/App.interactions.test.tsx` (74 tests), `npm run typecheck`, and `npm run lint`. Vite-only browser sanity verified `/checkout` has 10 Card surfaces, 9 semantic step articles, the complementary order-summary landmark, and no horizontal overflow; `/account` has 6 Card surfaces, Profile/Address/Payments named regions, and no horizontal overflow. Browser console errors were limited to expected `localhost:3000/api/cart` failures because the Express API server was not running. `npm run format:check` remains blocked by existing formatting drift in `server/src/paypal/client.ts`, `server/src/routes/cart.ts`, `server/src/routes/paypal.ts`, `web/src/features/cart/cartModel.ts`, `web/src/features/payments/DeliveryExpressAction.tsx`, and `web/src/features/payments/PayPalSdkProviderScope.test.tsx`.
- Closed the M16 Category/PLP reference-level polish row after the shadcn foundation migration. Category now renders a compact applied-filter and sort-control strip before the official amount-free Pay Later slot and product grid, uses shadcn `Badge` for applied filters, sale labels, and status/pickup labels, uses shadcn `Separator` for the desktop control divider, keeps unsupported `series` filters hidden, and tightens desktop/tablet/mobile control density without horizontal overflow. Verification passed `npm test -- web/src/features/catalog/CategoryPage.test.tsx`, broader `npm test -- web/src/features/catalog/CategoryPage.test.tsx web/src/app/App.test.tsx`, `npm test -- web/src/app/App.interactions.test.tsx`, `npm run typecheck`, and `npm run lint`. Vite-only browser sanity verified `/products` at 1440px and 375px with the controls before Pay Later/products, no horizontal overflow, desktop sort chips on one row, mobile filter rail visible, mobile redundant current-sort/separator hidden, and a reduced mobile control height. API-backed browser verification could not run in this shell because `npm run dev:server` requires local `APP_BASE_URL`, `ADMIN_PASSCODE`, Supabase, and PayPal environment variables; Vite-only console errors were the expected `localhost:3000` API connection failures.
- Recovered Vite-only/API-down Category and generated fallback PDP continuity after Computer Use testing found `/products` could show no products and generated category card links could land on PDP not-found. The App now promotes generated fallback category merchandising on catalog API failure, filters fallback products by the active category query, and renders generated fallback PDP data for known fallback slugs when product-detail loading fails. Focused regressions cover generated fallback product cards, no stale Labubu/Skullpanda/Hirono fixture fallback, and fallback category links resolving to generated PDP records. Computer Use verified category products, product-card navigation, PDP Add to cart, minicart outside-click close, mobile menu close, sign-in dialog close, and category filter sheet apply/reset behavior.
- Continued from the Category fallback recovery into the focused UI/UX cleanups surfaced by the GUI pass. Mobile Category/PLP controls now show a compact `Filter & sort` trigger, keep only the active sort option in the page chrome, move the full filter/sort option set into the shadcn bottom Sheet, and render sheet options as 44px tap rows. Disabled wishlist now exposes `Wishlist unavailable`, a `Coming soon` reason, and a tooltip. The shared delivery express missing-cart state now renders one consolidated `Cart is refreshing before delivery express checkout.` status instead of duplicate PayPal/Pay Later copies. Verification passed focused Category/App/App-interaction tests, typecheck, lint, targeted Prettier, targeted `git diff --check`, and Playwright 375px/320px viewport checks with no horizontal overflow and 44px mobile controls. Evidence screenshot: `.playwright-mcp/paypal-retail-category-mobile-controls-320.png`.
- Continued M16 with the cart/minicart summary and pending-state hierarchy slice. Added a shared cart summary breakdown for shipping plus promo/estimated-tax status and cart subtotal, moved the full cart checkout action ahead of Pay Later/express copy on mobile, kept the minicart Checkout action primary before View cart, and made PayPal SDK config/runtime status IDs and copy method-specific (`PayPal` vs `Pay Later`) so adjacent scopes do not repeat generic unavailable text. Verification passed full `npm test` (65 files, 445 tests), `npm run typecheck`, `npm run lint`, touched-file Prettier checks, and targeted `git diff --check`; full `npm run format:check` remains blocked by existing formatting drift in `server/src/paypal/client.ts`, `server/src/routes/cart.ts`, `server/src/routes/paypal.ts`, `web/src/features/cart/cartModel.ts`, and `web/src/features/payments/DeliveryExpressAction.tsx`. Playwright GUI QA on Vite-only fallback verified populated cart/minicart at 1440px, 768px, 375px, and 320px with no horizontal overflow, cart checkout reachable at y=702 on a 320x720 viewport, minicart checkout before View cart at y=383, compact summary present, and one consolidated delivery-express pending status. Console errors were expected `localhost:3000` API/SDK-config connection failures because the Express API server was not running. Evidence screenshots: `/Users/tengtao/Development/demo-projects/cart-summary-desktop-1440.png`, `/Users/tengtao/Development/demo-projects/cart-summary-tablet-768.png`, `/Users/tengtao/Development/demo-projects/cart-summary-mobile-375-reordered.png`, `/Users/tengtao/Development/demo-projects/cart-summary-small-320-reordered.png`, and `/Users/tengtao/Development/demo-projects/minicart-small-320-reordered.png`.
- Completed the M16 mobile cart/minicart layout slice with shadcn `ScrollArea`-backed minicart items, an independently capped XS/S minicart checkout/payment panel, and mobile cart rows that convert into vertical cards with full-width quantity strips at 320px/375px. Added jsdom `ResizeObserver` coverage for shadcn scroll-area tests. Verification passed full `npm test` (65 files, 445 tests), `npm run typecheck`, `npm run lint`, focused cart/style tests, touched-file Prettier checks, and targeted `git diff --check`; full `npm run format:check` remains blocked by existing formatting drift in `server/src/paypal/client.ts`, `server/src/routes/cart.ts`, `server/src/routes/paypal.ts`, `web/src/features/cart/cartModel.ts`, and `web/src/features/payments/DeliveryExpressAction.tsx`. API-backed Playwright GUI QA ran with Vite on `localhost:5174` and Express API on `localhost:3000`: cart/minicart were populated through PDP Add to cart, no horizontal overflow appeared at 320px, 375px, 768px, or 1440px, XS/S minicart item viewport scrolled independently while the checkout panel stayed capped and scrollable, mobile cart quantity strips were full-width, and outside-click plus X-button dismissal both closed the minicart. Evidence screenshots: `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/qa-minicart-final-320.png`, `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/qa-cart-final-320.png`, `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/qa-minicart-final-375.png`, `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/qa-cart-final-375.png`, `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/qa-minicart-final-768.png`, `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/qa-cart-final-768.png`, `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/qa-minicart-final-1440.png`, and `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/qa-cart-final-1440.png`.
- Completed the M16 checkout/account form and payment-placeholder polish slice. Checkout now fills the Order Summary payment reserve with state-specific buyer copy before payment unlocks, when card fields live in the expanded payment row, or when a selected wallet is unavailable; on mobile the order summary is ordered before the long checkout form stack. Checkout address/location fields now use shadcn `FieldGroup`/`Field`/`FieldLabel`/`Input`/`Checkbox`/`Button` with visible required markers, mobile input metadata, inline descriptions, and preserved validation IDs. Account guest lookup, address add/edit, and review forms now use shadcn field/button primitives with stable label text, touch-friendly controls, `autocomplete`/`inputMode`, and unchanged submit/delete/review behavior. Verification passed `npm run typecheck`, focused `npm test -- web/src/features/checkout/CheckoutPage.test.tsx web/src/features/account/AccountPage.test.tsx` (26 tests), full `npm test` (65 files, 447 tests), `npm run lint`, touched-file Prettier check, and targeted `git diff --check`. Vite-only Playwright sanity verified `/checkout` and `/account` at 375px and 1440px with no horizontal overflow, mobile checkout summary before the long form stack, visible locked payment placeholder, one-column mobile account address fields, and two-column desktop form density; expected console errors were limited to missing backend API fetches. Evidence screenshots: `/Users/tengtao/Development/demo-projects/checkout-mobile-form-placeholder-375.png` and `/Users/tengtao/Development/demo-projects/account-address-form-mobile-375.png`. Broader checkout reference polish remains open for breadcrumb/secure header, summary thumbnails, promo controls, and trust strip.
- Completed the M16 checkout reference-level polish slice. Checkout now has a secure breadcrumb/header, shadcn-card left/right rhythm, a right-side order summary with optional active-cart thumbnails, quantity badges, line totals, auto-offer status copy, subtotal/shipping/tax/total rows, and a selected payment action slot that keeps official PayPal surfaces untouched. The bottom trust strip now uses only implemented capabilities. Active-cart summary image mapping normalizes legacy Labubu/Hirono SVG fixture paths to generated PNG assets for checkout thumbnails. Verification passed full `npm test` (65 files, 448 tests), `npm run typecheck`, `npm run lint`, focused post-format `npm test -- web/src/features/checkout/CheckoutPage.test.tsx web/src/features/checkout/CheckoutPage.interactions.test.tsx web/src/app/App.interactions.test.tsx` (85 tests), touched-file Prettier checks, targeted `git diff --check`, shadcn docs lookup for Badge/Separator/Card, and Playwright GUI checks on `/checkout` at 1440px and 375px with no horizontal overflow, secure copy, offer panel, trust strip, and no framework overlay. Interaction proof covered Delivery/Pickup tab switching and the shipping submit path; Vite-only submit reached the existing visible blocked/error state because the Express backend was unavailable. Expected console noise was limited to `/favicon.ico` and `localhost:3000/api/cart` connection failures. Evidence screenshots: `/Users/tengtao/Development/demo-projects/checkout-reference-polish-desktop-1440.png`, `/Users/tengtao/Development/demo-projects/checkout-reference-polish-mobile-375.png`, and `/Users/tengtao/Development/demo-projects/checkout-reference-polish-mobile-submit-error-375.png`.

## 2026-06-23

- Completed the M16 captured Express Review/order-confirmation reference-level polish slice. Captured express orders now switch from the Review and Confirm layout to a buyer-safe `Thank you!` confirmation hero, receipt/order-number card, View Order and Continue Shopping actions, an order-details grid, preserved delivery/items/final-snapshot sections, optional real-data recommendations, and the guest save-order prompt without leaving a dominant disabled `Confirm and pay` action. The buyer-facing captured state hides the technical PayPal order ID while still showing the capture ID only in the capture receipt block. Browser QA first exposed a temporary mock API binding mismatch (`localhost` versus `127.0.0.1`) and then a confirmation CTA contrast issue; both were corrected before closing the slice. Final verification passed full `npm test` (65 files, 450 tests), `npm run typecheck`, `npm run lint`, focused post-format `npm test -- web/src/features/checkout/ExpressReviewPage.test.tsx web/src/app/App.interactions.test.tsx` (59 tests), touched-file Prettier checks, targeted `git diff --check`, and a refreshed Graphify index. In-app browser QA used a temporary localhost mock API and verified the express-review capture flow at 1440px and 375px with no console warnings/errors after rerun start, no Vite overlay, no horizontal overflow, top-aligned confirmation view (`scrollY: 0`), no stale `Confirm and pay`, no buyer-visible `PAYPAL_ORDER_EXPRESS`, and working buyer actions. Evidence screenshots: `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/express-review-confirmation-desktop-1440.png` and `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/express-review-confirmation-mobile-375.png`.
- Completed the M16 Account/sign-up reference-level polish slice. The new-account auth modal now uses shadcn `Dialog`, `Tabs`, `Field`, `Input`, `Checkbox`, and `Button` primitives; desktop shows a split POP MART product-art/benefits plus form composition, while mobile stacks the form first and hides nonessential art to protect first-view density. Registration now requires visible terms acceptance before submit, has an explicit reversible password visibility control, and renders Google/Apple alternatives as disabled unavailable buttons so the demo does not imply OAuth support. Browser QA also surfaced a Home release-calendar mobile overflow behind the modal; the shadcn `Calendar` month grid now collapses to seven `minmax(0, 1fr)` columns at mobile widths. Verification passed red/green `npm test -- web/src/features/account/AuthModalShell.test.tsx web/src/app/App.interactions.test.tsx web/src/styles/global.test.ts` (69 tests), `npm run typecheck`, `npm run lint`, and touched-file Prettier checks. Playwright GUI QA on Vite verified the account sign-up modal at 1440px and 375px with tabs, terms, disabled social buttons, password reveal, terms-gate error, and no horizontal overflow; expected console noise was limited to unrelated `localhost:3000` API connection failures because the Express API server was not running and account lookup was route-stubbed for the visual pass. Evidence screenshots: `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/account-signup-reference-desktop-1440.png` and `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/account-signup-reference-mobile-375.png`.
- Closed the supported M16 PDP detailed-reference row with the missing vendor/chip/shadcn Button and mobile-density evidence. API-loaded PDPs now map `vendorName` from the active storefront profile display name, fallback POP MART PDPs show `By POP MART`, status/vendor/review metadata is separated from the truthful series/category chip row, and the main Add to cart action composes through shadcn `Button` while official PayPal/Pay Later surfaces remain SDK-rendered inside the secured frame. Mobile PDP CSS now caps the gallery at `min(68vw, 260px)`, reduces thumbnail height, uses a two-column purchase-option chooser, and hides secondary option helper copy so the primary action is reached before lower story/review/recommendation content. Verification passed `npm test -- web/src/styles/global.test.ts`, broader `npm test -- web/src/features/catalog/ProductDetailPage.test.tsx web/src/app/App.interactions.test.tsx web/src/styles/global.test.ts` (74 tests), and live browser QA at 1440px, 375px, and 320px with no horizontal overflow and `hasShadcnButton`/`hasVendor`/`hasChipRow`/`hasPayPalFrame` all true. Evidence screenshots: `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/pdp-vendor-chip-desktop-1440.png`, `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/pdp-vendor-chip-mobile-375.png`, and `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/pdp-vendor-chip-mobile-320.png`. Optional future PDP polish remains a reserved sticky mobile purchase bar, while final richer product media assets stay open in their separate tracking row.
- Closed the M16 Pickup store-ticket card slice. Checkout inline Store selection and store-picker modal rows now use compact store-ticket surfaces with shadcn `Card` composition, address, phone, distance/status badge, available/unavailable count blocks, full/partial `data-inventory-state`, and partial-inventory notes when unavailable quantities remain in cart. Verification passed `npm test -- web/src/features/checkout/CheckoutPage.test.tsx web/src/features/checkout/CheckoutPage.interactions.test.tsx` (32 tests), `npm run typecheck`, `npm run lint`, touched-file Prettier check, targeted `git diff --check`, and refreshed Graphify. In-app browser QA used Vite and the Express API on `localhost` to match CORS, seeded a live cart through PDP Add to cart, verified the Pickup store lookup with 9 inline tickets and 9 selectable modal tickets, full and partial inventory states, address/phone/distance on every ticket, no framework overlay, and no horizontal overflow at 1440px and 375px. Early browser attempts on `127.0.0.1` failed because the API allows the configured `localhost:5173` origin; keep checkout browser QA on the configured app origin unless CORS is updated. Evidence screenshots: `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/checkout-pickup-store-ticket-desktop-1440.png` and `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/checkout-pickup-store-ticket-mobile-375.png`.
- Completed the focused M16 minicart drawer-separation slice. The local shadcn Sheet wrapper now allows per-sheet overlay styling, and the minicart passes a dedicated overlay class plus `data-visual-separation="minicart-drawer"` so the drawer owns a darker low-noise backdrop, fixed right-edge desktop anchoring, full-width mobile behavior, stronger panel elevation, and one restrained brand accent rail while leaving official PayPal frame/button/message surfaces untouched. Browser QA first caught a regression where the new accent rail CSS accidentally changed the fixed sheet to `position: relative`, causing the right drawer to render at the left edge; removing that override restored fixed right anchoring. Verification passed focused Minicart/CSS tests, targeted Prettier, desktop/mobile in-app browser geometry checks, and outside-click backdrop dismissal. Vite-only browser console errors were limited to expected absent-backend product/cart fetch failures while generated fallback data kept the route usable. Evidence screenshots: `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/minicart-separation-desktop-1440.png` and `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/minicart-separation-mobile-375.png`.
- Closed the M16 cart/minicart/checkout merchant-owned accent slice. Cart now marks the page and summary with visual-accent attributes and uses warm hero/summary panels, thin rails, focus-within item outlines, and a text-backed item-count summary chip. Checkout now marks its page, step cards, order summary, and trust strip with visual-accent attributes and uses warm hero/summary panels, step-state rails, and restrained trust-strip accents. The CSS regression test proves these accents stay off the cart PayPal frame and checkout payment slot. Verification passed focused Cart/Checkout/style tests and targeted Prettier. In-app browser QA verified `/cart` and `/checkout` at 1440px and 375px with the accent markers/styles present, no framework overlay, and no horizontal overflow; Vite-only console errors were limited to expected `Cart restore failed` backend fetches. A follow-up API-backed populated-cart pass could not run because `npm run dev:server` requires missing `APP_BASE_URL`, `ADMIN_PASSCODE`, Supabase, and PayPal environment variables in this shell, so the separate PayPal SDK layout-shift and full sandbox capture rows remain open. Evidence screenshots: `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/cart-checkout-accent-cart-desktop-1440.png`, `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/cart-checkout-accent-cart-mobile-375.png`, `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/cart-checkout-accent-checkout-desktop-1440.png`, and `/Users/tengtao/Development/demo-projects/demos/paypal-retail-demo/cart-checkout-accent-checkout-mobile-375.png`.
- Closed the M16 Vite-only responsive gate for buyer pages. In-app Browser sweep covered Home, Category, PDP, Cart, open Minicart, Checkout, Express Review/order confirmation, and Account at 320px, 375px, 414px, 768px, 1024px, and 1440px. Mobile target sizing was tightened for header discovery, shared buttons/shadcn primitives, calendar arrows, breadcrumbs/footer links, filter chips, checkout/account inputs, product-detail tabs, checkout tabs, release detail links, and minicart X close while preserving official PayPal surfaces. Final full sweep found no blank pages, no framework overlay, and no horizontal overflow; targeted rerun left only native PDP radio glyph scanner notes inside 124px by 190px clickable purchase-option labels. Verification passed focused buyer-surface tests (`npm test -- web/src/styles/global.test.ts web/src/features/catalog/HomePage.test.tsx web/src/features/catalog/CategoryPage.test.tsx web/src/features/catalog/ProductDetailPage.test.tsx web/src/features/cart/MinicartShell.test.tsx web/src/features/cart/CartPage.test.tsx web/src/features/checkout/CheckoutPage.test.tsx web/src/features/checkout/ExpressReviewPage.test.tsx web/src/features/account/AuthModalShell.test.tsx web/src/features/account/AccountPage.test.tsx`, 10 files, 81 tests), `npm run typecheck`, `npm run lint`, touched-file Prettier check, targeted `git diff --check`, and refreshed Graphify. Evidence directories: `/private/tmp/paypal-retail-responsive-gate-20260623-final` and `/private/tmp/paypal-retail-responsive-gate-20260623-targeted`. Console errors were expected Vite-only missing Express API fetches; PayPal SDK layout-shift and full sandbox capture rows remain open until API/env-backed verification.
- Closed the M16 API-backed PayPal SDK layout-shift verification row. With Vite on `localhost:5173` and the Express API on `localhost:3000`, Browser QA verified live Category, PDP, Cart, open Minicart, and unlocked Checkout payment surfaces at 375px. Category renders an amount-free official `paypal-message`; PDP renders live product data, generated PNG imagery, amount-aware official Pay Later message, and an official `paypal-button`; Cart and Minicart render official Pay Later messages plus visible PayPal buttons inside the shared `Secured by PayPal` frame; Checkout advances through shipping, billing, and shipping option into the payment step with PayPal selected, official PayPal button mounted, and Pay Later message shown for the recalculated `$75.63` total. No horizontal overflow or framework overlay appeared on these surfaces. Cart PayPal click opened the PayPal sandbox login modal and server logs recorded sandbox create-order success with `PAYER_ACTION_REQUIRED`; no sandbox buyer credentials were entered and capture remains open. Evidence screenshots: `/private/tmp/paypal-retail-api-backed-payment-gate-20260623/pdp-mobile-long-375.png`, `/private/tmp/paypal-retail-api-backed-payment-gate-20260623/cart-payment-area-375.png`, `/private/tmp/paypal-retail-api-backed-payment-gate-20260623/cart-after-paypal-coordinate-click-375.png`, `/private/tmp/paypal-retail-api-backed-payment-gate-20260623/checkout-payment-unlocked-after-submit-375.png`, `/private/tmp/paypal-retail-api-backed-payment-gate-20260623/category-paylater-375.png`, and `/private/tmp/paypal-retail-api-backed-payment-gate-20260623/minicart-payment-375.png`.
- Closed the remaining M16 buyer-page accessibility/visual tracker gates that were already covered by implementation evidence, then refreshed the API-backed mobile browser pass after fixing the one real rendered issue it surfaced. The mobile header cart badge now stays inside the 44px icon button touch target, and API PDP image alt text is normalized when live data still returns generic strings like `view 1`. Browser QA at 375px verified Home, Category, PDP, Cart, and open Minicart with no horizontal overflow, no visible clipping, no bad image alt text, and no sticky header overlap; checkout top view also has no sticky-header overlap, while the earlier checkout payment-step evidence keeps the sticky action visible without footer overlap. The tracker alignment now closes stale M16 rows for shared shadcn migration, generated hierarchy, global mobile gate, cart/minicart/checkout polish, POP MART visual control, meaningful image alt text, sticky overlap, and text-fit. Verification passed focused M16 coverage (`npm test -- web/src/features/checkout/CheckoutPage.test.tsx web/src/styles/global.test.ts web/src/app/App.interactions.test.tsx`, 86 tests), `npm run typecheck`, `npm run lint`, touched-file Prettier checks, targeted `git diff --check`, and refreshed Graphify. Evidence artifacts: `/private/tmp/paypal-retail-m16-a11y-visual-gate-20260623/postfix-mobile-audit-results.json`, `/private/tmp/paypal-retail-m16-a11y-visual-gate-20260623/postfix-home-mobile-375.png`, `/private/tmp/paypal-retail-m16-a11y-visual-gate-20260623/postfix-category-mobile-375.png`, `/private/tmp/paypal-retail-m16-a11y-visual-gate-20260623/postfix-pdp-mobile-375.png`, `/private/tmp/paypal-retail-m16-a11y-visual-gate-20260623/postfix-cart-mobile-375.png`, `/private/tmp/paypal-retail-m16-a11y-visual-gate-20260623/postfix-minicart-mobile-375.png`, and `/private/tmp/paypal-retail-m16-a11y-visual-gate-20260623/postfix-checkout-top-mobile-375.png`.
- Closed the M16 closeout/runbook alignment slice. Formatting drift in the touched PayPal/cart files was normalized so full `npm run verify` now passes again (`65` files, `455` tests, plus typecheck, lint, and Prettier format check). Updated `DEMO.md`, `README.md`, `PLAN.md`, the M16 reference-polish guide, checklist rows, todos, test cases, and reusable learnings so the current stage is consistent: buyer-facing POP MART polish, shadcn migration, responsive QA, API-backed PayPal SDK render checks, and accessibility/visual gates are complete for the supported scope, while full sandbox buyer approval/capture, final gallery/media plus LQIP workflow, generic MochiToy asset safety, local Supabase Docker verification, and M15 Admin Portal remain open.
- Re-ran the full cart PayPal sandbox approval/capture gate on 2026-06-24 with Vite on `localhost:5173` and the API on `localhost:3000`. The cart contained live API-backed items, the official PayPal button refreshed the cart, created and recorded PayPal order `6ET75592D0296261D` for merchant order `DO-20260624-000001`, and opened the PayPal sandbox login modal with `PAYER_ACTION_REQUIRED`; browser console output had no app errors beyond the existing missing favicon noise. The gate remains open because the Playwright browser context had no sandbox buyer session and no credentials were entered. Evidence screenshot: `/Users/tengtao/Development/demo-projects/paypal-retail-sandbox-capture-20260624-login-blocked.png`.
- Completed the full cart PayPal sandbox approval/capture gate on 2026-06-24 after sandbox buyer credentials were provided. Systematic debugging first found that the approved local express order had a valid payment session and matched amount, but no `paypal_shipping_update` total snapshot because local/no-callback express mode cannot receive PayPal shipping callbacks. The express-review loader now prefers the same-session `paypal_shipping_update` snapshot and falls back to the same-session `review_confirm` snapshot when no callback snapshot exists, while the frontend replaces stale sample review data with a blocked unavailable state on real load failures. Live browser QA reloaded the approved return for PayPal order `5YR26262S4472494N`, verified real order `DO-20260624-000001`, both items, `$69.68` total, amount verified, no stale `DO-20260607-000123`, clicked `Confirm and pay`, and reached the `Thank you!` captured confirmation with no stale confirm action. Supabase verification confirmed order status `paid`, payment session status `captured`, a present PayPal capture ID, matched merchant/provider total, a `capture` total snapshot, and an empty source cart. Cart UI verification showed `Cart (0)`, `0 items`, and no captured item rows. Focused verification passed `npm test -- server/tests/paypalOrderRepository.test.ts web/src/app/App.interactions.test.tsx` (72 tests). Evidence screenshots: `/Users/tengtao/Development/demo-projects/paypal-sandbox-express-review-before-capture.png`, `/Users/tengtao/Development/demo-projects/paypal-sandbox-capture-confirmation.png`, and `/Users/tengtao/Development/demo-projects/paypal-sandbox-post-capture-empty-cart.png`.
- Continued the delivery-checkout PayPal capture slice with TDD. PayPal and Pay Later standalone checkout actions now report approved PayPal order/session context into App, and App loads the pending review snapshot, posts backend capture, renders the confirmation state, closes minicart, and reloads the cart after capture. Focused verification passed `npm run test -- web/src/features/payments/paymentActionFailure.test.tsx`, `npm run test -- web/src/app/App.checkout-paypal-capture.test.tsx`, and `npm run typecheck -- --pretty false`. Live browser QA reached the official PayPal sandbox modal from checkout, created PayPal order `14F26791XC702383N` with payment session `69630706-f257-4b55-8950-3bf2c0d875ad`, and reached the PayPal approval screen, but PayPal sandbox returned 401 before SDK `onApprove`, so final delivery-checkout live capture stayed open until the later closure row below. The same pass found a separate PDP cart-continuity regression: PDP Add to cart can populate client minicart state while checkout refresh restores an empty server cart unless a cart binding is manually seeded. Evidence screenshot: `/Users/tengtao/Development/demo-projects/checkout-paypal-sandbox-401-modal.png`.
- Closed the PDP Add to cart server-cart persistence regression with TDD and rendered browser QA. Product detail API IDs now survive into `ProductDetailPageData`, local optimistic cart rows keep `productId` when available, and PDP Add to cart posts to `/api/cart/items` with the active cart/auth request options before reconciling the returned server cart and cart binding. The new App interaction regression proves API-loaded PDP Add to cart posts `product_id`, stores the binding, and keeps the item visible in checkout after `/api/cart/refresh`. Verification passed the red/green focused test, broader `npm run test -- web/src/app/App.interactions.test.tsx web/src/features/cart/cartModel.test.ts web/src/features/catalog/ProductDetailPage.test.tsx` (71 tests), `npm run typecheck -- --pretty false`, `npm run lint`, and targeted `git diff --check`. In-app Browser QA on `http://localhost:5173/products/blind-boxes-2?qa=pdp-cart-persist` verified no framework overlay, no console errors, Add to cart opened the minicart, minicart Checkout navigated to `/checkout`, and the checkout Order Summary still contained `Molly Blind Boxes 2` after the server cart refresh. Evidence screenshot: `/private/tmp/paypal-retail-pdp-cart-persist-checkout.png`.
- Closed the delivery-checkout PayPal sandbox approval/capture gate after the PDP server-cart persistence fix. Playwright GUI verified PDP Add to cart, minicart Checkout, Delivery checkout shipping/billing/shipping-option submits, official Pay Later message update for the recalculated checkout total, PayPal sandbox login and approval, app return to `/checkout/express-review`, backend capture, confirmation rendering, and cart clearing. Evidence: create response `DO-20260624-000004` / PayPal order `6GK84058A01890038` / payment session `66f7e2bd-87bc-43b8-9398-d5a9e6664f0b`; express-review amount guard matched; capture response `5VE76310HS283401C` with PayPal order/capture status `COMPLETED`; browser network `POST /api/paypal/orders/6GK84058A01890038/capture` returned 200; screenshot `paypal-retail-delivery-paypal-capture-confirmation.png`; console file `paypal-retail-delivery-paypal-capture-console.log` contained only PayPal sandbox-side autofocusing/preload warnings.
- Fixed the PDP lower-detail tab interaction regression found from browser comment QA. The controlled shadcn/Radix tabs now hide force-mounted inactive panels with explicit `hidden`/`aria-hidden` state, preserve Radix `aria-controls` panel linkage, and use a lighter horizontal line-tab treatment instead of a heavy active pill. Verification passed focused `npm run test -- web/src/features/catalog/ProductDetailPage.test.tsx web/src/styles/global.test.ts` (24 tests), `npm run typecheck -- --pretty false`, `npm run lint`, touched-file Prettier check, targeted `git diff --check`, and Playwright GUI on `http://localhost:5173/products/blind-boxes-2?qa=pdp-tabs-fix` confirmed Collector, Q&A, and Product facts switch with only the active panel visible and no console warnings/errors.
- Follow-up Product facts browser-comment QA hardened the exact `#product-detail-blind-boxes-2-facts-tab` target: each detail trigger now has an explicit click handler in addition to Radix `onValueChange`, the selected trigger has a stronger active underline/inset accent, and the active panel heading gets a small accent marker. Playwright GUI on `http://localhost:5173/products/blind-boxes-2?qa=delivery-paypal-capture` confirmed Product facts becomes `aria-selected="true"`, Collector/Q&A become inactive, the Product facts panel is visible with heading `Product facts`, and console warnings/errors stayed at zero.
- Continued M15 Admin Portal with the profile/market global controls slice. The unlocked admin shell now shows a compact shadcn Card-based ops surface with active runtime context, native profile/market selectors for seeded demo rows, signed `PATCH /api/admin/profile-market` submission, returned-context display, and buyer UI remaining unlinked from admin navigation. Focused verification passed `npm test -- web/src/app/App.interactions.test.tsx -t "App admin interactions"` (5 admin tests) and `npm run typecheck`.
- Continued M15 Admin Portal with the order list/detail/lifecycle slice. Added signed Admin APIs for `GET /api/admin/orders`, `GET /api/admin/orders/:id`, and `POST /api/admin/orders/:id/lifecycle`; the route reuses shared order-transition planning, returns allowed next statuses, and writes admin lifecycle timeline events. The unlocked admin shell now loads recent orders with `x-admin-session`, opens compact order detail with totals/items/timeline, and posts one-step lifecycle actions back to the API. Focused verification passed `npm test -- server/tests/adminRoutes.test.ts` (10 tests) and `npm test -- web/src/app/App.interactions.test.tsx -t "App admin interactions"` (6 admin tests).
- Continued M15 Admin Portal with the enriched Admin order-detail sections. `GET /api/admin/orders/:id` now includes payment sessions, sanitized PayPal snapshots, total snapshots, promo evaluation lines, inventory effects derived from order item quantities, and linked webhook metadata in addition to items/totals/timeline. The admin shell renders these as compact debug sections under the order timeline. Focused coverage was added in `server/tests/adminRoutes.test.ts` and `web/src/app/App.interactions.test.tsx`; final verification for this slice is recorded with the next focused test run.
- Closed the checkout polish follow-up from the 2026-06-25 UX review. Checkout shipping and billing submits now collapse their sections immediately into concise summaries while the backend draft save/recalculation continues, use icon-only pencil edit affordances, remove visible `Saved`/`Editing`/`Saving`/`Recalculating` chips, keep same-as-shipping checked by default with a visible shadcn checkbox checkmark, and hide inactive Order Summary payment placeholder copy until payment is actionable. Selected desktop/tablet PayPal and Pay Later actions now render as official provider surfaces directly in Order Summary without the previous blue wrapper/selected-heading chrome; Pay Later renders its official message below the selected action. On mobile, the sticky payment bar replaces the generic continue action with the selected official non-card payment surface and keeps Pay Later message space below it, while card payment stays inside the expanded payment row with compact 50px hosted-field containers. API-backed local browser verification with Vite on `127.0.0.1:5173` and Express on `localhost:3000` measured shipping summary collapse at about `38ms` and billing summary collapse at about `30ms`; the next-section unlock still waits on backend reconciliation (`~4.3s` shipping, `~2.3s` billing), so the remaining perceived-latency work is backend/recalculation optimization rather than keeping the form open. Verification passed full `npm run verify` (`66` test files, `497` tests, typecheck, lint, and format check), Computer Use Chrome inspection, and Playwright desktop/mobile checks for PayPal/Pay Later/card placement. Console noise during Pay Later checks was PayPal sandbox presentment-message `500` responses, not merchant-app exceptions.
- Fixed the checkout selected Pay Later blank-message fallback gap found after the polish slice. `PayLaterAmountMessage` now uses the SDK v6 manual message path (`handleFetchContent` plus `paypal-message.setContent`) so the official PayPal message content is explicitly applied under the selected Pay Later button; if the sandbox presentment request or content application fails, the slot shows buyer-safe fallback copy and logs structured placement/amount/country evidence instead of rendering blank. Updated the M12 implementation note, sandbox/UI test-case rows, debug notes, and todos to keep the stage aligned. Focused verification passed `npm test -- web/src/features/payments/paymentActionFailure.test.tsx web/src/features/payments/PayLaterStandaloneAction.test.tsx` (16 tests) and `npm run typecheck -- --pretty false`.
- Refined the checkout delivery/pickup UX from the 2026-06-25 GUI review with `ui-ux-pro-max` guidance. Pay Later radio rows now stay compact with PayPal SDK logo assets from the local wiki snapshot, selected Pay Later messages show a buyer-safe fallback if the official message times out or renders empty, and checkout promo copy no longer parks the buyer on indefinite `Promo calculating` text when no backend discount/code is present. The checkout repository now returns product names and per-store `inventory_lines`, so Pickup store cards can show each cart item as `In stock`, `Only N available`, or `Sold out`; the store picker modal adds per-card Select buttons and a stronger backdrop so the background list does not visually compete. Pickup date selection now uses the shadcn `Calendar` component, stale seeded date windows roll forward from the current checkout date, and the default selected date is included in submit payloads. Footer headings are readable on navy, and minicart actions stack vertically so View cart no longer appears as stray text. Verification passed focused checkout/cart/payment/repository tests (`66` tests), `npm run format:check`, `npm run lint`, and `npm run typecheck -- --pretty false`; Playwright GUI verified delivery payment Pay Later fallback, minicart stacked actions, pickup store modal item-level inventory, pickup Calendar on June 25, 2026, pickup-date PATCH body `{ "pickup_date": "2026-06-25" }`, and a clean pickup-tab console after final submit. Evidence screenshots: `checkout-delivery-paylater-fallback-fixed.png`, `checkout-minicart-actions-fixed.png`, `checkout-pickup-store-modal-fixed.png`, `checkout-pickup-calendar-normalized.png`, and `checkout-pickup-payment-open-fixed.png`.
- Closed the live mini-cart primary-action contrast follow-up from hosted Render/Safari QA. The `Checkout` link was present and navigable but visually blank because the shadcn Sheet portal could not rely on shell-scoped button paint, while only `View cart` had explicit fallback styling. Added portal-safe primary mini-cart action colors and a stylesheet contract test covering both primary and secondary actions. Verification passed the red/green `npm run test -- web/src/styles/global.test.ts`, focused `npm run test -- web/src/features/cart/MinicartShell.test.tsx web/src/styles/global.test.ts`, `npm run typecheck`, `npm run lint`, `npm run format:check`, `git diff --check`, and `npm run build`.

## 2026-06-26

- Closed the hosted/live storefront polish regression slice from GUI testing and `ui-ux-pro-max` review. Live Render probing showed Category, PDP, Cart, and open Minicart already mounted visible official Pay Later message elements, while Home only had generic PayPal/Pay Later copy; Home now renders an official amount-free Pay Later promo slot through the shared storefront PayPal SDK message wrapper. The PDP lower-detail shadcn tabs were still behaving like a max-content `w-fit` tablist on 390px/320px mobile, which clipped the rail and exposed a vertical-scrollbar artifact; the local PDP tabs root/list are now width-constrained to the card, horizontally scroll within the rail, and hide vertical overflow. PDP review/social-proof cards now expose stable `data-review-card` markers so the Customer reviews tab is testable and discoverable. Home release calendar content now sits below the sticky header stacking layer, the hot-sales board no longer stretches to the release-calendar height, product pending/loading routes use shadcn `Skeleton` blocks, and checkout payment-logo SVGs remain visible without duplicating radio accessible names. Verification passed full `npm run verify` (`66` files, `506` tests, typecheck, lint, and format check), `npm run build`, and `git diff --check`. Playwright GUI verified Home at 390px has header z-index `40`, calendar z-index `0`, no horizontal overflow, and the new Home Pay Later promo slot; PDP at 390px and 320px has a card-width tab rail with horizontal scrolling and no visible vertical scrollbar, plus two discoverable review cards.
- Ran hosted Render checkout wallet/card GUI verification on `https://retail-demo.onrender.com/checkout?qa=wallet-card-qa`. Delivery checkout reached Payment after submitting shipping (`~1.0s` click path), billing (`~0.4s`), and shipping option (`~0.5s`) with no console warnings/errors; all six payment rows rendered at a stable `52px` height. Pickup checkout reached Payment through ZIP `19720`, the store-picker modal, selected Austin Domain, billing, and the default current-date pickup calendar; background store tickets were suppressed outside the modal, and the pickup summary correctly paid only the 1 available cart unit while keeping 4 units unavailable in the original cart. Card Fields rendered in both Delivery and Pickup with provider status ready, three mounted `paypal-hosted-card-field` elements, stable `50px` containers, and the Pay by card action inside the card box. Venmo rendered in both flows with a mounted `venmo-button`. Apple Pay and Google Pay did not become capturable in this browser: Apple Pay stayed eligibility-pending with no button, and Google Pay stayed runtime-pending/disabled because the Google Payments client/runtime was unavailable. Capture remains open for approved sandbox card data and wallet-eligible browser/device coverage.
- Closed the Homepage Reference Polish V2 implementation slice from the Sakura reference comparison and `ui-ux-pro-max` guidance. Home now uses a full-bleed image-overlay hero, Lucide/shadcn trust cards, denser release-calendar plus pre-order board, shadcn Badge product labels, small product action chips, arrowed category cards, image-backed promo/event cards, and compact series badges/rail while preserving supported POP MART data and official PayPal message placement. A medium desktop header breakpoint now collapses buyer action labels to icon buttons between 901px and 1280px so the discovery pill and actions do not overlap before the mobile header takes over. Updated `DESIGN.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` with the V2 homepage contract and acceptance row. Verification passed focused `npm run test -- web/src/features/catalog/HomePage.test.tsx web/src/styles/global.test.ts` (23 tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, `npm run build`, and `git diff --check`. In-app browser QA on `http://127.0.0.1:5174/?qa=homepage-v2-header-fixed` verified the mid-width header fix at the browser's 1280px viewport (`discovery.right=1073`, `actions.left=1089`, `overlap=false`, `horizontalOverflow=false`, header z-index `40`). Evidence files: `/private/tmp/paypal-retail-homepage-v2-evidence/homepage-v2-header-fixed-1280.png` and `/private/tmp/paypal-retail-homepage-v2-evidence/homepage-v2-header-fixed-metrics.json`. The Playwright MCP resize path did not provide reliable resized DOM metrics for 390px/320px in this session, so mobile behavior is covered by the stylesheet contracts/focused tests here and should be re-swept on hosted Render after deploy.
- Follow-up hosted homepage GUI QA on Render verified the deployed Homepage Reference Polish V2 selectors and caught a 1440px desktop header issue: the full Sign in/Wishlist/Cart action buttons needed roughly 452px but the desktop action grid column resolved to about 348px, so the first action could visually intrude into the discovery pill. Fixed the base desktop header grid to reserve a 460px action column while preserving the existing 901px-1280px icon-only breakpoint. Local browser evidence at 1440px now shows `discovery.right=844.5`, first action `left=875.46`, `overlap=false`, and `horizontalOverflow=false`; screenshot: `/private/tmp/retail-demo-homepage-render-gui/local-header-fix-1440.png`. Verification passed focused homepage/style tests, typecheck, lint, format check, build, and `git diff --check`.
- Verified the deployed `b88c8ad` homepage on Render after the header follow-up. Live `index-CKJWnwwK.css` includes the 460px desktop action column, medium icon-only breakpoint, homepage trust/icon/promo selectors, and mobile rail rules. Hosted GUI metrics passed at 1440x900, 1280x800, 1024x768, 768x1024, 390x844, and 320x700: no page-level horizontal overflow, no two-dimensional header/discovery/action collision, header z-index `40`, release calendar z-index `0`, hero image/title present, and mobile keeps the release/product board before the trust/utility strip. Home Pay Later message visual QA at 390px shows the official `paypal-message` shadow DOM text `Buy now, pay later with . Learn more` in a 343px x 28px slot with no fallback copy. Evidence screenshots and metrics are under `/private/tmp/retail-demo-b88c8ad-homepage-gui/`, including `xl-1440x900.png`, `xs-390x844.png`, `xxs-320x700.png`, and `home-paylater-390.png`. One low-priority visual note remains: some loaded category/media crops can look beige or low-information in the compact mobile category rail, but the sources are present and loaded, so this belongs with the later media-quality/LQIP workflow rather than a broken-image fix.

## 2026-06-27

- Ran a hosted Render storefront regression sweep after the homepage deployment across Home, Category, PDP, Cart, Checkout, PDP detail-tab interaction, and open Minicart. Render evidence confirmed no page-level horizontal overflow at the active browser viewport, official Pay Later messages mounted and rendered on Home, Category, PDP, Cart, and open Minicart, the Minicart `View cart` and `Checkout` actions are visibly styled, and outside-click closes the Minicart. The same sweep found the PDP shadcn detail tab rail still had a compact-list height conflict: tab clicks worked, but the tab list reported `scrollHeight` larger than its visible row, matching the earlier internal scrollbar artifact. Fixed the PDP tab rail by overriding the shadcn compact `h-8` list height, setting a stable 55px tablist row with 44px triggers, and keeping horizontal-only tab scrolling. Focused verification passed `npm test -- web/src/styles/global.test.ts` and `npm run build:web`; local browser verification at `http://127.0.0.1:5176/products/blind-boxes-2?qa=pdp-tabs-height-local` measured `clientHeight=54`, `scrollHeight=54`, 44px triggers, no horizontal page overflow, and successful Customer reviews tab activation. Evidence files: `/private/tmp/paypal-retail-post-homepage-regression/metrics.json`, `/private/tmp/paypal-retail-post-homepage-regression/pdp.png`, and `/private/tmp/paypal-retail-post-homepage-regression/minicart-open.png`.

## 2026-06-28

- Verified the deployed `f60113f` PDP tab-rail fix on Render. Hosted CSS now serves `index-DNCUVoEx.css` with `.product-detail-tabs__nav` `min-block-size:55px`, `block-size:auto!important`, and trigger `height:44px!important`. Browser verification on `https://retail-demo.onrender.com/products/blind-boxes-2?qa=f60113f-*` passed at the default 1280px viewport and at 390x844 mobile: tablist `clientHeight=54`, `scrollHeight=54`, horizontal-only rail overflow on mobile, no page-level horizontal overflow, one official Pay Later message, and Customer reviews tab activation with the review heading visible. Evidence files: `/private/tmp/paypal-retail-f60113f-render-qa/metrics.json`, `/private/tmp/paypal-retail-f60113f-render-qa/desktop-1280.png`, and `/private/tmp/paypal-retail-f60113f-render-qa/mobile-390.png`.
- Closed the checkout draft recalculation latency follow-up from the shipping/billing submit review. `buildDraftResponse` now reuses cart rows and eligible delivery shipping options already loaded by the shipping/billing update path, computes delivery and pickup response payloads in parallel, and avoids re-reading cart rows during tax calculation. Repository regressions prove shipping-address and billing-address recalculation each call cart/shipping reads once. Verification passed `npm test -- server/tests/checkoutRepository.test.ts server/tests/checkoutRoutes.test.ts web/src/features/checkout/CheckoutPage.interactions.test.tsx web/src/app/App.interactions.test.tsx` (`97` tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, and `git diff --check`.
- Closed Homepage Reference Polish V3 after the search/typography review. The header discovery pill is now a real shadcn-backed search form with a labelled searchbox, visible focus-within state, keyword submission to `/products?q=...`, explicit Enter-key handling, and live/fallback catalog keyword filtering. The POP MART shell type scale is lighter in the utility/header/nav controls and the homepage hero heading is smaller with a more open line height, reducing the oversized/heavy feel called out in review. Full verification passed `npm run verify` (`66` files, `511` tests, typecheck, lint, and format check), `npm run build`, and `git diff --check`. In-app browser GUI QA on local Vite at 1440x900 and 390x844 verified click focus keeps the route unchanged, typing works, Enter submits to `/products?q=molly`, `All products` renders, the search form has no horizontal overflow, mobile hides the long brand text, and computed homepage typography is lighter (`utility=700`, brand/nav `800`, search input `600`, desktop hero `59.04px/61.4px`, mobile hero `39px/40.56px`). Evidence files: `/private/tmp/paypal-retail-homepage-v3-search-qa/desktop-1440-search-results.png`, `/private/tmp/paypal-retail-homepage-v3-search-qa/mobile-390-search-results.png`, `/private/tmp/paypal-retail-homepage-v3-search-qa/desktop-1440-home-typography.png`, `/private/tmp/paypal-retail-homepage-v3-search-qa/mobile-390-home-typography.png`, `/private/tmp/paypal-retail-homepage-v3-search-qa/metrics.json`, and `/private/tmp/paypal-retail-homepage-v3-search-qa/typography-metrics.json`.
- Verified deployed commit `4bbd2bc` on Render after pushing Homepage Reference Polish V3. Hosted `https://retail-demo.onrender.com` serves the new `form.site-header__discovery` with `input[type=search][name=q]`, no old `a.site-header__discovery`, and script asset `index-DdtRwVJv.js`. In-app browser QA at 1440x900 and 390x844 verified click focus keeps the route unchanged, typing works, Enter submits to `/products?q=molly`, `All products` renders, mobile hides the long brand text, and there is no horizontal overflow. Evidence files: `/private/tmp/paypal-retail-homepage-v3-render-qa/desktop-1440-search-results.png`, `/private/tmp/paypal-retail-homepage-v3-render-qa/mobile-390-search-results.png`, and `/private/tmp/paypal-retail-homepage-v3-render-qa/metrics.json`.
- Locked the Homepage Reference Polish V4 planning contract before implementation to avoid the V2/V3 missing-acceptance loop. `DESIGN.md` now owns the V4 module-level visual/UX contract, `IMPLEMENTATION_TASKS.md` has the canonical V4 task breakdown, `tracking/todos.md` has near-term implementation slices, and `tracking/test-cases.md` has the detailed V4 acceptance gate. No runtime code changed in this planning slice; V4 implementation should start from release-calendar/mobile discovery, shell/footer visual-mass reduction, merchandise-density cleanup, and six-width GUI evidence.
- Completed the Homepage Reference Polish V4 pre-implementation review gate before runtime edits. `ui-ux-pro-max` guidance reinforced mobile-first layout, high contrast, non-color-only states, accessible labels, and shadcn-backed search/menu primitives; the design-system search was useful mainly as a contrast warning because its black/gold recommendation would worsen the current dark-mass problem. Hosted Render Playwright GUI evidence at 1440, 1280, 1024, 768, 390, and 320 widths showed no page-level horizontal overflow, focusable real search, one official amount-free Pay Later message, and accessible 48px+ mobile release-day targets. Open V4 issues are now concrete: sticky header remains 143-146px with a deep navy frame, desktop/tablet calendar panels still measure roughly 1116-1256px tall, mobile product cards start around 1624-1647px because inline calendar/details consume roughly 962-1008px, mobile footer remains a 606px dark block, and some non-hero section headings still compute at 900 weight. Evidence screenshots were moved to `/private/tmp/paypal-retail-homepage-v4-preaudit-20260628/`.
- Started Homepage Reference Polish V4 implementation with the release-calendar mobile/density slice. Home now shows a compact release date rail and selected-release card before the full month calendar, moves the full shadcn calendar behind a mobile `Collapsible`, keeps the full-calendar trigger at a 44px touch target, and removes the old oversized selected-release details card in favor of compact color-independent legend copy. Focused verification passed `npm test -- web/src/features/catalog/HomePage.test.tsx web/src/styles/global.test.ts` (23 tests) and `npm run typecheck`; local Playwright GUI on `http://127.0.0.1:5176` verified no horizontal overflow, desktop calendar panel height reduced to about 890px, mobile full calendar closed by default at 390px/320px, first product card depth improved to about 1086px at 390px and 1155px at 320px, and the 320px full-calendar expand interaction opens without page overflow. Console errors during local GUI were expected missing Express API/env fetches because the Vite visual pass ran without the API server secrets. Remaining V4 calendar work is dynamic release-date selection with `aria-live` feedback and adjacent product-shelf updates.
- Continued Homepage Reference Polish V4 with the dynamic release-calendar discovery slice. Release days now carry product slugs, compact date chips and full shadcn calendar day buttons share one selected-release state, the selected-release card and adjacent shelf context update through `aria-live`, and the adjacent `Pre-order now` shelf reorders to show the selected release product first. Focused verification passed `npm test -- web/src/features/catalog/HomePage.test.tsx web/src/styles/global.test.ts` (24 tests), `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run build:web`, and `git diff --check`. Local Playwright GUI on `http://127.0.0.1:5176` verified at 1440px that selecting June 18 updates the chip, full calendar selected day, context, and first shelf product to `Pucky Vinyl Figures 2`, and selecting June 10 from the full calendar updates the first shelf product to `Molly Plush 2`; at 390px and 320px the full calendar stays collapsed by default, the trigger remains 44px tall, compact chip selection updates the shelf, expanded full-calendar selection stays synchronized, and no page-level horizontal overflow was observed. Console errors during local GUI were expected missing Express API/env fetches because the Vite visual pass ran without the API server secrets.
- Closed the Homepage Reference Polish V4 desktop/tablet release-calendar density follow-up. The pre-check showed 1024px tablet landscape still behaved like desktop because the full-calendar open breakpoint was `1021px`, leaving the release board about `866px` tall. The full-calendar default-open media query now starts at `1181px`, and CSS exposes the collapsible trigger plus closed-content hiding for the `1021px-1180px` tablet band without changing the wider page grid. Local Browser GUI on `http://127.0.0.1:5176` verified 1280px keeps the full desktop calendar at roughly `872px` board height, 1024px now starts collapsed with a 44px trigger and drops to roughly `467px` board height, 768px remains compact/collapsed, and the 1024px expanded calendar plus June 18 date selection updates the selected chip/day, shelf context, and first shelf product to `Pucky Vinyl Figures 2` with no page-level horizontal overflow. Evidence is in `/private/tmp/paypal-retail-homepage-v4-calendar-density-20260628/`.
- Closed the Homepage Reference Polish V4 shell/footer visual-mass slice. The buyer shell now uses light utility/header surfaces with dark readable text, keeps deep navy as one slim desktop product-nav row, preserves compact mobile logo/search/account/cart/menu controls, and splits the old dark footer block into a light collector-updates band plus a shorter navy base with retail actions. The real V3 search form still focuses, accepts text, and submits `molly` to `/products?q=molly`. Verification passed `npm test -- web/src/app/App.test.tsx web/src/styles/global.test.ts`, `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run build:web`, and local Browser GUI at 1440x900, 390x844, and 320x740 with no page-level horizontal overflow. Evidence is in `/private/tmp/paypal-retail-homepage-v4-shell-footer-20260628/`. Local browser console errors were expected Vite-only API fetch failures because the Express/Supabase API server was not running.
- Closed the Homepage Reference Polish V4 typography plus hero/trust reconciliation slice. Homepage hero text now keeps the expressive POP MART feel at 800 weight with reduced scale and softer image chrome, trust cards use compact light surfaces, and non-hero commerce headings/labels now stay in the V4 650-800 target range instead of drifting back to 900-heavy section/product typography. Focused style and homepage tests cover the weight contract plus existing search behavior. Local Browser QA on `http://127.0.0.1:5177/` verified 1440x900, 390x844, and 320x740 have no page-level horizontal overflow; computed weights include hero `800`, section heading `760`, trust/product title `750`, and product price `760`; and the real header search submits `molly` to `/products?q=molly`. Evidence is in `/private/tmp/paypal-retail-homepage-v4-typography-hero-trust-20260628/`. Console errors during local GUI were expected Vite-only API fetch failures because the Express/Supabase API server was not running. The broader V4 merchandise-density, Pay Later/loading, and six-width final GUI gate remain open.
- Closed the Homepage Reference Polish V4 merchandise-density slice. Home product cards now expose category/series, status, and pickup metadata through shadcn `Card`/`Badge` composition; category and popular-series rails use shadcn `ScrollArea` with explicit mobile containment; promo cards use generated image-led assets for New arrivals, Limited drops, and Pickup nearby; unsupported rewards copy stays absent; the popular-series rail now precedes the Home Pay Later surface; and the V3 real search path still submits to `/products?q=molly`. The first local Browser QA pass caught mobile page-level horizontal overflow from the new ScrollArea rails (`docWidth 682` at 390px/320px); the rail wrapper/viewport containment fix brought 1440x900, 390x844, and 320x740 back to `horizontalOverflow=false` while preserving generated promo images and product metadata. Scrolled browser evidence also confirmed lazy popular-series images load with `naturalWidth=900`. Evidence is in `/private/tmp/paypal-retail-homepage-v4-merchandise-density-20260628/`. Focused tests passed `npm test -- web/src/features/catalog/HomePage.test.tsx web/src/styles/global.test.ts` (`26` tests). The broader V4 Pay Later/loading follow-up and six-width final GUI gate remain open.
- Closed the Homepage Reference Polish V4 Pay Later/loading follow-up and final six-width GUI gate. Home now renders shadcn `Skeleton` placeholders for pending release/product/category/promo/series modules instead of empty/mock-looking loading areas, while the full-server Pay Later surface remains after merchandising and reaches official `paypal-message` ready state without provider or amount fallback. The initial Vite-only browser check showed the expected fallback because that target cannot fetch `/api/paypal/sdk-config`; rebuilding the production bundle with `VITE_API_BASE_URL=http://127.0.0.1:3018` and running the full Express server verified the real acceptance path. Verification passed `npm test -- web/src/features/catalog/HomePage.test.tsx web/src/styles/global.test.ts` (`27` tests), `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run build:web`, `npm run build:server`, and `git diff --check`. Browser GUI evidence at 1440x900, 1280x800, 1024x768, 768x1024, 390x844, and 320x740 found no page-level horizontal overflow, official Pay Later ready with `paypal-message` mounted and no fallback, labelled 58px release date chips, collapsed full calendar at 1024px and below with a 44px trigger, and search routing to `/products?q=molly`. Evidence is in `/private/tmp/paypal-retail-homepage-v4-paylater-loading-20260628/`.

## 2026-06-29

- Ran the hosted Render V4 homepage smoke after the deployed V4 merge. In-app browser QA on `https://retail-demo.onrender.com/` covered 1440x900, 1280x800, 1024x768, 768x1024, 390x844, and 320x740. Every breakpoint passed with no page-level horizontal overflow, one official Home Pay Later `paypal-message` mounted and no fallback, compact calendar behavior at 1024px and below, desktop full-calendar rendering at 1280px+, a real typeable search input, and no captured console warnings/errors. The search interaction accepted `molly`, routed to `/products?q=molly`, and settled with product results after the transient `Loading products` state. Evidence is in `/private/tmp/paypal-retail-homepage-v4-render-smoke-20260629/`.
- Locked the Category + PDP Reference Polish V5 planning contract before runtime edits, using the latest live Category/PDP review, Sakura-reference comparison, and `ui-ux-pro-max` guidance. Added the focused implementation handoff at `docs/superpowers/plans/2026-06-29-category-pdp-reference-polish-v5.md`, updated `DESIGN.md` with the V5 UX contract and inspection standards, added canonical open checklist rows in `IMPLEMENTATION_TASKS.md`, aligned the near-term queue in `tracking/todos.md`, added detailed acceptance rows in `tracking/test-cases.md`, and rerouted `PLAN.md` to the V5 baseline/review gate as the next page-polish slice. No runtime code changed in this planning slice; implementation should start with the V5 preaudit, then Category Pay Later/filter/card-state work, a Category review gate, PDP coming-soon/review/tab/sticky-action work, a PDP review gate, and final six-width GUI evidence.
- Refined the Category + PDP Reference Polish V5 planning contract after user review found the first task list still too high-level and at risk of missing requirements after deploy. Expanded the implementation handoff with a last-review coverage matrix, route-specific preaudit metrics, detailed Category Pay Later wrapper rules, filter/sort toolbar and mobile Sheet behavior, product-card state mapping, unreleased PDP gating, purchase-rail support-module compaction, real-data-only review requirements, mobile detail navigation acceptance, sticky Add to cart behavior, and hard blockers that prevent V5 closure. Mirrored the same missing-prevention rules into `DESIGN.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md`. No runtime code changed in this refinement.
- Ran the focused hosted Render V5 preaudit before runtime edits with Computer Use state confirmation, Playwright screenshots/snapshots, and `ui-ux-pro-max` design-system/UX/shadcn guidance. Evidence is under `/Users/tengtao/Development/demo-projects/.playwright-mcp/v5-preaudit-20260629/` and covers `/products` at 1440 and 390, released `/products/blind-boxes-2` at 1440 and 390 with Product facts/Customer reviews tab clicks, and unreleased `/products/blind-boxes-1` at 390. Findings now mirrored into `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md`: Category Pay Later is mounted but detached from page tokens, desktop filters remain sidebar-heavy, coming-soon cards need stronger top-right labels plus muted media, released PDP support modules still create low-density rail/blank-gallery space, mobile detail tabs interact but hide offscreen triggers, released mobile PDP lacks a sticky Add to cart candidate after scroll, and unreleased PDP still exposes disabled Add to cart plus disabled PayPal/Pay Later DOM instead of hiding purchase/payment surfaces. The full six-width/five-route GUI matrix remains a V5 final-close gate because the live Playwright transport was intermittently slow during this preaudit.
- Continued Category + PDP Reference Polish V5 with the first Category runtime slice. Category Pay Later now sits inside a merchant-owned shadcn `Card` wrapper with compact warm token chrome, a short buyer-facing label, and a dedicated official-message slot while avoiding CSS against PayPal SDK internals. Category cards now derive a coming-soon state from not-released product status, render a top-right `Coming soon` text badge, mute/desaturate unreleased media, expose `data-purchasable="false"`, preserve PDP navigation, and let unreleased state win over sale-badge collisions. Updated `DESIGN.md`, the V5 implementation handoff, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` to mark only this focused slice complete; Category control/browser review, final hosted Pay Later readiness, and PDP V5 remain open. Verification passed `npm test -- web/src/features/catalog/CategoryPage.test.tsx web/src/styles/global.test.ts` (`27` tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, `npm run build:web`, and `git diff --check`. `npm run build:web` emitted only the existing Vite large-chunk warning.
- Continued Category + PDP Reference Polish V5 with the Category control-hierarchy runtime slice. The dominant desktop Category filter sidebar is removed from the runtime markup; Category now leads the grid with result context, quick category chips, sort, an `All filters` shadcn Sheet trigger, active filter chips, reset, then product cards. Secondary filters remain available in shadcn Sheets on desktop/mobile with title/description, selected state, and disabled pickup reason, while the compact mobile filter trigger remains intact. Updated `DESIGN.md`, the V5 implementation handoff, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` to mark only the runtime/interaction coverage complete; explicit apply/reset polish, direct URL/reload/back-forward route-state evidence, 320px browser overflow proof, final hosted Pay Later readiness, and PDP V5 remain open. Verification passed `npm test -- web/src/features/catalog/CategoryPage.test.tsx web/src/styles/global.test.ts` (`29` tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, `npm run build:web`, and `git diff --check`. `npm run build:web` emitted only the existing Vite large-chunk warning.
- Ran a local production-preview Category GUI review gate on `http://127.0.0.1:4177/products`. Playwright MCP screenshots/snapshots at 1440, 390, and 320 showed the sidebar removed, the new result/quick-chip/sort/All-filters/active-chip toolbar above products, the Pay Later card integrated before the grid, sale and coming-soon cards visible, and the compact mobile filter row retained. The first 390px pass showed product cards just below the fold, so the mobile CSS now hides duplicate toolbar context/sort on small screens because those controls are available through the filter Sheet; the tightened 390px snapshot shows the product grid in the mobile viewport when the catalog area is reached. Evidence files are under `/private/tmp/paypal-retail-category-v5-gui-20260629/`. Local preview emitted expected API/fallback console errors because the Express/Supabase API server was not running, and the Playwright MCP surface did not expose click/evaluate tools in this session; therefore final hosted SDK readiness, exact DOM overflow metrics, route-state/back-forward evidence, and the six-width/five-route V5 matrix remain open.

## 2026-06-30

- Continued Category + PDP Reference Polish V5 with the first PDP runtime slice. `ProductDetailPage` now branches on `purchasable`: released products keep the existing price -> Pay Later -> purchase options -> Add to cart -> secured PayPal frame -> compact trust flow, while unreleased products render a shadcn-backed `product-release-preview` card with disabled `Coming soon` action and release context. For unreleased PDPs, Add to cart, PayPal frame/buttons, Pay Later message/button, payment/trust support cards, review summary/cards/social-proof cards, enabled notify/email capture, and sticky purchase candidates are absent, while breadcrumb, gallery, title/status/vendor, price, facts/detail tabs, shipping, and Q&A remain inspectable. The PDP review gate found and the slice fixed a social-proof edge case that could reopen Customer reviews on unreleased PDPs; a later 2026-07-01 follow-up restored direct SDK custom-element layout sizing after the wrapper-only cleanup caused PayPal button autofit regression. Updated `DESIGN.md`, the V5 implementation handoff, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` to mark only the focused runtime/test coverage complete; hosted browser proof at 1440/390/320, released-PDP support density, review summary, mobile details, sticky Add to cart, second PDP review gate, and final V5 matrix remain open. Verification passed `npm test -- web/src/features/catalog/ProductDetailPage.test.tsx web/src/app/App.test.tsx web/src/styles/global.test.ts` (`45` tests).
- Added local browser proof for the V5 unreleased PDP gating slice. Vite-only `/products/blind-boxes-1` rendered not-found because that exact hosted preaudit slug is not available in local fallback mode, so the browser proof used the actual local unreleased fallback PDP `/products/vinyl-figures-7?qa=pdp-v5-unreleased-gating`. Playwright MCP verified 1440x900, 390x844, and 320x740: preview card present, disabled `Coming soon` action present, no Add to cart, no PayPal frame/SDK markers, no Pay Later message, no payment/trust grid, no review/social-proof cards, facts/shipping/Q&A/gallery still present, and no horizontal overflow. Evidence metrics are in `/private/tmp/paypal-retail-pdp-v5-unreleased-gating-20260630/metrics.json`; screenshots are `.playwright-mcp/pdp-v5-unreleased-desktop-1440.png`, `.playwright-mcp/pdp-v5-unreleased-mobile-390.png`, and `.playwright-mcp/pdp-v5-unreleased-small-320.png`. Hosted `/products/blind-boxes-1` remains in the final V5 matrix gate.

## 2026-07-01

- Closed the local Category + PDP Reference Polish V5 final evidence gate. Playwright browser evidence covers `/products`, `/products?category=blind-boxes`, `/products?q=molly`, released `/products/blind-boxes-2`, and unreleased `/products/blind-boxes-1` at 1440, 1280, 1024, 768, 390, and 320 widths under `/Users/tengtao/Development/demo-projects/.playwright-mcp/paypal-retail-category-pdp-v5-final-20260701/`; the initial cold `/products` 1440 capture was rerun as `category-all-1440-rerun.png` after API settle and showed 25 cards, no skeletons, and no horizontal overflow. The matrix found no page-level horizontal overflow, no unreleased PDP payment/add-to-cart/sticky/review leak, mobile filter Sheet rows at 44px+ touch sizing, and working released-PDP sticky purchase behavior. A focused 390px browser check now activates the mobile `Customer reviews` tab, scrolls it into view, shows one real review card (`Pickup was smooth`), reports tablist `clientHeight=55` and `scrollHeight=55`, and has no React/browser errors. Remaining local console warnings are PayPal sandbox Pay Later presentment timeouts with buyer-safe fallback available; hosted Render smoke remains the post-deploy follow-up.
- Fixed the primary released PDP review-data gap found during V5 final matrix. The linked seed now attaches a real active POP MART review to `blind-boxes-2`; `npm run seed:linked` was made idempotent after live demo cart mutations by reconciling mutable seeded `app.cart_items` rows before upserts. The catalog PDP API now selects and returns review IDs, and the frontend defensively keys API review cards if a future payload is missing an ID, removing the API-backed `ProductDetailPage` React key warning. Verification passed `npm test -- supabase/seed/seed-data.test.ts server/tests/catalogRepository.test.ts server/tests/catalogRoutes.test.ts web/src/features/catalog/ProductDetailPage.test.tsx web/src/styles/global.test.ts` (`63` tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, `npm run build:web`, and `git diff --check`. `npm run build:web` emitted only the existing Vite large-chunk warning.
- Fixed the PayPal button autofit regression introduced by the V5 unreleased-gating slice. Root cause: SDK custom-element sizing selectors for `paypal-button`, `paypal-pay-later-button`, wallet custom elements, and `paypal-message` were removed in favor of wrapper-only sizing, so official PayPal-rendered buttons could fall back to intrinsic/autofit width inside the secured frame. Restored direct SDK-element `display:block`, `min-height`, and `width:100%` rules while keeping merchant chrome on wrapper classes. TDD red/green verification passed `npm test -- web/src/styles/global.test.ts`.

## 2026-07-02

- Closed Category + PDP Refinement V6 with the approved A+B Category direction and A PDP support placement with simpler B-style wording. Category now removes the large `All products` hero, compacts desktop controls, hides top filter controls on mobile, keeps product imagery in the 320px and 390px first viewport, renders a circular Lucide/shadcn floating filter Sheet trigger, and changes Category Pay Later from a large card to a centered quiet strip. Released PDP now keeps the purchase rail commerce-only, moves four support tiles between collector story and series lineup, uses a 2x2 mobile support tile grid, labels reviews as `Customer reviews (1)`, and replaces visible score text with real-data-only SVG/icon star rows plus count while preserving accessible rating meaning. Footer now renders verified local SVG marks from the PayPal logo repo for PayPal, Pay Later, Visa, Mastercard, Apple Pay, and Venmo. Verification passed focused `npm test -- web/src/features/catalog/CategoryPage.test.tsx web/src/features/catalog/ProductDetailPage.test.tsx web/src/app/App.test.tsx web/src/styles/global.test.ts` (`63` tests), `npm run typecheck`, `npm run lint`, `npm run format:check`, and `git diff --check`. Final local Playwright evidence on Vite `http://127.0.0.1:5173` covers `/products`, `/products?category=blind-boxes`, `/products?q=molly`, released `/products/blind-boxes-2`, and local unreleased `/products/vinyl-figures-7` at 1440, 1280, 1024, 768, 390, and 320 widths under `/Users/tengtao/Development/demo-projects/.playwright-mcp/paypal-retail-category-pdp-v6-final-20260702/`; `metrics.json` reports `30` checks and `0` failures.
- Checkpointed and pushed Category + PDP Refinement V6 as commit `35af53ff` on branch `codex/category-pdp-v6-polish`, then ran a hosted Render probe against `https://retail-demo.onrender.com`. Render health returned `200`, but production had not picked up the V6 build yet: hosted Category still rendered the previous `catalog-paylater__card` instead of `catalog-paylater__strip`, and hosted PDP still rendered the old `5.0 / 5 Based on 1 collector review` text with no `data-rating-stars` SVG star rows. Evidence screenshots are under `/Users/tengtao/Development/demo-projects/.playwright-mcp/paypal-retail-category-pdp-v6-render-smoke-20260702/`. The V6 hosted smoke remains pending until `35af53ff` is deployed.
- Landed Category + PDP Refinement V6 on `main` as commit `7f1c5d70`, after fixing the stale cross-app search test to wait for the V6 `Products` region instead of the removed `All products` heading. Fresh post-merge verification on `main` passed `npm run verify` (`66` files, `530` tests, typecheck, lint, format) plus `npm run build` with only the existing Vite chunk-size warning. Render deployed asset `index-DY1tYvnK.js`; hosted smoke on `https://retail-demo.onrender.com` covered Category at 1440/390/320, released PDP at 1440/390, and hosted unreleased PDP `/products/blind-boxes-1` at 390 with 0 failures, no captured rerun console warnings/errors, no horizontal overflow, V6 Pay Later strip present, mobile filter FAB visible at 390/320, first product media in the mobile viewport, support tiles before series lineup, SVG rating stars with yellow filled state, unreleased purchase/payment/review content hidden, and footer marks for PayPal, Pay Later, Visa, Mastercard, Apple Pay, and Venmo. Evidence screenshots are under `/Users/tengtao/Development/demo-projects/.playwright-mcp/paypal-retail-category-pdp-v6-render-smoke-after-main-20260702/`.

## 2026-07-03

- Locked and refined the Cart + Checkout A+ Polish design direction before runtime edits. The user selected Superpowers visual companion Variant A (`A Compact Retail Flow`) and requested a `ui-ux-pro-max` sub-agent review. Added the focused design contract at `CART_CHECKOUT_A_PLUS_SPEC.md`, updated `PLAN.md` to point the next page-polish slice at that spec, added A+ rules to `DESIGN.md`, added open M16 checklist rows in `IMPLEMENTATION_TASKS.md`, and mirrored acceptance criteria into `tracking/todos.md` and `tracking/test-cases.md`. Follow-up review by sub-agent `019f2851-83f7-77b1-a21e-25775b2d81cf` found missing gates for stale/loading payment readiness, Pay Later amount refresh, cart-to-checkout binding continuity, sticky/modal/keyboard behavior, placeholder-language drift, and measurable inspection standards; those gaps are now reconciled into the spec and trackers. No runtime code changed in this planning slice.
- Continued Cart + Checkout A+ Polish with the first runtime slice. Cart now uses a compact `Bag` header, item count/subtotal context, visible item line totals, and buyer-safe summary placeholders (`Calculated after Delivery/Pickup`, `Calculated in checkout`). Checkout now shows compact active-step progress, keeps initial payment surfaces hidden until selection/readiness, preserves thumbnail-backed Order Summary content, blocks provider actions when the checkout draft is missing, suspends selected provider actions after upstream shipping edits, and reserves mobile safe-area padding for sticky checkout/payment actions. Verification passed focused Cart/Checkout/App/style tests, `npm run typecheck`, `npm run lint`, `npm run format:check`, and `git diff --check`. Live API-backed Browser evidence on `localhost:5173` + API `localhost:3000` is under `/private/tmp/paypal-retail-cart-checkout-a-plus-20260703/`: `/cart` at 320/390/1024/1440 shows populated product-first rows, line totals, and no horizontal overflow; initial `/checkout` at 320/390/1024/1440 shows two summary thumbnails, progress `Delivery - Shipping address - 1 of 4`, zero provider actions, and no horizontal overflow. Remaining A+ rows are selected-provider sticky/payment proof, stale/loading/failed readiness coverage, Pay Later amount/message refresh, Pickup progression, sticky/modal/focused-input overlap, and final warning triage.
- Continued the Cart + Checkout A+ payment-readiness slice. Checkout now accepts an explicit `paymentReadiness` state per fulfillment draft and withholds selected provider actions/card fields for missing, syncing, stale, recalculating, and failed checkout payment states with buyer-safe retry/sync copy. Focused tests cover callback-level blocking, mobile selected non-card sticky rendering, inline card behavior, and App-level provider-scope suppression for PayPal, Pay Later, and card when readiness fails. Added `PayLaterAmountMessage` rerender coverage proving the official Pay Later message fetch and DOM amount refresh when the selected checkout total changes from `$25.98` to `$37.98`. Browser selected-provider/sticky overlap evidence and eligibility-error proof remain open before A+ closeout.
- Added a narrow live browser proof for the A+ selected Pay Later mobile path with Vite on `127.0.0.1:5173` and the API on port `3000`. At the 390px mobile breakpoint, initial checkout reported `providerActions: 0`, `stickyActions: 0`, progress `Delivery - Shipping address - 1 of 4`, and no horizontal overflow. After Delivery shipping, billing, shipping option, and selecting Pay Later, the page reported selected row `paylater`, exactly one selected provider action in the mobile sticky area, PayPal provider scope `checkout/paylater/ready`, Pay Later message amount `$25.64`, no fallback duplicate, no horizontal overflow, and no sticky/footer overlap. Screenshot: `/private/tmp/paypal-retail-cart-checkout-a-plus-20260703/checkout-mobile-selected-paylater-390-clean.png`. The browser log API still surfaced stale pre-server `localhost` cart-restore errors from the earlier tab load, so full warning/error triage remains open.

## 2026-07-06

- Continued Cart + Checkout Round 2 with sub-agent review `019f36df-fd6b-7851-9807-000a5a25ceeb` focused on the selected Pay Later pre-activation warning and missing recalculation/failure visual rows. The review found the timeout fallback is buyer-safe expected behavior and should not be warning-class telemetry, while recalculation/failure visual rows should be produced by helper-owned route interception or mocked API responses rather than buyer-visible `?qa=` toggles. Runtime now logs expected Pay Later message timeout fallback as structured `console.info("[paypal-retail-demo] Pay Later message fallback shown", { reason: "timeout", ... })`; real SDK errors, presentment fetch failures, and content-application failures still log as errors. Focused regression coverage in `paymentActionFailure.test.tsx` proves the fallback appears, `console.warn` is not called, and the structured info payload includes amount, buyer country, currency, placement, and timeout reason. Verification passed `npm test -- --run web/src/features/payments/paymentActionFailure.test.tsx web/src/features/payments/PayLaterAmountMessage.test.tsx` (`15` tests). Updated `CART_CHECKOUT_A_PLUS_SPEC.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` so Pay Later timeout warning triage is closed, and remaining deploy quality is narrowed to hosted smoke plus helper-owned recalculating/failed rows with visible readiness copy, latest displayed totals, surface-scoped provider counts, and zero create-order request/callback deltas.
- Closed the local helper-owned recalculating/failed readiness evidence gap for Cart + Checkout Round 2. Added committed helper `tools/round2-checkout-readiness-evidence.playwright.js` plus `npm run evidence:round2:checkout-readiness`, which uses browser route interception rather than buyer-visible QA flags and writes screenshots/metrics to `/private/tmp/paypal-retail-round2-checkout-readiness-evidence/`. The evidence covers `checkout-recalculating-readiness-390`, `checkout-failed-readiness-390`, and `checkout-shipping-save-failed-390`; `metrics.json` reports no failed rows, zero provider nodes, zero create-order request/callback deltas, zero horizontal overflow, and zero sticky occlusion for the readiness/save-failure rows. Recalculating and failed rows assert buyer-safe readiness copy plus displayed total/promo/shipping/tax labels, and the save-failure row asserts the retryable Shipping address alert plus exactly one intercepted shipping-address `500`, exactly one matching checkout-draft app failure log, and zero unrelated console errors. The helper now fails if label, provider-count, request/callback, overflow, sticky-occlusion, or console allowlist expectations regress. Updated `CART_CHECKOUT_A_PLUS_SPEC.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` so local recalculating/failed/save-failure rows are closed while hosted smoke and hosted required-state sticky/fixed overlap proof remain open before deploy closure.
- Added the Round 2 hosted/local deploy-smoke helper and closed the local working-tree smoke gap. New helper `tools/round2-hosted-checkout-smoke.playwright.js` plus `npm run evidence:round2:hosted-smoke` targets the current Playwright page origin and records `/cart` and `/checkout` at `320`, `375`, `390`, `768`, `1024`, `1280`, and `1440`, plus 390px checkout billing, shipping-method, no-method, selected PayPal, expanded/collapsed order details, selected Pay Later, selected Card, focused input, footer clearance, mobile menu, minicart, and sign-in dialog states. Local Vite/API evidence at `/private/tmp/paypal-retail-round2-local-smoke-evidence.json` passed `27` rows with `failedRows: []`; screenshots use `/private/tmp/paypal-retail-round2-local-smoke-evidence-*`, and footer clearance reports zero sticky occlusion after adding mobile footer bottom reserve while `.checkout-sticky-summary` exists. The helper records route-scoped console/response issues, horizontal overflow, target-level sticky overlap, provider buckets by surface, create-order request/callback baselines/deltas/cumulative counts, grabber ARIA/rects, order-sheet state, focused element, and overlay suppression. A current Render comparison returned health `200` (`dbg_ba8fd084a906`) but still served `/assets/index-CuNnoNV5.js` and `/assets/index-CajkqOhp.css`; hosted smoke against that pre-deploy asset failed on `cart-first-pass-320` cart-restore fetch noise and `checkout-footer-clearance-390` with seven sticky-occluded targets. Updated `CART_CHECKOUT_A_PLUS_SPEC.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md`. Verification passed `npm test -- web/src/styles/global.test.ts`, `npm run verify` (`565` tests across `68` files plus typecheck/lint/format), and `git diff --check`; the hosted smoke gap was closed by the following post-deploy Render evidence entry.
- Closed the Cart + Checkout Round 2 post-deploy hosted smoke gate after the user deployed commit `ef8d902b` to Render. Render health returned `200` with debug ID `dbg_d40682632c40`, live assets changed to `/assets/index-uhOMu6vW.js` and `/assets/index-DdbX4s-t.css`, and `npm run evidence:round2:hosted-smoke` against `https://retail-demo.onrender.com` wrote `/private/tmp/paypal-retail-round2-hosted-smoke-after-deploy.json` with `27` rows and `failedRows: []`. The previously failing `cart-first-pass-320` row now has no console warnings/errors or response issues, `checkout-footer-clearance-390` reports `occludedByStickyCount: 0`, and mobile menu/minicart/sign-in overlay rows report no checkout sticky summary while overlays are open. Updated `CART_CHECKOUT_A_PLUS_SPEC.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` to close the hosted smoke/deploy evidence gate.

## 2026-07-07

- Planned Checkout Round 3 pickup and drawer repair after the user approved Option A from the visual companion. Added `ROUND3_CHECKOUT_PICKUP_DRAWER_PLAN.md` as the implementation handoff, then mirrored the plan into `CART_CHECKOUT_A_PLUS_SPEC.md`, `DESIGN.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md`. The active scope is pickup store-picker/header layering, picker cancel fallback to Pickup location re-submit, mobile order-detail duplication removal through a single bottom drawer, billing-submit optimistic progression, real backend-backed promo activation, selected PayPal/Pay Later action width parity, and a repeatable Round 3 evidence helper. No runtime code changed in this planning slice; implementation remains open.
- Completed Round 3 Slice 1 local implementation for pickup picker layering and cancel fallback. Added failing-first coverage in `CheckoutPage.interactions.test.tsx` for guest Pickup ZIP -> picker -> Cancel -> editable Pickup location, dormant Store selection, no inline store cards, and no selected store commit, then hardened it with guest Escape/dismiss and direct no-`pickup_store` draft-update assertions. Added CSS coverage in `global.test.ts` proving `.checkout-modal` sits above `.site-header` while staying on the shadcn Dialog/Sheet layer; runtime CSS now sets `.checkout-modal` to `z-index: 50`. `CheckoutPage.tsx` now tracks explicit pickup picker close reasons, suppresses inline store tickets after cancel/dismiss without a confirmed store, and preserves the confirmed-store path. Verification passed `npm test -- web/src/features/checkout/CheckoutPage.interactions.test.tsx web/src/styles/global.test.ts` and `npm run typecheck`. Browser-width evidence remains open for the Round 3 evidence helper.
- Completed Round 3 Slice 2 local implementation for mobile order-detail ownership. Added failing-first coverage in `CheckoutPage.test.tsx` proving the mobile sticky drawer must be the only order-detail surface; the test first failed because the full complementary `Order summary` card still rendered on mobile. `CheckoutPage.tsx` now unmounts `CheckoutSummary` on mobile when the sticky drawer is active, while preserving the desktop/tablet summary and the fallback summary path when the mobile sticky drawer is suppressed. The sub-agent review found no blockers and requested one extra fallback regression, now covered by a mobile `suppressMobileStickySummary` test. Existing shadcn bottom Sheet interaction coverage stayed green for open/close/focus/payment-action behavior. Verification passed `npm test -- web/src/features/checkout/CheckoutPage.test.tsx web/src/features/checkout/CheckoutPage.interactions.test.tsx web/src/styles/global.test.ts` (`75` tests). Browser/API-backed drawer evidence remains open for the Round 3 evidence helper.
- Completed Round 3 Slice 3 local implementation for billing-submit progression. Added failing-first interaction tests proving slow Delivery billing should open Shipping options within the 250ms optimistic target and slow Pickup billing should open Pickup date within the same target while Payment method remains idle. Added rollback coverage proving failed slow billing saves return Delivery/Pickup flows to Billing with keyboard focus, inline retry state, and downstream payment actions blocked. `CheckoutPage.tsx` now extends the existing optimistic `saveStepAndEditNext` behavior from Shipping address to Delivery billing and Pickup billing, and submit-save failures reuse the checkout focus target so the buyer lands back on the failed section. Verification passed `npm test -- web/src/features/checkout/CheckoutPage.interactions.test.tsx` (`24` tests) and `npm test -- web/src/features/checkout/CheckoutPage.interactions.test.tsx web/src/features/checkout/CheckoutPage.test.tsx` (`60` tests). Browser/API-backed timing and visual proof remains open for the Round 3 evidence helper.
- Completed Round 3 Slice 4 local implementation for real promo activation. Added typed frontend helpers for checkout promo evaluate/apply/remove routes, then wired App checkout draft updates to evaluate recommended backend promos and apply only when the evaluation returns a positive discount. Zero-discount/rejected evaluations do not apply, promo activation network failures do not invent a discount, and the buyer summary updates only from the apply response. Failing-first App coverage proved the previous gap by expecting `/promos/evaluate` and `/promos/apply` calls before the signed `-$4.00 promo (SAVE10)` summary appears. After sub-agent review flagged thin drawer proof, `CheckoutPage.test.tsx` was expanded to assert the same signed promo line in the collapsed mobile drawer and expanded order-details sheet. Verification passed `npm test -- web/src/features/checkout/CheckoutPage.test.tsx` (`36` tests) and `npm test -- web/src/features/checkout/checkoutDraftApi.test.ts web/src/features/checkout/CheckoutPage.test.tsx web/src/app/App.interactions.test.tsx` (`109` tests). Browser/API-backed visual proof remains open for the Round 3 evidence helper.
- Completed Round 3 Slice 5 local implementation for selected payment width parity. Added failing-first CSS coverage for the missing checkout selected-payment slot contract, then added wrapper-only width/fill rules for the desktop summary slot, collapsed sticky action slot, expanded order-sheet payment slot, direct PayPal provider scope child, and PayPal/Pay Later standalone action wrappers. Official provider internals remain untouched. Verification passed `npm test -- web/src/styles/global.test.ts` (`19` tests) and `npm test -- web/src/styles/global.test.ts web/src/features/checkout/CheckoutPage.test.tsx` (`55` tests). Browser rect metrics remain open for the Round 3 evidence helper.
- Completed Round 3 Slice 6 local implementation for App-level payment readiness regression proof. Added focused capture coverage for stale/syncing readiness, missing selected-payment draft, collapsed mobile drawer activation for selected PayPal and Pay Later, expanded drawer activation for selected Pay Later, and selected-PayPal pickup picker suppression after returning upstream from Pickup payment. Existing App tests stayed green for no method, recalculating, failed, focused upstream input, open mobile menu, minicart Sheet, sign-in dialog, selected Card inline/no sticky provider behavior, expanded drawer selected PayPal activation, and opening/closing the drawer without payment. No runtime guard changes were needed after the fixture red pass was corrected. Verification passed `npm test -- web/src/app/App.checkout-paypal-capture.test.tsx web/src/app/App.interactions.test.tsx` (`86` tests). Browser/API-backed request and callback deltas remain open for the Round 3 evidence helper.
- Continued Round 3 with the mobile drawer/provider follow-up from GUI QA. The selected-payment action contract now includes PayPal, Pay Later, Apple Pay, Google Pay, and Venmo, with shared merchant-owned width/height rules and compact Pay Later message suppression inside sticky/order-sheet payment action slots. The collapsed order drawer no longer uses a separate button-like expand affordance: the summary surface remains the trigger and the visible handle is passive chrome. Preselected Pickup store summaries no longer show `Continue with this store`; the buyer proceeds directly to Billing with the store change/edit affordance still available. Updated `ROUND3_CHECKOUT_PICKUP_DRAWER_PLAN.md`, `CART_CHECKOUT_A_PLUS_SPEC.md`, `DESIGN.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md` so future evidence must measure equal selected-action rects for PayPal/Pay Later/Apple Pay/Google Pay/Venmo and prove the no-continuation store summary. Verification passed `npm test -- web/src/features/checkout/CheckoutPage.test.tsx web/src/features/checkout/CheckoutPage.interactions.test.tsx web/src/styles/global.test.ts` (`80` tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, and `git diff --check`. In-app browser CSS-fixture evidence at `390 x 844` on local Vite confirmed PayPal, Pay Later, Apple Pay, Google Pay, and Venmo selected-action wrappers/custom elements all measure `132 x 52`, compact Pay Later message display is `none`, and the drawer handle is passive (`88 x 18`, no border/shadow, `pointer-events: none`). Full API-backed checkout progression evidence remains open because the local API server requires missing env/secrets.
- Continued Round 3 evidence closure after committing the helper slice. Initial hosted Render execution of `tools/round3-checkout-pickup-drawer-evidence.playwright.js` first exposed helper drift: checkout readiness waited for literal `Molly Blind Boxes 2` even when the deployed cart contained different products, and pickup selection assumed a separate `Confirm pickup store` button even though the deployed flow had already committed the store. The helper now waits on checkout shell/actions/inputs and uses `commitPickupStoreSelection()` to support both confirm and immediate-commit picker variants; `server/tests/evidenceScripts.test.ts` statically prevents those assumptions from returning. The next hosted run then exposed a real optimistic-submit race: while Billing was still reconciling, Shipping options accepted a fast click and submitted static fallback `{"shipping_option_id":"ship_standard"}`, producing Render `PATCH /api/checkout/drafts/:id/shipping-option` `500` (`dbg_16f8a7235586`). `CheckoutPage.tsx` now keeps dependent primary actions disabled while their upstream step is `saving` or `recalculating`, so the next section can still open within 250ms without allowing stale fallback choices to hit the backend. Verification passed `npm test -- web/src/features/checkout/CheckoutPage.interactions.test.tsx server/tests/evidenceScripts.test.ts` (`25` tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, and `git diff --check`. Hosted Round 3 evidence remains open until this patch is deployed and the helper is rerun against the new Render asset.

## 2026-07-08

- Planned Round 4 Auth, Minicart, and Checkout surface polish after the user accepted the UI/UX findings. Added `ROUND4_AUTH_MINICART_CHECKOUT_POLISH_PLAN.md` as the detailed handoff, generated `mockups/round4-auth-minicart-checkout-polish.html` as the Superpowers visual companion reference board, and mirrored the scope into `DESIGN.md`, `IMPLEMENTATION_PLAN.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md`. This planning slice covers scoped transaction typography/favicons, email/password/register modal polish, minicart drawer hierarchy, checkout payment helper/order-sheet/safeguards polish, and pickup inventory-row wrap repair. No runtime code changed in this planning slice; implementation remains open.
- Incorporated the read-only Round 4 plan/AC sub-agent review. No P0 issues were found; the P1/P2 tightenings are now mirrored across `PLAN.md`, `ROUND4_AUTH_MINICART_CHECKOUT_POLISH_PLAN.md`, `DESIGN.md`, `IMPLEMENTATION_TASKS.md`, `tracking/todos.md`, and `tracking/test-cases.md`. The updates make Round 4 unambiguously active, add Register-specific modal AC, add a surface-by-width evidence matrix, require positive selected PayPal/Pay Later activation proof, metricize mockup comparison, require minicart quantity accessible names, include full/partial/sold-out pickup inventory proof, and require official PayPal runtime boundary evidence. The visual mockup now includes the Register state as well.
- Completed the Round 4 foundation slice locally. Added scoped `--transaction-*` typography tokens and applied them to Auth/Minicart/Checkout-owned transaction surfaces without changing Home/Category/PDP page typography contracts. Added a local `/favicon.svg` asset with an SVG title and linked it from `web/index.html` so future visual evidence does not rely on a missing default favicon request. Focused verification passed `npm test -- web/src/styles/global.test.ts` after a red pass proved the missing token/link contracts. Browser contrast checks, 320px label-fit proof, and console/no-`favicon.ico` evidence remain open in the Round 4 evidence/review slice.
- Resolved the read-only Round 4 foundation review P2 findings. The transaction font-family stack now defaults to shell tokens at `:root` and is overridden only by `.theme-popmart`, preserving the generic profile separation contract. Pickup inventory line item/status text now participates in the transaction weight layer, and `global.test.ts` proves both the POP MART-scoped transaction font contract and pickup inventory selector coverage. No P0/P1 findings remain for the foundation slice.

## 2026-07-09

- Completed the Round 4 Auth modal runtime polish locally. `AuthModalShell` now uses the accepted email-first copy (`Use your email to continue checkout.`), `inputmode="email"`, a single full-width primary `Continue` action, compact email summary rows for password/register states, low-emphasis `Edit email`, inline Lucide eye/eye-off password toggles, full-width `Sign in` / `Create account` CTAs, and specific `Checking...`, `Signing in...`, and `Creating account...` loading copy. Register keeps terms validation, no enabled social-auth behavior, `new-password` autocomplete, and cart/session semantics. Focused tests were added for the auth behavior and CSS contracts, and App interaction tests now assert the compact email summary instead of the old read-only email input. Verification passed `npm test -- web/src/features/account/AuthModalShell.test.tsx web/src/styles/global.test.ts` (`30` tests), broader `npm test -- web/src/app/App.interactions.test.tsx web/src/app/App.checkout-paypal-capture.test.tsx web/src/features/account/AccountPage.test.tsx web/src/features/account/AuthModalShell.test.tsx web/src/styles/global.test.ts` (`124` tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, and `git diff --check`. Browser width/focus-return/no-overlap screenshots remain open in the Round 4 evidence/review slice.
- Closed the read-only Round 4 Auth sub-agent P1/P2 findings locally. Buyer-visible auth copy no longer contains implementation/demo placeholders; auth inputs now have an auth-scoped `44px` minimum height on desktop as well as mobile, and the inline password wrapper preserves the same touch target. The accepted Delivery and Pickup checkout payment helper copy now uses `Choose a payment method.` in runtime and test fixtures, and the forbidden old-helper grep is clean. Fresh post-resume verification passed `npm test -- web/src/features/account/AuthModalShell.test.tsx web/src/styles/global.test.ts` (`30` tests), broader `npm test -- web/src/app/App.interactions.test.tsx web/src/app/App.checkout-paypal-capture.test.tsx web/src/app/App.test.tsx web/src/features/account/AccountPage.test.tsx web/src/features/account/AuthModalShell.test.tsx web/src/features/checkout/CheckoutPage.test.tsx web/src/styles/global.test.ts` (`178` tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, and `git diff --check`.
- Closed the follow-up read-only Round 4 Auth review nit from Locke. The reviewer found no P0/P1 blockers and accepted the main auth/helper-copy implementation, with one P2 request to replace the auth session status `Merging cart...`; runtime now uses buyer-facing `Saving your cart...`. Final verification passed the forbidden-copy grep, focused `npm test -- web/src/features/account/AuthModalShell.test.tsx web/src/app/App.interactions.test.tsx web/src/app/App.test.tsx web/src/styles/global.test.ts` (`114` tests), broader `npm test -- web/src/app/App.interactions.test.tsx web/src/app/App.checkout-paypal-capture.test.tsx web/src/app/App.test.tsx web/src/features/account/AccountPage.test.tsx web/src/features/account/AuthModalShell.test.tsx web/src/features/checkout/CheckoutPage.test.tsx web/src/styles/global.test.ts` (`178` tests), `npm run typecheck -- --pretty false`, `npm run lint`, `npm run format:check`, and `git diff --check`. The full Round 4 browser evidence gate remains open by design.
- Completed the Round 4 checkout payment/order-sheet runtime polish locally. The trust strip now uses buyer-facing labels (`Secure payments`, `Final totals`, `Delivery or pickup`, `Order recovery`) and compacts to mobile badge chips, while the expanded mobile order details remain a shadcn bottom Sheet with contained scroll, an absolute neutral grabber, less than 24px title-top padding, and no large blank header band. Existing close paths still work by handle, Escape, and scrim with focus returning to the collapsed summary trigger. Verification passed `npm test -- web/src/features/checkout/CheckoutPage.test.tsx web/src/styles/global.test.ts` (`59` tests) and broader `npm test -- web/src/app/App.checkout-paypal-capture.test.tsx web/src/app/App.interactions.test.tsx web/src/features/checkout/CheckoutPage.test.tsx web/src/styles/global.test.ts` (`145` tests), including blocked readiness gates plus selected PayPal/Pay Later collapsed and expanded sheet activation paths. Browser screenshot/metrics, sticky overlap, provider-count, and official runtime-boundary evidence remain open in the Round 4 evidence gate.
- Resolved the read-only Round 4 checkout review findings from Einstein. The reviewer found no P0 issues, flagged direct Card Fields iframe CSS as a P1 provider-boundary concern, and flagged stale tracker wording as P2. Runtime CSS now styles only the merchant-owned hosted-field container and `global.test.ts` forbids `.card-fields-checkout-action__hosted-field iframe`; `tracking/todos.md` now marks the checkout local slice complete while leaving browser/runtime evidence open. Final verification passed the focused checkout/style tests, broader App checkout tests, typecheck, lint, format check, and `git diff --check`.

## 2026-07-10

- Completed the Round 4 pickup inventory-row runtime polish locally. `PickupStoreInventoryRows` now gives the modal picker and collapsed inline preselected-store summary one shared item-name/status contract; desktop/tablet rows use a constrained two-column grid with right-aligned status, while sub-520px rows move status below a two-line clamped item name without forcing vertical word stacks. Draft reconciliation now labels zero-available stores `Sold out` instead of `Partial inventory`, preserving text-labelled full/partial/sold-out states. Failing-first focused coverage proved the missing classes/CSS and incorrect sold-out mapping, then passed `npm test -- web/src/features/checkout/checkoutDraftApi.test.ts web/src/features/checkout/CheckoutPage.test.tsx web/src/styles/global.test.ts` (`67` tests). Rendered 320/390/desktop wrap and overflow evidence remains open in the Round 4 evidence/review gate.
- Resolved the read-only Round 4 pickup review findings from Huygens. The reviewer found no P0 issues, identified a P1 gap where the actual collapsed preselected-store summary omitted item rows, and identified a P2 CSS test that could accidentally match the desktop grid rule. Runtime now extracts `PickupStoreInventoryRows` for both the modal ticket details and the collapsed selected-store summary; focused tests assert both surfaces, and the mobile CSS test requires the exact one-column declaration. The follow-up review confirmed both findings resolved with no new P0/P1 issues. Final verification passed `npm run verify` (`595` tests across `69` files plus typecheck/lint/format), `scripts/check-agent-system.sh`, Graphify refresh (`3012` nodes, `6161` edges), and `git diff --check`. Browser wrapping and overflow evidence remains open by design.

## 2026-07-11

- Closed the local Round 4 Auth, Minicart, Checkout, and Pickup deploy-quality gate after an adversarial read-only review found one real Auth focus-return defect and four evidence-helper weaknesses. Auth now restores focus to the actual opener, including the post-purchase guest-account trigger, with an App interaction regression. The evidence helper now measures the real Minicart quantity controls, hard-fails missing/generic accessible names or sub-44px targets, records exact Minicart PayPal/Pay Later ownership, requires scope-specific Auth/Minicart contrast samples, requires true store-level full/partial/empty Pickup states, outputs quality-95 JPEGs, decodes every capture with a suspicious-pixel retry/fail gate, and analyzes the matching `image/jpeg` MIME type.
- Final local evidence contains `31` required rows, `40` passing JPEGs, and `metrics.json` under `/private/tmp/paypal-retail-round4-local-auth-minicart-checkout-evidence/`, with a review-visible ignored copy under `.playwright-cli/round4-local-auth-minicart-checkout-evidence/`. The report has `missingRows: []`, `failedRows: []`, no issue rows, `65` scoped contrast samples with a `4.60:1` minimum, maximum near-black ratio `0.0027`, asserted Auth initial focus, six product-specifically named quantity controls per Minicart row, exact one-PayPal/one-Pay-Later Minicart ownership, dedicated visible full/partial/empty Pickup screenshots at 320/390/1440, placement-scoped official selected-provider nodes, and zero console/response/overflow/sticky-overlap failures.
- Read-only `ui-ux-pro-max` reviewer Pasteur (`019f4e7a-a903-70e3-898f-78f0942faa86`) inspected all 31 workspace-visible JPEGs and the report after the original `/private/tmp` evidence expired at the midnight environment rollover. Final verdict: no P0, P1, or P2 findings; all original and follow-up findings are closed; local Round 4 closure is unblocked. Fresh `npm run verify` passes `599` tests across `69` files plus typecheck/lint/format, `scripts/check-agent-system.sh` passes, Graphify is refreshed to `3015` nodes / `6163` edges / `173` communities, and hosted Render smoke remains the separate post-deploy gate.
- Pre-release reviewer Dewey (`019f4ec7-922e-7ca2-9748-2fb41aa21609`) found two P1 evidence gaps and two P2 repeatability/router gaps: official provider nodes were counted globally rather than within their checkout placement, Pickup screenshots did not visibly show partial/sold-out cards, Auth initial focus was recorded but not asserted, and `PLAN.md` still routed to the local browser gate. The helper now hard-fails wrong-placement official nodes, emits nine dedicated full/partial/empty Pickup screenshots at 320/390/1440, asserts real Auth input IDs, and treats the active modal as the overlap owner while preserving picker/header checks; `PLAN.md` now routes to deploy plus hosted evidence. The strengthened run passes 31 required rows and 40 JPEGs with no failures. Dewey's follow-up review found no P0/P1/P2 findings and marked the diff safe to commit/push.
- The first Round 4 hosted Render review kept deploy quality open after finding that selected PayPal could be attached but visually blank, checkout contrast rows could pass with no samples, touch-target reporting included a hardcoded 44px target instead of measured geometry, `Confirm pickup store` wrapped at 320px, and PayPal appeared as duplicated visible wordmark plus text. Root-cause tracing showed the helper waited only for custom-element attachment while SDK shadow content hydrated asynchronously; the runtime also provided no visual fallback during provider config loading. The correction adds a full-width disabled `Preparing ...` fallback, single visible wordmark labels, one-line 44px pickup actions, and actual 44px `paypal-hosted-card-field` controls. The helper now waits for `ready` provider scope plus visible official/shadow control geometry, measures selected actions and real touch targets, and requires nonempty checkout contrast samples. A Node runner captures the isolated helper result, writes `metrics.json`, and applies the failure exit gate. Focused tests, typecheck, lint, and a fresh 31-row/40-image local matrix pass with no missing or failed rows; full verification, correction review, commit/deploy, and fresh hosted proof remain next.
- Read-only correction reviewer Mendel (`019f5019-d909-7340-9d66-e96b40ad94c6`) found one P1 timing gap after the first correction: the visible fallback covered SDK configuration but disappeared while the PayPal runtime still hydrated. `PayPalSdkProviderScope` now keeps the fallback mounted through runtime `pending` and atomically replaces it only at `resolved`; selected-action evidence records and requires that runtime state. The helper now directly samples sticky total/promo and order-sheet breakdown/item text rather than relying on container colors. The refreshed 31-row/40-image matrix has `missingRows: []`, `failedRows: []`, `127` contrast samples at `4.60:1` minimum, measured targets at 44px+, and runtime-resolved selected provider geometry. At 320px the expanded PayPal action is hydrated, 52px high, and reachable within contained sheet scrolling even when just below the initial viewport. Mendel's follow-up found no P0/P1/P2 issues and marked the patch safe to commit/deploy for hosted verification. Fresh `npm run verify` passes `602` tests across `69` files plus typecheck/lint/format; the hosted Render gate remains open.
- Investigated the two asset `404` console entries reported after the Render origin changed to `https://paypal-retail-demo.onrender.com`. Git, `web/public`, `web/dist`, the deployed `index-CBeAxtU2.js` bundle, and the live catalog API all agree on the correct absolute `/assets/...` paths. Live health and exact asset probes returned `200`, and a fresh in-app browser load recorded no console warnings/errors; the PayPal Messages cache entries are informational. Hardened Round 2/3/4 evidence host classification to any HTTPS `*.onrender.com` service and added a Round 4 hosted `HEAD` preflight for the Apple Pay logo and seeded blind-box image. Focused TDD proof in `server/tests/evidenceScripts.test.ts` now locks the generic classification and preflight paths. Fresh `npm run build` emitted both assets at the expected paths, and full `npm run verify` passed typecheck, `603` tests across `69` files, lint, and format.
- Repaired the Round 4 hosted runner after the first command failed on a transient `page.goto net::ERR_ABORTED` and masked it as invalid JSON. Follow-up runs exposed two deterministic readiness gaps—Pickup submitted before guest-cart restoration and Minicart captured before cart population—plus a 30-second default on the intercepted Pickup request. Failing-first evidence-script coverage now locks explicit CLI-error handling, one hosted navigation retry, guest-cart/Minicart readiness, and the 90-second intercepted-request budget. The final run against deployed asset `index-CBeAxtU2.js` exited `0` with `31` rows, `40` JPEGs, `missingRows: []`, `failedRows: []`, `4.60:1` minimum contrast, 44px minimum measured target, and `0.0027` maximum near-black ratio. Fresh independent visual review remains open before the hosted deploy-quality gate closes.

## 2026-07-12

- Corrected the three findings from the refreshed hosted visual review with failing-first coverage: Auth/Minicart artwork now has a shared accessible failure fallback and evidence waits for viewport-visible image completion plus decode; selected Card keeps one inline `Pay by card` action and uses concise sticky guidance instead of disabled `Choose payment`; password/register email summaries use single-line ellipsis while preserving the 44px edit control. The exact current-code local matrix at SHA1 `cf77b82d3bd99ced8a57967e7a4c1d7d44298c48` passes `31` rows and `40` JPEGs with no missing/failed rows, `4.60:1` minimum contrast, 44px minimum measured target, and `0.0027` maximum near-black ratio. Fresh read-only review inspected all images and returned no P0/P1/P2 findings. `npm run verify` passes `606` tests across `69` files plus typecheck/lint/format, `scripts/check-agent-system.sh` passes, and `git diff --check` is clean. Hosted closure remains deploy, rerun against Render, and independent hosted review.
- Implemented the wallet SDK upgrade in an isolated worktree: `@paypal/react-paypal-js` moved from `9.2.0` to `10.1.2` and `@paypal/paypal-js` from `9.7.0` to `10.0.3`; Apple Pay `1.latest`, Google Pay JS, and the PayPal sandbox well-known association payload are now browser assets. Google Pay now uses the official PayPal React session hook and mounts the element returned by `createGooglePayButton()` directly into the 52px action container instead of showing a fake disabled button; Apple/Google/Venmo approvals reach the existing checkout capture callback with their created payment-session IDs. Failing-first tests cover the missing SDK assets, fake Google button replacement, disconnected App approval bridge, and 52px wallet styling. Hosted domain registration and payment-wall preselection eligibility remain open.
- Wallet upgrade verification passed `npm run verify` with `612` tests across `71` files plus typecheck/lint/format and `npm run build`. Local browser inspection confirmed `ApplePaySession` and Google `PaymentsClient` are functions, `<apple-pay-button>` is registered, the official Google-created element is a direct child of its merchant container and both measure `320x52`, and the well-known association route returns `200` with `9,095` bytes. Headless Chromium reports the Apple element itself as `display: none`, which is the expected ineligible-device state rather than a missing SDK regression.
- Resolved the independent wallet review's three P1 and one P2 findings with isolated red/green proof: Express now serves the dotfile association through an exact route; provider config context maps Google sandbox/production to `TEST`/`PRODUCTION`; Apple capture follow-up moved from pre-completion `onApprove` to `onApproveCompleted`; and Google uses the public PayPal React session hook to mount the official `createGooglePayButton()` result directly without selecting package-private DOM. Fresh full verification passes `612` tests across `71` files plus typecheck/lint/format. The final read-only hook review reran 62 focused tests plus `git diff --check` and found no P0/P1/P2 issues; it confirmed stale-effect guards, cleanup/session destruction, current click wiring, environment mapping, and one approval-to-capture chain.
- Completed checkout wallet preselection eligibility with failing-first coverage. Apple Pay and Google Pay rows now start absent and become selectable only after amount-aware PayPal eligibility plus `ApplePaySession.canMakePayments()` or PayPal-formatted Google `isReadyToPay()` succeeds; pending/errors stay absent and probes cannot create orders. Production-build browser QA caught and closed a Google readiness rerender loop. Independent review then caught a P1 generic-versus-amount-aware eligibility mismatch and a follow-up P1 one-render stale-key race. The final fix keys eligibility by provider/market/currency/mode/amount, fails closed synchronously when that key changes, refuses Google readiness while eligible methods are loading/erroring, and destroys the previous Google session exactly once. Final production bundle `index-JG0pAw7a.js` loads Apple Pay `1.latest`; ineligible/pending Apple and Google rows/actions remain absent. Fresh `npm run verify` passes `624` tests across `72` files plus typecheck/lint/format; `npm run build`, agent-system validation, Graphify refresh, and diff checks pass. The final read-only follow-up review ran `140` focused tests, found no remaining P0/P1/P2 issues, and confirmed the branch is clean. Render Apple domain registration and eligible Apple-device proof remain open.
- Approved the Post-Purchase Operations and Account Experience design after `ui-ux-pro-max` retrieval and incremental user review. The accepted architecture replaces the current catch-all Admin page with route-backed Orders, Lifecycle, Inventory, Webhooks, and Diagnostics workbenches; adds server-side URL-preserved filters, genuine received webhooks only, persisted sanitized runtime logs, atomic one-step merchant lifecycle transitions, and Account order/timeline refresh polish. The design is recorded in `DEMO.md`, `DESIGN.md`, and `IMPLEMENTATION_PLAN.md`; router, open task, todos, and acceptance rows are synchronized. No runtime code changed, and detailed implementation planning remains gated on written-spec review.
